# Roadmap TODO — items up for consideration, with origin back-references

date: 2026-06-22

status: living index. This is a **provenance-indexed backlog**: one row per open item, each
pointing back to where it originated (file:line). It does **not** replace the homes that carry
the reasoning — `ROADMAP.md` (curated roadmap + "Next to do"), `thinking/open-edges.md`
(topic-organized review surface with `[doable]/[decision]/[resource]` tags),
`thinking/open-considerations.md` (unresolved strategic questions), and `COHESION.md` (seams).
It aggregates them into one actionable list so nothing hides in a single doc.

**How to read the Origin column:** `file:line` is a **point-in-time anchor** (as of 2026-06-22) —
files shift, so the *section header* named in the row is the durable reference; the line number
is a convenience. Type legend mirrors open-edges: **[decision]** (user/product gate — surface,
don't resolve), **[doable]** (no blocker), **[resource]** (blocked on hardware/account/network),
**[backport]** (corpus-coherence work), **[explore]** (adjacent/new thinking).

---

## A. Decisions — gates that are the user's to make (surface, don't resolve)

> **Open-gate walkthrough outcomes (2026-07-09).** The user walked these; the authoritative record with
> reasoning homes is `../beta/DECISIONS.md` → "Decisions taken 2026-07-09". Summary:
> **A1** accepted (MPL-2.0 dep) · **A2+A12** recovery architecture decided — meer-blind invariant, separate
> custodial conditional-access recovery role, quorum and/or delegate, group-default + per-user, *mechanism →
> prototype* · **A5** kept as product item T51 · **A9** stable logical URI + portable offline
> proof-of-prior-identity · **A10** pluggable custody, safe default (PDS-held-default acceptable), self-control
> option · **A11** spike-then-decide (revocation-immediacy; DOI critical path) · **A13** deferred (placeholder
> "gating role"). Non-A: **Noria** adopted pending clearance · cooperative **legal-review** deferred to Phase-1 ·
> **T4** Croft = product / reference impl neutrally branded (vendor-neutral `drystone-*`) · **badge-teeth** =
> technical conformance with principles encoded as conformance requirements · **C15** closed (leave as per-conclusion).

| # | Item | Type | Origin (file:line) | Also tracked |
|---|---|---|---|---|
| A1 | **MPL-2.0 license gate** — `hpke-rs` mandatory for RFC 9420 HPKE; no permissive substitute. Compliance call, not code. | [decision] | `ROADMAP.md:102` | `AGENTS.md:56` |
| A2 | **Total-device-loss recovery anchor** — mnemonic seed vs social-recovery quorum vs broker-held encrypted backup. Largest residual risk; none chosen. | [decision] | `ROADMAP.md:109` | `thinking/open-edges.md:83`, `thinking/open-considerations.md:28`, `COHESION.md:212`, `AGENTS.md:64` |
| A3 | **S3 — quiet membership** (be in a group without exposing other edges). | [decision] | `thinking/open-edges.md:89` | `ROADMAP.md:63` |
| A4 | **S4 — multi-identity, no forced linkage** (distinct lineages, no provable correlation). | [decision] | `thinking/open-edges.md:91` | `ROADMAP.md:67` |
| A5 | **T8 / V3 republish UX control** — structural V3 done; the human-layer "can still quote" control unspecified. | [decision] | `thinking/open-edges.md:93` | `ROADMAP.md:61`, `ROADMAP.md:107` |
| A6 | **Vote-accumulation under churn/partition** — vote expiry, retraction, stale-vote handling. | [decision] | `thinking/open-edges.md:70` | — |
| A7 | **Pin remaining sub-product name map** (top-level "Croft" pinned; **P2P protocol pinned 2026-06-22 → "Drystone P2P Protocol"**, `NAMING.md` "Protocol-layer naming"; app/brand layer still open → see C6). | [decision] | `ROADMAP.md:121` | `NAMING.md`; app brand draft `thinking/app/brand-and-voice-notes.md`, `NAMING.md:108`; `COHESION.md` §24 |
| A8 | **Import Croft-app Phase 0 (CroftC PR #10) → `experiments/` + the IP/ownership decision** — ✅ **IMPORT DONE 2026-06-22** (`experiments/croft-app-phase0/`, byte-identical), decision **exercised** by the user directing the import; paper trail in `experiments/croft-app-phase0/PR-CONVERSATION.md`. **Residual (user's):** the CroftC-side PR #10 is still **OPEN** — decide whether to close/annotate it on the CroftC side to complete the clean paper trail. | [decision] | `ROADMAP.md:157` | `COHESION.md` §23, `experiments/croft-app-phase0/PR-CONVERSATION.md` |
| A9 | **Anchor-URI stability contract** — the cross-platform provenance anchor in `alsoKnownAs` must be a *stable logical URI* (domain/path committed to staying resolvable/redirecting) with *mutable content*; pointing at a content-addressed/immutable thing re-freezes what portability is meant to avoid. Deliberate choice, not default. | [decision] | `thinking/cross-platform-identity-provenance.md:213` | `COHESION.md` §21 |
| A10 | **PDS-held vs self-controlled did:plc rotation key** — changes who can issue future operations and whether the did:webvh root genuinely functions as a recovery anchor for the Bluesky spoke. Staging advice exists; the keep-vs-drop-PDS-key call is the user's. Relates A2. | [decision] | `thinking/cross-platform-identity-provenance.md:218` | A2, `COHESION.md` §21 |
| A11 | **Drystone capability mechanism — Track A (Meadowcap-shaped) vs Track B (Keyhive-shaped)** (2026-06-24) — §X.5 deliberately UNCOMMITTED. Track A = delegated tokens + epoch keys, expiry-bounded revocation, clean Willow fit, weaker immediacy; Track B = convergent membership graph, generation-bounded revocation, stronger immediacy, depends on in-flight research. Decide in the richer-access-control phase on the revocation-immediacy criterion. Both satisfy R1–R3+R6 (state-reset avoidance) identically. | [decision] | `thinking/drystone-spec/section-x-governance-conflicts.md` §X.5.4 | A2, `ECOSYSTEM.md` §2/§4 |
| A12 | **Drystone key-custody default — blind-relay (Option A) vs revocable trusted delegate (Option B)** (2026-06-24) — Option A keeps the anchor blind (serves ciphertext + sealed key material), honest "history can be lost" outcome; Option B is a designated readable delegate (re-provisions new devices), better UX, a bounded/revocable trust concession. Open question flagged in the dialogue's self-critique: *what mechanism, if any, structurally resists Option B becoming the default and quietly rebuilding a readable homeserver* — the §2.8 exitability requirement is the partial answer. Plain-English outcome strings must be settled either way. | [decision] | `thinking/drystone-spec/section-2-peers-rights-capabilities.md` §2.8 | A2, A11 |
| A13 | **Rename the "geer" gating-peer** (2026-06-24) — the user dislikes the name; gating is the one capability that bumps the read right (§2.7 open). Role names must decompose to `floor + [capabilities]`; "gating peer" / "gating role" are the literal fallbacks. | [decision] | `thinking/drystone-spec/section-2-peers-rights-capabilities.md` §2.7 | `thinking/geer-gating-peer.md`, `COHESION.md` §37 |
| A14 | **Drystone spec text license — CC0 vs CC-BY 4.0** (2026-06-24) — ✅ **DECIDED 2026-06-25 → CC0 1.0** (user-approved; maximal "no one can claim or restrict the idea" beats attribution-only for a freely-buildable standard). Folded into beta `07` C2 + narrative/charter/banner. **Reference-code license SUPERSEDED 2026-07-09 (C13) → AGPL-3.0-or-later + DCO** (was Apache-2.0 here; the user chose the copyleft lock). Intersects but does **not** resolve **A1** (MPL/AGPL substrate). **NOT-LEGAL-ADVICE** — attorney review of the patent-non-assertion paragraph still advised. (The priority-vehicle half — Zenodo+OTS vs IETF-I-D — was decided Zenodo, also folded into `07`.) | [decision] ✅ | `thinking/drystone-publication-and-defensive-disclosure.md` §7 | A1, `beta/07` Pillar C (C2), `COHESION.md` §38, `BETA-ROLLUP.md` (Beta refinement) |
| A15 | **FS-V1..V5 owner walk — xmtp-ambassador (RUN-FS-01) settled inputs** (2026-07-20) — the second-instance validation of the delivery-plane-role charter (RUN-AP-01's §0 governing principle) needs owner-walked verdicts BEFORE execution, mirroring how AP-V1..V5 landed as SETTLED INPUTS in the RUN-AP-01 brief. Pre-brief lands XMTP-specific angles + proposed answers + tradeoffs for each verdict: FS-V1 (register — recommend `ConsentGranted` as the Follow-analogue), FS-V2 (record composition — MLS envelopes with `installation_key` pinning + `inbox_id` associated + MLS epoch captured), FS-V3 (undo/delete — recommend NO Delete rider; MLS forward-secrecy solves it for the future, no protocol handle for the past), FS-V4 (governance — identical to AP-V4, the abstraction we're validating), FS-V5 (identity — honor XMTP's `AssociationState` as the XMTP-side origin proof, subject-initiated by construction). Owner walk turns proposals into DECIDED inputs; execution follows. | [decision] | `alpha/experiments/RUN-FS-01-OPEN-QUESTIONS.md` | RUN-AP-01, `alpha/experiments/ap-ambassador/AP-AMBASSADOR.md`, `EXPERIMENT-BACKLOG.md` §6i |
| A16 | **AP-OC-8 — blinded-tier follower-count disclosure dial** (2026-07-20) — does a blinded roster publish cardinality? Ambassador's blinded posture (RUN-AP-01 §P2) keeps bodies out of the record; whether the count itself leaks (or is publishable as a first-class fact) is an OC. The sharper form is "at what N does cardinality stop carrying identifying signal?" — an anonymity-set measurement in the `FINDINGS-ANONYMITY-SETS.md` style (RUN-ATTEST-02 lineage). Owner call on whether to expose cardinality at all, and if so at what tier. | [decision] | `alpha/experiments/ap-ambassador/AP-AMBASSADOR.md` §4 (AP-OC-8) | RUN-AP-01 §P2 blinded posture, `attest-family/FINDINGS-ANONYMITY-SETS.md` |
| A17 | **AP-OC-9 — ambassador stance on inbound non-follow AP activities** (2026-07-20) — replies, likes, boosts on the ActivityPub side are a different fact family than Follow/Undo/Delete. Design call BEFORE any build: does the ambassador observe these at all, or forward without attesting? A Like has receiver stake AND ordering matters, brushing against the "no ordering authority" principle. Recommendation not yet drafted; wants a scoping pass first. | [decision] | `alpha/experiments/ap-ambassador/AP-AMBASSADOR.md` §4 (AP-OC-9) | RUN-AP-01 §0 governing principle, RUN-AP-03 candidate scope |
| A18 | **Stellin name-clearance gate (2026-07-20)** — the clearance report (`research/stellin-name-clearance-2026-07.md`) verdict is **CONTESTED, not blocked**, but every primary-source check (live RDAP for stellin.app + siblings; USPTO/EUIPO for "STELLIN"/"STELLINA" in classes 9/35/42/45; Bluesky handle resolution for stellin.bsky.social / stellin.app) **could not be verified across two passes** and must be re-run live before any filing or domain purchase. Owner decisions: (a) accept the CONTESTED risk (Vaonis "Stellina/Stellinapp" SEO adjacency; brand as "Stellin by Croft", never "Stellin app") and proceed, or reconsider if a senior live "STELLIN" mark in class 9/42 or a confusingly-similar "STELLINA" registration surfaces; (b) the NSID strategy — standalone `stellin.app` → `app.stellin.*` vs nesting `stellin.croft.ing` → `ing.croft.stellin.*` — before publishing any lexicon. Like Noria, a user gate; do not propagate into durable structure until cleared. | [decision] | `research/stellin-name-clearance-2026-07.md` (Recommendations) | `NAMING.md` (Stellin app-layer entry), COHESION §46, RUN-14 appview-infra |
| A19 | **Spec scope beyond the core (2026-07-20)** — whether, and when, to reach past Part 1/2 in a reconciliation. Parked while the core-only currency/CCC pass (the generated-prompts spec-currency brief) runs. Two options to weigh later: **(2) graduate mature alpha lanes** (GROUPS/CONTACT/PUBLICATIONS in `appview-infra/`, `attest-family/PRIMITIVES-ATTEST.md`) up into the beta design tree `beta/impl/drystone-design/` as recognized companions — needs a per-lane "is it ripe?" gate since most evidence is loopback/Modeled grade; **(3) fold lane results directly into Part 1/2** — advised against (bloats the certifiable core; breaks one-design-one-document / the V10 `fold_auth` lesson). Decision: do neither, one, or a staged subset of (2). Not urgent; the core stays lean until decided. | [decision] | this session's planning thread (2026-07-20) | `beta/impl/drystone-design/` (graduation target), `alpha/seeds/generated-prompts/` (the core-only brief), V10 graduation-trigger in `attest-family/PRIMITIVES-ATTEST.md` |
| A20 | **Amble (forum pad) name-clearance gate (2026-07-31)** — the forum-layer working name moved **Graze → Amble by Croft**; clearance report `research/amble-name-clearance-2026-07.md` verdict **CONTESTED, not blocked**. Broader gate than A18 — it carries an **outreach dependency**. Owner steps/decisions: (a) counsel knockout search classes 9/38/41/42/45 + TSDR on Serials **99174346** (Class 9 language/reading software — the closest live risk) + **99446780**; (b) live **`amble.ing` premium-tier** registrar quote (+ optional defensive `amble.*` set); (c) **send the AMBLE NZ coexistence letter before launch and pursue the answer** — the load-bearing mitigation, since silence ripens reverse-confusion against a direct-category common-law senior user (`amble.social`); intent-to-use filings fast on silence; (d) **`graze.ing` disposition** (retire / redirect / hold) now the name is superseded (relates E73); (e) reconsider → **Meander** (register-cleaner; MEANDER Reg. 3738941 DEAD) if AMBLE NZ gains US traction/files, or 99174346 matures broad, or amble.ing prices premium. Like A18/Noria: a user gate — do not propagate into durable structure or rename the built `graze` mock until cleared. | [decision] | `research/amble-name-clearance-2026-07.md` (Recommendations) | `NAMING.md` (Forum-layer 2026-07-31), COHESION §64, A18, E73, **E80**/E62 |
| A21 | **Cooperative + storage-service name — undefined (2026-07-31)** — the coop-metering dialogue used "Drystone" for the PDS-shaped object-store co-op, but that was a **miscommunication / recall-drift** (user, 2026-07-31): **Drystone stays the P2P protocol** (`NAMING.md` → Protocol-layer), unchanged. Residual open item: **the co-op and its storage service are yet to be named** — a lane naming task (E82), not a gate/blocker. (No collision; the "Drystone (storage)" framing is retired.) **[RESOLVED 2026-08-03 — storage service named `CISS` (Croft Item Storage Server), a "PDS+ storage server"; crate/binary `ciss`; its own repo `CroftCommunity/CISS`. The co-op entity itself remains unnamed — that residual is E25/D5, not this row.]** | [naming] | `seeds/transcripts/raw/amble-naming-coop-metering-dialogue-2026-07-31.md` | `NAMING.md` (Protocol-layer Drystone), `thinking/cooperative-social-union-model.md`, COHESION §65, **E82**, E25/D5 |

## B. Validation / spikes

| # | Item | Type | Origin (file:line) | Also tracked |
|---|---|---|---|---|
| B1 | **Run deferred live-bsky validation** — built, waiting on egress allowlist + app-password env; flips H2/H3/H5 to live-network. | [resource] | `ROADMAP.md:126` | `thinking/open-edges.md:122` (T10) |
| B2 | **Verify openmls leaf-credential dependency** (multi-device 8.1) — so lineage-fold/thresholds rest on real library behavior. | [doable] | `ROADMAP.md:112` | `thinking/open-edges.md:137`, `COHESION.md:137` |
| B3 | **Run Achilles-heel adversarial research** (seed prompt, unrun) — "is the superpeer secretly the ordering authority?" | [doable] | `ROADMAP.md:115` | `thinking/open-considerations.md:50` |
| B4 | **Add hashing-tree / Merkle trust-proof** (offline transitive trust) to Proofs. | [doable] | `ROADMAP.md:118` | `ROADMAP.md:68` |
| B5 | **Relay capacity ceilings** — real absolute throughput (generators understated it). | [doable] | `thinking/open-edges.md:58` | `croft-relay-lab` memory |
| B6 | **E6 steady-state goodput under shaping + bandwidth cap.** | [doable] | `thinking/open-edges.md:61` | — |
| B7 | **E0-NAT hole-punch ingress** — public 3343/3478 currently closed. | [resource] | `thinking/open-edges.md:124` | — |
| B8 | **E4 — LVS frontend** (`ipvsadm`); E1 suggests one fat process suffices. | [resource] | `thinking/open-edges.md:125` | — |
| B9 | **E8 / E9 — `meer` binary** (superpeer bridge + confidentiality tiers); **T13 — iOS build host**. | [resource] | `thinking/open-edges.md:126` | `thinking/open-edges.md:123` |
| B10 | **Resume the reference implementation (`experiments/alpha/croft-chat/` + `local_storage_projection`)** — the multi-platform Rust core (`social-graph-core` + `group-chat-core`, hexagonal Transport port) + the `croft-chat` CLI/TUI shell, closed at P20 (2026-06-27) and dormant during the discovery/beta pass. **The host for all forward-road proofs (WS0).** Immediate: land the two deferred items — live **cross-host** runs (pending secroute boxes + `RUN.md`) + the **cargo-mutants** auth/governance re-sweep — then build WS1's harness + the A11/recovery experiments into it. redb/T25 is already here (`local_storage_projection`, mutation-vetted). | [doable] / [resource] | `../experiments/alpha/croft-chat/plans/2026-06-26-1-plan-integrated-drystone-cli.md` (CLOSED) | `plans/2026-07-09-engineering-validation-plan.md` (WS0), `plans/2026-07-09-proof-experiments-a11-and-recovery.md` |
| B10 | **Background BLE-meshing feasibility spike (NEW)** — CoreBluetooth doesn't relaunch on new-advertiser discovery; Berty says bg P2P dies in seconds. Prove before any design leans on it. | [explore] | `thinking/ios-opportunistic-p2p.md:82` | `COHESION.md:17`, `...-FACTCHECK.md` |
| B11 | **Plan an independent crypto/security audit before any production claim (NEW, surfaced 2026-06-26 by the alpha left-behind audit)** — none of the crypto integration has been audited; every credible system in the field has been. A standing pre-production gate, not a design decision. | [pre-prod] | `research/messaging-solutions-landscape.md` §top-unresolved #5 | alpha left-behind audit |
| B12 | **Integrated Drystone CLI — first wired-together demo** — ✅ **BUILT 2026-06-27** (plan CLOSED, all 20 phases). `ratatui` two-pane chat over the three-layer split (`social-graph-core` + `group-chat-core` + `croft-chat`); order-insensitive convergence (I5) proven locally (shared-dir) and over **real iroh-gossip** 2-node + 4-node (in-process + the `serve` binary across processes); §7.6 hard-stop banner shipped. **Residual (deferred):** (a) the live *cross-host* runs over the secroute boxes (binary + `RUN.md` ready, pending box reachability); (b) the cargo-mutants re-sweep of auth/governance (the user's Proof-B trust gate); (c) substrate follow-up — carry each governance op's intended antecedent `gov_seq` so a true cross-node membership contradiction surfaces as a fork through the high-level replication path. | [doable] ✅ | `experiments/alpha/croft-chat/plans/2026-06-26-1-plan-integrated-drystone-cli.md` (Review Log close-out) | substrate `local_storage_projection/MUTATION_TESTING.md`; `experiments/alpha/croft-chat/RUN.md`; iroh `RELAY-LAB-CONCLUSIONS.md` |
| B13 | **Attended fed-shim live leg vs real Mastodon or GoToSocial (RUN-FED-SHIM-01)** (2026-07-20) — the gated live leg named in `FED-SHIM.md §4`. Two shapes: (a) fed-shim ↔ Mastodon docker-compose on a real host, (b) fed-shim ↔ GoToSocial single-binary. Predictions: outbound shim Follow → 202; inbound real-instance Follow → shim's `accept_inbox_post` returns `Ok(VerifiedActivity)`. Byte-level: specimens under `fed-shim/tests/specimens/` match observed real-instance output; any diff records as a specimen update + shim update + naming the change in the observing run's summary. Needs attended host + disposable AP instance; cannot run in this ephemeral in-env session (docker-compose disk footprint + real HTTPS + valid cert). | [resource] | `alpha/experiments/fed-shim/FED-SHIM.md` §4 | RUN-AP-01 §6 gated live leg, RUN-FED-SHIM-01 summary §definition-of-green |
| B14 | **libxmtp compile-fits-env check (before RUN-FS-01 P1)** (2026-07-20) — before authoring xmtp-ambassador's P1 verify tests, confirm the `libxmtp` Rust dev-dep compiles cleanly against this workspace and fits the ephemeral-container disk allowance. If it doesn't, the fallback is a byte-level XMTP wire-shape model (analogous to fed-shim's Mastodon-wire model) rather than actual MLS verify — but that changes the run's grade and adds a new FS-OC that owner sign-off is needed for. | [doable] | `alpha/experiments/RUN-FS-01-OPEN-QUESTIONS.md` §Blocker | A15 |
| B15 | **AP × HIST integration — ride ambassador evidence bodies on the HIST rung** (2026-07-20) — RUN-HIST-01 landed a PDS-backed history store (`hist-atproto-spike/`). RUN-AP-01's ambassador holds evidence bodies in-memory (declared stand-in). This integration test wires the ambassador's store surface into the HIST rung: evidence bodies as PDS records, commitments in the fold-serving index; tests whether the HIST lane's shape holds for a lane with different fact provenance (gateway-attested vs native two-sided). Cross-lane test, non-trivial scoping. | [doable] | RUN-AP-01 §5 declared stand-in "ambassador store", RUN-HIST-01 | RUN-HIST-01, RUN-AP-01, `EXPERIMENT-BACKLOG.md` §6h/§6i |
| B16 | ✅ **DONE 2026-07-20 — RUN-HIST-02 rev B executed + merged** (`spike/hist_live/HIST-LIVE-RESULTS.md`; branch `claude/hist-atproto-live-gentle`, PR #30 → main). The LIVE PDS history-backend proof (gentle profile: one throwaway bsky.social account + app password, 100-record/3-blob budget, 1 rps, full teardown + CAR archive). **E1/OC-2 settled GREEN: a repo record's CID serves directly as the Drystone reconciliation byte-head with ZERO re-hash**, provided the encoder honors atproto's canonical dag-cbor rules (map keys sorted length-then-bytewise-lex; `$bytes`→Bytes / `$link`→CBOR-tag-42); a non-atproto-canonical downstream gets a divergent CID and MUST re-hash (measured, `canonical.rs` + `evidence/live/e1_cid_identity.json`). Range finding: `rkeyStart/rkeyEnd` are deprecated/non-filtering; `sync.getBlocks(cids)` is the "name the bounding digests" primitive, not `listRecords`. Personal-deep-history tier validated by the same tail-vs-archive machinery. Briefs frozen at `seeds/pdshistory-unpacked/` (RUN-HIST-02 + rev B). Remaining OCs (OC-1 repo ownership, OC-3 scribe custody / PLC rotation, OC-5 sealed-posture backend) still owner-walked. | [doable] ✅ | `spike/hist_live/HIST-LIVE-RESULTS.md` | RUN-HIST-01 (`hist-atproto-spike/`), `HIST-ATPROTO-MATCHUP.md`, COHESION §47 |

## C. Backports / reconciliations (corpus hygiene)

| # | Item | Type | Origin (file:line) | Also tracked |
|---|---|---|---|---|
| C1 | **Backport social-layer §8–10** into `thinking/social-layer.md` (proof implemented regimes the doc's header promises). | [backport] | `ROADMAP.md:105` | `COHESION.md:52` |
| C2 | **Reconcile public-path duplication** — PR #3 vs PR #4/#6 overlap; pick canonical AppView home. | [backport] | `ROADMAP.md:133` | `COHESION.md:105` |
| C3 | **Record the sequencer honestly** — "minimal, blind, not a rights authority," not "no ordering authority." | [backport] | `ROADMAP.md:138` | `COHESION.md` §4 |
| C4 | **Reconcile peer-equality wording** — "equal in rights, not capabilities" (current) vs dossier's "ability/capacity." | [backport] | `COHESION.md:256` | `crystallized/principles.md` |
| C5 | **Distill the app-dialogue research into `research/` + `ECOSYSTEM.md`** — iroh-in-browser (relay-only browser peers), webxdc/Delta-Chat games + the WebRTC-transport-swap recipe, super-apps/W3C-MiniApp, atproto appview routing (service-proxy/service-auth + OpenMeet recipe), Rust client libs (ATrium/Jacquard/megalodon/lemmy-client-rs), Crux/FCIS. The `ECOSYSTEM.md` §5c rows are staged **dialogue-sourced, pending independent verification**. | [backport] | `ROADMAP.md:164` | `ECOSYSTEM.md` §5c, `thinking/app/README.md` "Follow-ons" |
| C6 | **Reconcile app brand naming into `NAMING.md`** once `brand-and-voice-notes.md` settles (Croft product vs umbrella; pond/pad; "Grow your own"). The app-level answer to A7. | [backport] | `NAMING.md:108` | `thinking/app/brand-and-voice-notes.md`, `COHESION.md:322` (DRIFT), A7 |
| C8 | **Beta coverage recovery** — ✅ **Phase 0 DONE 2026-07-08** (10-cluster transcript fan-out → `plans/2026-07-08-beta-coverage-gap-ledger.md`; `LAYER-ROLLUP.md` gate widened, line 43–44 corrected). Finding: mature layers carried cleanly; outer/late layers + **all of `ECOSYSTEM.md`** have load-bearing gaps (rollup dropped rationale/prior-art/illustration *type*). **Residual = Phase 1:** surface the by-layer work-list into the correct beta layers (user's depth/sequencing call). The user's stronger concern. | [explore] / [backport] | `plans/2026-07-08-beta-comprehensive-pass.md` | `../LAYER-ROLLUP.md` (widened), `plans/2026-07-08-beta-coverage-gap-ledger.md` |
| C9 | ✅ **DONE 2026-07-08.** **Per-layer source-of-record** — one `reference-index.md` per layer (model: `beta/activism/reference-index.md`): every thinker/book/paper/RFC/site, grouped by type, with epistemic tag + PRIMARY/SECONDARY marker + resolvable locator, cross-checked against the claim it grounds. **`cairn/` is a projects register**, not a bibliography. Phase 2 of the pass. | [backport] | `plans/2026-07-08-beta-comprehensive-pass.md` | `beta/activism/reference-index.md`, `beta/LAYERS.md` |
| C12 | ✅ **DONE 2026-07-08** (16 of 20 recovery docs gained fold-ins, incl. a thesis-correctness fix in fenced/app-store-survivability; 494+/148- ). **Consolidation-completeness sweep** — the Phase-1 recovery docs were written from the ledger-identified *home* of each conclusion (one alpha pointer each); per the `beta/LAYERS.md` "consolidate duplicate thinking into one whole" rule, sweep each recovered doc against the *full* prior tier for stray duplicate instances of the same thinking to fold in, so no doc reads thinner than the corpus supports. Do before rc hardening. | [backport] | `beta/LAYERS.md` (consolidation rule) | `plans/2026-07-08-beta-coverage-gap-ledger.md`, the Phase-1 recovery docs |
| C11 | ✅ **DONE 2026-07-09** — `beta/DECISIONS.md` written: 12 settled decisions + 11 open gates, each linking down to its beta reasoning home (anti-rollup rule); reasoning-gaps flagged (A6, S3/S4, the A8 app-body IP call, A5 republish-UX). Indexed in `beta/README.md`. **Current-state decisions register (`beta/DECISIONS.md`)** — a scannable index of every settled decision (from `CLOSED-THREADS.md` + layer docs) and open gate (README "Standing decisions" + §A), each row at pitch resolution **linking down to its full reasoning at library resolution**. The user's "current-state doc with decisions," governed by the `beta/LAYERS.md` "reasoning travels with the decision" rule — a decision may not enter without its library-resolution reasoning existing + linked. Phase 4 of the pass; depends on C8 recovery. | [backport] | `plans/2026-07-08-beta-comprehensive-pass.md` (Phase 4) | `beta/LAYERS.md` (anti-rollup rule), `beta/CLOSED-THREADS.md`, `beta/README.md` (Standing decisions) |
| C13 | ✅ **DECIDED 2026-07-09 → AGPL-3.0-or-later + DCO for the neutral reference implementation** (user's call: copyleft lock over permissive spread; AGPLv3 §11 preserves the patent grant that had motivated Apache-2.0; supersedes A14's Apache-2.0 reference-code choice). Aligned across both governance docs, the governance reference-index, `drystone-spec/README.md`, and `beta/OPEN-THREADS.md`. **Reconcile the reference-implementation license** — surfaced by the Phase-2 governance reference-index: `beta/governance/foundation-cooperative-and-sustainability.md` licenses the ref impl **AGPL-3.0-or-later** while `beta/governance/open-publication-and-ip-stewardship.md` licenses "the reference code" **Apache-2.0**, with no reconciliation. Pick one (the user's licensing call; relates A1 MPL substrate gate) and align both docs + the governance reference-index. | [decision] / [backport] | `beta/governance/reference-index.md` (coverage/gaps note) | `beta/governance/*`, A1 |
| C14 | ✅ **DONE 2026-07-09** — Part 1's two stale `[confirm]` flags (§2.2 CVE-2025-49090, §2.3 MSC4289) + the §5 status-summary line aligned up to Part 2 Appendix C's confirmed state, each pointing at the primary-source check. **Reconcile the spec Part 1/Part 2 Project-Hydra status** — surfaced by the Phase-2 drystone-spec reference-index: Part 2 Appendix C treats the Matrix Project Hydra / MSC4289 facts as confirmed, while Part 1 still carries inline `[confirm]` on the MSC4289 steelman and the CVE-2025-49090 root-cause vs MSC4297. Resolve the flags one way across both parts. | [backport] | `beta/drystone-spec/reference-index.md` (citation-accuracy note) | `beta/drystone-spec/part-1-reasoning-underpinnings.md`, `part-2-certifiable-design.md` |
| C10 | ✅ **DONE 2026-07-09** (Phase 3) — 7-layer conservative sweep of the pre-recovery docs. Fixed genuine violations (activism `structural-argument-narrative`: ~12 folded primary quotes → standing block quotes + an ellipsis-collapse repair; `crofting-dry-stone`: 4 verse passages → block quotes; `operational-rates`: arXiv finding → block quote; impl `01-delivery-architecture`: RFC 9750 clause → block quote; terms-of-art → italics in modular-politics/09-provenance/12-replant/mls-hardcases; `sixty-second-pitch`: added Resolution label). Most docs compliant. **Spec Parts 1&2 left as RFC-style** per the user + the new `LAYERS.md` carve-out. **Quote & resolution remediation sweep** — apply the `beta/LAYERS.md` "Quote discipline and resolution labels" tie-breaker across all source-grounded docs (elevate quotations to standing block quotes with per-quote `[UNVERIFIED]` flags; demote term-fragments to italics; add `Resolution:` labels + rung links; name every "compressed version" companion by a resolvable pointer). Tie-breaker + exemplar (`philosophy/commensurability-and-the-two-ledgers.md`) landed 2026-07-08; sweep pending. Phase 3 of the pass. | [backport] | `plans/2026-07-08-beta-comprehensive-pass.md` | `beta/LAYERS.md`, `beta/README.md` §4, `beta/impl/doc-writing-method.md` §16/§103 |
| C15 | **Name "evidentiary, not operational" as a canonical principle? (alpha curation)** — relocated from beta thread T27 (closed in beta 2026-07-09). The conclusion is settled and homed in `beta/drystone-spec` §5 + `beta/DECISIONS.md`; the only open action is whether to elevate it to a named Tier-1 principle in `crystallized/principles.md`. Curation call, not design. | [decision] / [backport] | `beta/CLOSED-THREADS.md` (T27), was `beta/OPEN-THREADS.md` T27 | `crystallized/principles.md` |
| C16 | **Run the "left-behind" audit** — a finer-grained, whole-corpus double-check for quotes / references / concepts / conclusions / corrections / prior-art / analogies left behind in alpha (or orphaned in beta), extending the conclusion-focused Phase-0 pass. Ready-to-run handoff prompt written; exhaustive fan-out, produces a second-pass ledger for review (no remediation). The user's requested double-check. | [backport] / [explore] | `seeds/generated-prompts/left-behind-audit-prompt.md` | `plans/2026-07-08-beta-coverage-gap-ledger.md`, the per-layer `reference-index.md` files, C12 |
| C17 | **Cairn orientation-tagging pass** — the cairn now defines three stone orientations (Drystone-oriented / Croft-oriented / related-ecosystem, `beta/cairn/README.md`), but only the newest stone (`lexicon-community-governance.md` = related-ecosystem) is tagged. Tag the remaining cairn stones in the Contents table, with the user's review (per-stone judgment). Introduced 2026-07 (COHESION §44). | [backport] | `beta/cairn/README.md` (Orientation note) | COHESION §44 |
| C18 | **Lexicon Community governance-repo verification pass** — `beta/cairn/lexicon-community-governance.md` is grounded-from-site, not yet verified against the primary. Confirm the actual `GOVERNANCE.md` bylaws text, TSC membership + seat-turnover rules, the full state of the Attestation & Signatures thread, and whether a Lexicon Lenses reference implementation has shipped; then upgrade the stone + the `reference-index.md` marker from grounded-from-site to verified. | [backport] / [doable] | `beta/cairn/lexicon-community-governance.md` (status note) | `experiments/attest-family/ENGAGE-LEX.md`, COHESION §44 |

## D. Strategic considerations (unresolved, surfaced not solved)

| # | Item | Type | Origin (file:line) | Also tracked |
|---|---|---|---|---|
| D1 | **What is the actual product?** (the M0 wedge framing). | [decision] | `thinking/open-considerations.md:19` | `ROADMAP.md` M0 |
| D2 | **Possible over-application of Automerge.** | [explore] | `thinking/open-considerations.md:40` | — |
| D3 | **Moderation & abuse under a blind broker** is unaddressed. | [decision] | `thinking/open-considerations.md:60` | `COHESION.md:157` |
| D4 | **Encrypt-then-content-address kills cross-user dedup** — a real tradeoff to record. | [explore] | `thinking/open-considerations.md:80` | `COHESION.md` §13 |
| D5 | **Infra-sustainability ↔ the cooperative as a *mechanism*** (existential) — relays (browser peers permanently relayed), bridge node, scoped appview, push origination = ongoing metered cost; "cooperative" is so far a value, not yet a funding/governance mechanism. The most important unthought thing. **PARTIALLY-WALKED 2026-06-22** → a concrete **MO Chapter 351** mechanism proposed (`cooperative-social-union-model.md`; E25; COHESION §33); legal-review gate remains. | [decision] | `thinking/open-considerations.md:89` | `ROADMAP.md:172` (§15) + §8 charter, `governance-and-survivability.md`, `thinking/cooperative-social-union-model.md`, E25 |
| D6 | **Moderation/safety vs the kid-friendly goal** — E2EE-P2P-relay carries traffic the operator can't read, in tension with courting gen-alpha/kid use; the public bridge is an abuse vector. The app-layer sharpening of D3. | [decision] | `thinking/open-considerations.md:103` | D3, `thinking/geer-gating-peer.md` |
| D7 | **Cold-start for the owned pond + "composability: user-want or builder-elegance?"** — aggregator ponds inherit populations; Croft Group has the empty-room problem (the games hook is the only answer, and games is a *candidate*); the default ignore-everything experience must be compelling alone. Relates D1. | [explore] | `thinking/open-considerations.md:113` | D1 |
| D8 | **The centerless-meets-center frontier** — where a centerless federation touches politics/finance/technology (legal entity to sue/bill, name registrar, the relay that must scale, the money) without quietly growing a center at the seam. The largest-clothes version of the irreversible-singleton problem; arranged to be deferrable/excisable, but **deferred, not solved**. Wikipedia met it at the Foundation, AP at the instance, atproto at the relay. | [decision] | `thinking/local-first-as-design-imperative.md` (Open frontiers) | D5 (infra-sustainability ↔ cooperative), `COHESION.md` §22 |
| D9 | **Governance at scale** — representative quorum vs. the cheap-fork sybil defense getting expensive at 200k; likely direction **subsidiarity + liquid delegation** (instantly-revocable weight), with concentration the default failure (Pirate Party lesson) resisted by decay/caps/bounded-chains/expiry/visibility. Member ≠ governance-constituent must be modeled. Surfaced not solved. | [decision] | `thinking/local-first-as-design-imperative.md` (Open frontiers) | `COHESION.md` §22, A2 |
| D10 | **Forward-only revocation under irreversible commitments** — revoking consent can't rewind a spent check; decisions must be tagged reversible-or-committing at decision time, and the record must permanently/honestly attribute which consent supported which irreversible consequence. The governance-plane face of D4/A2. | [decision] | `thinking/local-first-as-design-imperative.md` (Open frontiers) | D8, `COHESION.md` §22 |
| D11 | **Use-case beachhead: lead with meetup/community, not dating** — the dating/friendship/meetup fit research recommends targeting the Meetup-refugee organizer first (roster ownership, organizer succession, re-derivable attendance counts, operator-swappable delivery map ~1:1 onto documented Meetup-under-Bending-Spoons pain). Dating is a narrow trust-sidecar only after a real private layer; friendship rides the group wave. Product-direction call. Report's other recs (ship the RSVP+roster primitive reusing the Smoke Signal strong-ref pattern + align with lexicon.community; dating-as-trust-layer-only; "Drystone is infrastructure not an app"; instrument the auditable-count wedge) live in the report. | [decision] | `research/dating-friendship-meetup-fit-2026-07.md` (Recommendations) | COHESION §44, D1, `experiments/appview-infra/PUBLICATIONS.md` |

## E. Adjacent / new explorations (this batch, 2026-06-22)

| # | Item | Type | Origin (file:line) | Also tracked |
|---|---|---|---|---|
| E1 | **AT-Proto "atmospheric web" product surface** (Neo-GeoCities / open-LinkedIn / private AppView). Demand-side argument for the lineage-groups MLS crypto. | [explore] | `thinking/atproto-atmospheric-web.md:1` | `COHESION.md:17`, `ECOSYSTEM.md` §5b |
| E2 | **Private groups for any atmospheric-web product = Croft's lineage-groups MLS proof** — no native AT-Proto E2EE (REFUTED claim); Germ/XMTP are third-party. | [explore] | `thinking/atproto-atmospheric-web.md:47` | `ECOSYSTEM.md` §6 (Germ) |
| E3 | **Gateway-renders-HTML XSS surface** — declaration-not-execution design for any web bridge. | [explore] | `thinking/atproto-atmospheric-web.md:76` | — |
| E4 | **Music-over-Iroh streaming** (iroh-blobs + QUIC migration) — Aster is a real precedent. | [explore] | `thinking/ios-opportunistic-p2p.md:64` | `thinking/realtime-media-over-iroh.md` |
| E5 | **The Croft app / client layer (new body, 2026-06-22)** — composable ponds/pads garden riding the proven substrate; stack decided (Rust functional core + Tauri/Leptos, web-first, Crux *pattern* slim); Phase 0 built, Phases 1-2 spec'd. | [explore] | `thinking/app/README.md:1` | `ROADMAP.md:146` (§12), `COHESION.md:322` (§18) |
| E6 | **Games pond** — P2P games over iroh, outcomes as a custom lexicon (rendered-where-desired), Bluesky as the rendezvous channel; the hook that teaches composability; outcome-attestation is the real design work (set aside). **Research prompt now RUN → produced the `ponds/` catalog (E8).** | [explore] | `thinking/app/design-philosophy.md:634` | `thinking/app/ponds/`, `COHESION.md` §19 |
| E7 | **OpenMeet read-only-meets candidate pond** — thinnest useful slice (read-only your-own-meets); pond-worthiness is a product assessment, distinct from crediting its auth technique. | [explore] | `thinking/app/design-philosophy.md:641` | `ECOSYSTEM.md` §5c |
| E8 | **Ponds & pads catalog + build-order (2026-06-22)** — games/utilities/ritual catalog by inclusion pathway (build-fresh / wrap-via-webxdc-shim / port-via-WebRTC-swap), ranked by fun + by "have you seen this?" energy; six-phase build sequencing. | [explore] | `thinking/app/ponds/build-order.md:1` | `thinking/app/ponds/` (games/apps/launch-set lists), `COHESION.md` §19 |
| E9 | **Fair-reveal (commit-reveal) primitive** — one shared module giving fairness with no server: powers secret-ballot voting + dice + Two-Truths-and-a-Lie. Build in Phase 1 so later games/voting just "call it." Watch: nonce for low-entropy values, the commit-then-abort reveal deadline. | [doable] | `thinking/app/ponds/fair-reveal-primitive-spec.md:1` | `thinking/app/ponds/build-order.md` (Phase 1), local real-time voting |
| E10 | **On-device-LLM navigation assistant** — natural-language front-end to the deep-link resolver ("any travel games?" → catalog links). **Great-to-have, NEVER a requirement** (hardware coverage; first targets Android+macOS, iOS deferred); strictly optional, easily disabled, complete-navigation-without-it. Phase 5 (can slip indefinitely). | [explore] | `thinking/app/ponds/on-device-llm-feasibility.md:1` | `thinking/app/ponds/build-order.md` (Phase 5) |
| E11 | **Relay dependency / deferred-deep-link reality (the two "serverless"/"frictionless" asterisks)** — browser peers are permanently relayed (self-host relays if it grows, `[resource]`); seamless cold-install deep-linking is not privately achievable (Instant Apps + Firebase Dynamic Links dead; MMPs need fingerprinting) → claim-code one-more-tap, framed as a feature. | [decision] | `thinking/app/ponds/build-order.md` (tier-zero resolver) | `open-considerations.md` §8, `ECOSYSTEM.md` §1 (iroh relays) |
| E12 | **Cross-platform identity provenance (new body, 2026-06-20)** — closed-root-of-trust → out-of-band attestation is the only cross-platform linkage; hub-and-spoke (did:webvh root, did:plc/AP/Hive spokes); key lineage = attestation not derivation; `alsoKnownAs` equivalence ladder; did:webvh portability (`portable` genesis-only); did:plc↔did:webvh convergence as cheap hedge (keep the SCID as anchor). Extends `plc-identity-resilience.md` on a new axis. | [explore] | `thinking/cross-platform-identity-provenance.md:1` | `plc-identity-resilience.md`, `COHESION.md` §21, A9/A10 |
| E13 | **Per-platform trust-model doc (next artifact)** — the dialogue's repeatedly-offered, highest-leverage write-up: per network (Bluesky/AP/Mastodon/GoToSocial/Threads/Hive), the field used, what we claim / don't claim, the backlink mechanism, and exact verifier steps + pseudocode. Not yet written. | [doable] | `thinking/cross-platform-identity-provenance.md:222` | E12 |
| E14 | **Test `alsoKnownAs` extra-entry persistence** `[UNVERIFIED]` — confirm atproto PDS/PLC tooling preserves a non-`at://` provenance entry on write (and which AP servers tolerate a non-actor URI). Pure implementation check, not spec. | [explore] | `thinking/cross-platform-identity-provenance.md:159` | E12, B1 (live-bsky) |
| E15 | **Design-imperative / system-architecture body (new, 2026-06-20)** — the deep "why" (cross-field lineage) + the protocol-substrate architecture (delegates, planes, governance-as-substrate, federation, local-first-as-premise). Grounds the existing principles in a 2,400-year lineage; resolves the Kleppmann reconvergence tension (per-plane policy). | [explore] | `thinking/local-first-as-design-imperative.md:1` | `narrative/lineage-of-a-design-imperative.md`, `crystallized/principles.md`, `COHESION.md` §22 |
| E16 | **Federation / inter-collective peering PoC** — BGP-autonomy + postal-hierarchy + DID-signed routing; identity-vs-locator; recursive-worker resolution over atproto with DNS as a swappable resolver. Show bounded per-node state + log-depth resolution at small N (4 collectives) to demonstrate the scaling math. | [explore] | `thinking/local-first-as-design-imperative.md` (Federation) | D8, RINA/NDN/Yggdrasil prior art |
| E17 | **Three new threat-model seams (write each up before code)** — (1) MLS epoch keying × fork × offline pre-fork grant-holder; (2) the delegate courier-vs-agent unification (does C2/live-authority break the abstraction); (3) content-predicate search-coverage attestation (honest plaintext evaluation, not just structural coverage). | [doable] | `thinking/local-first-as-design-imperative.md` (What's new) | `COHESION.md` §22 |
| E18 | **Crypto-shred + moderation hold/release plane** — erasure = disassociate + purge group-held copies + honest-limit (can't reach independent copies); the chat plane needs crypto-shred from day one; holds = a `{pending,released,rejected}` predicate-gated moderation plane (the "two convergent groups, one a queue"). Willow-style prunable store for stable content. | [doable] | `thinking/local-first-as-design-imperative.md` (substrate / planes) | D6 (moderation), `app/ponds/` |
| B11 | **Finish Croft-app Phase 0 M6 — live Jacquard adapter** — wire the real atproto Rust client to live Bluesky behind the port (DECISION 1); M1–M5 done + real fixtures present, M6 deferred. Lights up the live happy path. | [doable] | `experiments/croft-app-phase0/PR-CONVERSATION.md` (Milestones) | `appview-validation` (live-bsky), B1 |
| C7 | **Reconcile as-built Phase-0 spec → thinking/app** — the PR's `BUILD-SPEC.md`/`design-philosophy.md` are what the code was written to; `thinking/app/` is more-developed (§3a proof, §1a, §4a). On graduation, reconcile, and address CodeRabbit doc nits ("written-down shortcut" undefined; DECISION-5 burden on the CLI fake). | [backport] | `COHESION.md` §23 | `experiments/croft-app-phase0/`, `thinking/app/` |
| E20 | **PDS-hosting + P2P-blended business model as sustainability input (2026-06-22)** — a managed Bluesky-PDS host ($5-10/mo anchor) stacking consumer (custom domains, encrypted "Vault", cross-post relays), creator (firehose analytics, feed hosting, CAR "escape hatch"), operator (Iroh relays, headless PDS), and **enterprise-compliance** tiers. The compliance demand is **CONFIRMED real** (SEC 17a-4/FINRA 4511/2210; **$3.5B+** off-channel fines; Deloitte $200k; Velox $1.8M; Smarsh/Global Relay). Permissioned-data gap noted (PDS public-by-default → public repo + E2EE Iroh-doc private vault); Proton-augment-not-clone framing. **DECISION-adjacent:** source frames this as for-profit SaaS; Croft's stance is cooperative/non-extractive — feeds the existential **sustainability ↔ cooperative *mechanism*** item; surface, don't let the framing become the answer. | [decision] / [explore] | `seeds/transcripts/raw/crypto-wars-to-p2p-pds-economics-dialogue-2026-06-22.md` | `open-considerations.md` §8, `COHESION.md` §25 |
| E21 | **Mobile-P2P "four-property impossibility" + prior-art map (2026-06-22)** — moderation + multi-device + PFS + offline-mesh can't coexist without an unequal peer (= MLS's Delivery Service, which Croft already accepts); + the secondary-dials catalogue (MLS wire-overhead, metadata-vs-scale, eviction-lag, log-pruning, Energy-Depletion, Sybil, traffic-analysis, DHT warm-up). **`Peat`/Defense Unicorns** = CONFIRMED production prior art for Croft's exact stack (Rust+iroh+Automerge+MLS in denied/degraded). Backs the protocol spine + `ios-opportunistic-p2p.md`; the cleanest articulation of *why* the design is shaped as it is. | [explore] | `seeds/transcripts/raw/crypto-wars-to-p2p-pds-economics-dialogue-2026-06-22.md` | `thinking/ios-opportunistic-p2p.md`, `ECOSYSTEM.md` §1/§6, `COHESION.md` §25 |
| E22 | **Distill the atproto private-data architecture + confirm Croft's differentiation (2026-06-22)** — a **real, community-led ATProto Private Data Working Group** exists (atproto.wiki / discourse.atprotocol.community, Boris Mann; GitHub #3363 "Namespaces"→"buckets/realms", #121 "Encryption for private content"; Paul Frazee *informally*). It is converging on **access-controlled, PDS-gated** private data (PDS-as-trusted-agent); **true E2EE / zero-knowledge is explicitly deferred** behind public-broadcast + Auth-Scopes work. This *sharpens* Croft's host-untrusted lineage-groups MLS differentiation (Croft sits on the ZK side the atproto core team is reluctant to take) and maps onto the blind-broker line. **Net-new vs §25:** the private-data WG + the key-revocation "cat-out-of-the-bag" debate. ✅ **CLOSED 2026-06-22** → `research/atproto-private-data-architecture.md` (analysis) + `ECOSYSTEM.md` §5e (PDS implementations/hosts/blob backends) + differentiation framing backported into `crystallized/principles.md` ("the recurring inversion" → host-untrusted/zero-knowledge note). | [backport] ✅ | `seeds/transcripts/raw/croft-atproto-pds-germ-privatedata-dialogue-2026-06-22.md` | `research/atproto-private-data-architecture.md`, `COHESION.md` §26, `crystallized/principles.md` (host-untrusted note), source-of-truth FACTCHECK addendum |
| E24 | **Sovereign PDS/AppView "club" model + Twitter-Circles corroboration (2026-06-22)** — owning the AppView (read/index) + PDS for a small group is a buildable shape of Croft's blind-broker / honest-seams-ponds stance: private blocking (inbound-effective / outbound-impossible → **experience-shaping**), off-repo private feeds + encrypted blobs, private cooperative Labelers (≈ the **geer**), multi-source AppView (AT+AP+Nostr+RSS), CAR/MST offline mesh. ✅ **Distilled** → `research/atproto-sovereign-appview-club.md` + `ECOSYSTEM.md` §5f (Bridgy Fed/A New Social, Groundmist, AppViewLite, Blacksky/rsky-wintermute, Zeppelin, Ouranos/Heron/atcute, goat/PDS MOOver, Jetstream). ✅ **Backport done 2026-06-22:** the **Twitter Circles → Communities → Group DMs** cautionary proof is folded into `thinking/social-layer.md` as **invariant S5** (semi-private-on-a-public-plane leaks → make private data *structurally* private — billion-user corroboration of structural-not-runtime). **Caveats to honor:** offline-mesh unattended-wake inherits the iroh "OS-kills-bg-P2P" caveat; dual-PDS one-identity is **not native** (sidecar/off-repo, not delegate keys); SQLite-schema PDS hacks are fragile (see E23). | [explore] / [backport] | `seeds/transcripts/raw/croft-atproto-sovereign-appview-open-social-dialogue-2026-06-22.md` | `research/atproto-sovereign-appview-club.md`, `thinking/social-layer.md` (V3), `ECOSYSTEM.md` §5f, `COHESION.md` §29 |
| E23 | **Prototype the PDS-as-selective-file-proxy shim (2026-06-22, blind-broker line)** — serve a blob the network believes is PDS-native while the bytes live in your own object store, zero duplication (reverse-proxy `getBlob` interceptor + a blob-row in the PDS DB). Rhymes with Croft's **content-blind mule** + the `encrypted-blob-share` experiment. **Unverified, Gemini-self-described-fragile** (depends on the PDS's internal SQLite schema) — prove/disprove before any design leans on it; an idea to prototype, not a recipe. | [explore] | `seeds/transcripts/raw/croft-atproto-pds-germ-privatedata-dialogue-2026-06-22.md` (FACTCHECK Cluster 3) | `experiments/encrypted-blob-share`, `COHESION.md` §26 |
| E25 | **Cooperative "Social Union" mechanism for D5 (2026-06-22, the existential item)** — a concrete sustainability/governance mechanism: a **MO Chapter 351** Limited Cooperative Association (multi-class membership, nonpatron capital, **progressive decentralization** w/ hardcoded founder-sunset, capped revenue royalty / RBF, indivisible reserve + internal credits = anti-Steemit, **501(c)(3) tandem** for grant-funded labor, **labor-as-first-class**), federating on **AT-Proto day-one** (private "cozy union" = where lineage-groups MLS plugs in). ✅ **Distilled** → `thinking/cooperative-social-union-model.md`. Verified anti-pattern lineage (Ello/Ampled/Steemit/Diaspora/Coomappa). **Open work-list:** (a) **[decision] legal-review gate** — a MO cooperative attorney/CPA must confirm all §/fee specifics (**NOT-LEGAL-ADVICE**; confirmed-wrong in source: §351.1015→§351.1036, CA-41 $100 not $105, name-res $25 not $20); (b) **[decision] entity structure** — single co-op vs co-op + 501(c)(3) tandem; (c) **[explore] MVC** — single PDS + AppView + cozy feed + founding-member ledger; DBA→IP-assignment path. Advances **D5**; touches **D9** (governance at scale). **UPDATE 2026-07-31 — the charging mechanism landed, proven in code.** The PDS-hosting metered-billing design (meter-the-boundary postage/rent, cost-priced audit dial `1−(1−f)^k`, sealed/tombstone tiers, on-book grace ledger, two-plane public/private split, and the extinguishing-royalty + funder-side diligence machine) is the concrete answer to Pillar 3's "how does the co-op charge non-extractively" — built as `experiments/item-storage-protocol` (E0–E14, **green-real**, on `main`). Distilled → `cooperative-social-union-model.md` (new charging-mechanism section), `principles.md` Tier 3 ("meter the boundary"), proof-ledger + test-narrative (T-ISP). Biggest single advance on D5. **Open:** the coop/storage-service is unnamed (**A21**; Drystone stays the protocol); the metering design widens the existing **legal-review gate** (deferred to the coop-layer pile). The work now has a lane — **E82**. | [decision] / [explore] | `seeds/transcripts/raw/cooperative-social-union-governance-dialogue-2026-06-22.md` | `thinking/cooperative-social-union-model.md`, `COHESION.md` §33, **D5**, D9, `crystallized/principles.md` |
| E26 | **Iroh + open-social ecosystem distillation (2026-06-22)** — two fact-checked Gemini surveys: **(a)** iroh/QUIC/local-first stack (noq multipath/QAD, Automerge + CRDT alternatives Loro/Y-CRDT/Diamond-Types, **Peat/peat-mesh**, app roster Prime-Intellect/Tandemn/Bones/cross.stream/Hubris/Teamtype/Zeco/DashChat/**Holochain-on-iroh**) — reinforces the iroh substrate bet; **(b)** open-social protocols & aggregators (Nostr+Blossom+**Marmot/MLS**, Farcaster **Neynar acq.**, Lens **Mask stewardship**, thirdweb, Yup, **Flare-AGPL/Bridgy-Fed-CC0/SkyFeed/Mixpost** — the fork-a-client license map). **Residual [backport]:** register ecosystem rows in `ECOSYSTEM.md` (flag dialogue-sourced); fold the platform-coop orgs (PCC/Stocksy/Drivers-Coop/Resonate/Social.coop/Purpose-Foundation/SPI-SFC-CS&S). FACTCHECK fixes: ALPN `iroh/automerge/2`; Huitema=co-author-not-endorser; Farcaster rent ~$7; Clanker=token-launchpad. | [explore] / [backport] | `seeds/transcripts/raw/iroh-quic-localfirst-ecosystem-dialogue-2026-06-22.md` + `opensocial-nostr-farcaster-aggregators-dialogue-2026-06-22.md` | `COHESION.md` §31/§32, `ECOSYSTEM.md` §5d |
| E29 | **Membership-vs-access decoupling + Discord money/IPO/onboarding intake (2026-06-22)** — the public door a stakeholder can hold open: **two orthogonal axes** Discord conflates — *who holds a stake* (membership/governance = pond/infra layer) vs *who can walk in* (access = pad/room layer). A member can open a **public/anonymous door** into a pad while the pond stays member-governed; the guest carries **no governance weight → Sybil softens**. ✅ **Distilled** → `thinking/membership-vs-access-the-public-door.md`; competitive grounding + the **moderator-labor-as-captured-enterprise-value** framing (the cooperative-thesis counter-illustration) → dated Update in `research/discord-dominance.md`. **Open edges (mostly already tracked):** the anonymous-guest-first-iroh-pad-entry property = **E11** (tier-zero deep-link resolver / "ten-second door"); member ≠ governance-constituent = **D9**; sponsee mechanics + guest→member conversion (new, small). Feeds **D5/E25** (Discord = the extractive default the co-op inverts). | [explore] | `seeds/transcripts/raw/croft-discord-money-ipo-onboarding-dialogue-2026-06-22.md` | `thinking/membership-vs-access-the-public-door.md`, `research/discord-dominance.md`, **E11**, **D9**, D5/E25, `thinking/social-layer.md` (S3), `thinking/geer-gating-peer.md`, `COHESION.md` §36 |
| E28 | **Foundation + IP-stewardship layer + the foundation name (2026-06-23)** — the IP half of D5/D8: a three-layer **code (AGPL-3.0-or-later + DCO, structural openness lock) / brand (trademark → certification/compat badge, held by a neutral foundation) / coop (operates under a free, conditioned mark license, repeatable for many coops = even-footing made mechanical)** architecture, with entity **phasing** (Phase 0 build → coop-when-real → **Aspiration as interim foundation** → own foundation Phase 2) and **asset transfer via assignment-with-goodwill**. ✅ **Distilled** → `thinking/foundation-and-ip-stewardship.md` + `NAMING.md` (foundation-name section + Croft collision-check + domains) + ECOSYSTEM rows (SPI/SFC/Aspiration). **Open work-list:** (a) **[decision] legal-clearance + entity gate** (nonprofit/IP attorney: structure, assignment-with-goodwill, **trademark clearance incl. the foundation name** — the user owns this; **NOT-LEGAL-ADVICE**, all $/§ specifics dialogue-sourced); (b) **[decision] foundation NAME** — leading candidate **Noria** (The Noria Foundation / Noria Commons Foundation) is **a CANDIDATE pending legal clearance, NOT decided** ("Watershed" rejected — $1.8B Watershed Technology collision; "Wellspring" rejected — crowded + billionaire-fund association); do not propagate into structure; (c) **[doable] fiscal-sponsor exploratory** (free Aspiration call; confirm it sponsors a coop-operated project); (d) **[decided, capture-only] AGPL-3.0-or-later via DCO** as the openness lock + honor-system compatibility badge. Advances **D5**, partially walks **D8** (legal entity / name registrar / the money). Exercises **A7** (sub-product name map). | [decision] / [doable] | `seeds/transcripts/raw/croft-foundation-coop-ip-naming-dialogue-2026-06-23.md` | `thinking/foundation-and-ip-stewardship.md`, `NAMING.md`, `thinking/cooperative-social-union-model.md`, ECOSYSTEM §8, **D5**, D8, A7, E25, `COHESION.md` §35 |
| E27 | **Croft etymology / commons research loose ends (2026-06-23)** — three optional follow-ups left open by the etymology+commons dialogue (distilled to `narrative/verticals/croft-the-name-and-the-commons.md`): **(a)** confirm the **exact 1772 Manchester Directory wording** of the verb's first attestation against an open scan, to settle bleaching-vs-farming sense (`[UNVERIFIED]` — date solid, sentence not surfaced behind OED paywall); **(b)** read **Allan Greer, *Property and Dispossession*** (Cambridge, 2018) as the global-enclosure-pattern scholarly anchor (Part 5); **(c)** read **Elinor Ostrom, *Governing the Commons*** (1990) as the tragedy-of-the-commons counter-case — flagged as likely the most useful next read given the cooperative-governance work (relates D5/E25). Low priority; none blocks the name or the narrative. | [explore] | `seeds/transcripts/raw/croft-etymology-enclosure-tradition-dialogue-2026-06-23.md` | `narrative/verticals/croft-the-name-and-the-commons.md`, `NAMING.md` (etymology section), `COHESION.md` §34 |
| E19 | **Adapt croft-chat-cli to the shared-core / per-platform-shell model** — ✅ **architecture DECIDED 2026-06-22** (`thinking/app/client-architecture-adr.md`): one shared functional core + thin per-platform shells, two callout axes (platform `effects.rs` + implementation adapters behind a port). `croft-chat-cli` already has the implementation seam (`Transport` port + in-proc fake) but **not** the core/shell — so adoption is greenfield growth, not a refactor. Gap analyzed; **plan drafted + Pass-3 reviewed, execution started 2026-06-22** (`experiments/croft-group/plans/2026-06-22-1-plan-croft-chat-cli-group-core.md`; happy-path slice — extract to dedicated `croft-group` workspace, grow `group-core`, wire through the existing `Transport` port). Decomposition **RESOLVED (option C)**: per-pond domain cores unified by the shared `shell` composition layer (group-core + Transport port symmetric to feed-core + Bluesky port); cross-pond awareness = read-only shell composition, interactivity = a deferred broker. Also close the `iroh/`-not-in-`experiments/README` gap. | [doable] | `thinking/app/client-architecture-adr.md` | `experiments/croft-group/crates/croft-chat-cli` (+ memory), `COHESION.md` §23, `crystallized/principles.md` Tier 3 |
| E30 | **Drystone governance-layer spec body (§2 peers/rights/capabilities + §X governance conflicts) (2026-06-24)** — distilled from the Matrix-contrast dialogue → `thinking/drystone-spec/`. Settled vocabulary: one peer kind; rights (universal) vs capabilities (additive/delegated/revocable); the **capability / role / PeerSet** layers; **exitability** backstop + **asymmetry-of-expressible-range**; revocation = epoch-rotating expulsion-shaped governance fact; the **append-only-fold, timestamp-free causal resolution order, no-state-reset** governance model + the R1–R6 interface + attributable-acceptance dichotomy + the regress-breaking termination construction. **Open work-list:** (a) **[doable] write the §1 `P-*` principles these `Realizes`** — `P-Local-Truth`, `P-Peer-Equality`, `P-Knowable-Truth`, **`P-Durable-Enablement`** (new: "only ship a guarantee you can keep enabled by default; a feature too costly to implement uniformly becomes one users route around") + the **peer-capability-floor** principle — not yet in `crystallized/principles.md`; (b) **[doable] fill the `ENABLING` wire formats** (canonical fact encoding §X.3.2 → gates the Zenodo DOI; **frontier-closure-before-sort §X.8.5** = highest cross-impl divergence risk; frontier-commitment §X.7.4; root-succession §Y); (c) **[explore] adversarial press-test** — *does the flexibility scale in the default case, not just at the unused margins* (§2.8.3), and the R6-vs-manufactured-concurrency / R4-laundering seam; (d) **[backport] confirm the cited Matrix/Willow/Meadowcap/Keyhive facts** against a SoT before beta. Capability-mechanism + key-custody decisions = A11/A12; geer rename = A13. **Staged for beta as `beta/OPEN-THREADS.md` T1 (gated — promote into a theme only when the gates clear); not folded into the settled themes yet.** | [explore] / [doable] | `thinking/drystone-spec/section-2-peers-rights-capabilities.md`, `...section-x-governance-conflicts.md` | A11, A12, A13, `thinking/meer-superpeer-design.md`, `thinking/revocation-authority.md`, `crystallized/CROFT-PROTOCOL.md`, `ECOSYSTEM.md` §2/§4/§6, `COHESION.md` §37 |
| E31 | **Publish Drystone as a defensive publication (2026-06-24)** — the *how Drystone goes public and stays open* work-list distilled from the publication/defensive-disclosure dialogue. Settled direction: **single document, Design Principles up front → mechanics as the outcome** (RFC 6762 / IKEv2-rationale lineage; philosophy must cash out into a mechanic); **iroh-first feedback venue** (iroh GH Discussions + Discord; Willow maintainers if it touches their data model; local-first community for the principles layer; Malleable Systems forum for early shaping) — **IETF is the wrong first venue** (wants 2 interop impls + running code; drafts are IETF-Trust-encumbered), a possible later destination; **defensive (not offensive) posture**; **Zenodo DOI (+ OpenTimestamps) as the priority/prior-art vehicle** over a fakeable local repo or an encumbered IETF draft. **Open work-list:** (a) **[decision] text license A14** (CC0 vs CC-BY); (b) **[doable] make §7 Synchronization enabling** (field-by-field message formats) — *gates minting the v0.1 DOI* (don't freeze off the skeleton); (c) **[doable] pre-empt the interop question in §9** (where the wire format forces two impls to honor peer-equality); (d) **[doable] add the OpenTimestamps proof + Zenodo–GitHub release integration** at v0.1; (e) **[backport] primary-source-verify** the IETF/RFC/Zenodo/OTS facts before they harden. Skeleton ready (`thinking/drystone-spec/drystone-spec-v0.1-skeleton.md`); the §2/§X deep drafts (E30) slot into it. | [explore] / [decision] | `thinking/drystone-publication-and-defensive-disclosure.md`, `thinking/drystone-spec/drystone-spec-v0.1-skeleton.md` | A14, E30, **K9** (`beta/07` Pillar C), `ECOSYSTEM.md` §publication, `COHESION.md` §38 |
| E32 | **Rights-vs-capabilities definitions — ground 01 §5's boundary (2026-06-24)** — distilled to `thinking/rights-vs-capabilities-definitions.md`. **Settled:** the discriminating test (a *right* is standing whose removal cancels the conditions of its own contestation; a *capability*'s removal leaves contestation intact — the standing-side companion to 01 §6.1's data-plane/control-plane cut) + the voice-vs-amplification edge case (voice = assert into your own record + reach willing peers, NOT compel amplification). **Open work-list:** (a) ✅ **[done 2026-06-25] folded the rights-vs-capability grounding into beta `01` §5** (user-approved; K17) — the boundary "no right to remove the rights of others" is now grounded by the discriminating test + the four-rights-by-removal + the voice-vs-amplification cut; (b) **[decision] is `share` fully a right** or partly a membership-class capability — name the inviolable floor (→ 07 / cooperative model) — **staged beta `OPEN-THREADS` T21**; (c) **[doable] does the `04` survivor re-key strand a peer's `tenure`** — if so, name tenure's implementation-level exception explicitly (protocol check vs the re-key/survivor mechanism) — **staged beta `OPEN-THREADS` T22**. Until (b)/(c) clear, do not harden the four-rights *closed set* into the normative spec. | [decision] / [doable] | `thinking/rights-vs-capabilities-definitions.md` | `beta/01` §5/§6.1 (K6), `beta/05` (rights-floor), `beta/06` (label-not-enforce), `beta/04` (survivor re-key), `thinking/drystone-spec/` §2.3/§5, `COHESION.md` §39 |
| E34 | **RUN-AP-02 — outbound AP delivery mechanics (AP-OC-6) (2026-07-20)** — the natural next run after RUN-AP-01: queue, retry span, sharedInbox strategy for outbound-projected activities. Lean-projection posture already landed in RUN-AP-01 §3 as **posture language only** — the mechanics are AP-OC-6. Uses fed-shim as receiver for in-env integration tests (RUN-FED-SHIM-01, `alpha/experiments/fed-shim/`). The interesting question is idempotence-under-partial-failure (already-emitted-detection using the ambassador's receipt lane as source-of-truth); the queue/retry mechanics themselves are mostly mechanical. | [doable] | `alpha/experiments/ap-ambassador/AP-AMBASSADOR.md` §4 (AP-OC-6) | RUN-AP-01, RUN-FED-SHIM-01, `EXPERIMENT-BACKLOG.md` §6i |
| E35 | **RUN-FS-01 — xmtp-ambassador (second-instance of the delivery-plane-role charter) (2026-07-20)** — blocked on **A15** (FS-V1..V5 owner walk) + **B14** (libxmtp compile check). Once both clear, execute red-first mirroring RUN-AP-01's shape: P1 verify_xmtp_envelope, P2 canonical dag-cbor with XMTP-specific fields (inbox_id, installation_key, mls_group_id, epoch), P3 interval fold on ConsentGranted/Revoked pairs, P4 (recommend NO Delete rider per FS-V3), P5 identical role boundary shape (permanent-red pair), P6 dual-proof binding via XMTP `AssociationState`, P7 non-touch. **The abstraction validation is the run's purpose** — if a second instance of the delivery-plane-role charter cements the same shape as the first, the abstraction is worth extracting as its own primitive (E37). | [decision] / [doable] | `alpha/experiments/RUN-FS-01-OPEN-QUESTIONS.md`, RUN-AP-01 §0 governing principle | A15, B14, RUN-AP-01, E37 |
| E36 | **fed-shim wire-conformance OCs (RUN-FED-SHIM-01)** (2026-07-20) — three surfaced-not-decided OCs from the shim's landing: (a) **FS-SHIM-OC-1** sharedInbox emit — real Mastodon batches Delete(Actor) fan-out via sharedInbox endpoints; the shim's `build_inbox_post` is per-peer today, sharedInbox awareness is a delivery-run concern (E34/AP-OC-6). (b) **FS-SHIM-OC-2** additional AP kinds — `Announce`/`Like`/`Update`/`Note` return `NotAShim`; a future run may add them if downstream ambassadors need them (touches A17 / AP-OC-9). (c) **FS-SHIM-OC-3** algorithms beyond RSA-SHA256 — some fediverse servers now emit `hs2019`/Ed25519 signatures; the current fixture leg is RSA-only. Ed25519 verify would need adding to both fed-shim SIGN and ap-ambassador VERIFY (dual-implementation discipline). | [doable] | `alpha/experiments/RUN-FED-SHIM-01-SUMMARY.md` §OCs | E34, A17, `alpha/experiments/fed-shim/FED-SHIM.md` §3 |
| E37 | **Delivery-plane-role abstraction as its own primitive (2026-07-20)** — if RUN-FS-01 (E35) instantiates the same charter shape as RUN-AP-01 (same §0 governing principle, same P5 permanent-red pair, same evidence-complete+blinded record shape, same subject-initiated dual-proof binding), extract the abstraction as a `role-charter/` doc (or crate) that both crates instantiate. Cross-cutting design run. Premature until a third instance is credible; landing E35 is the gate. | [explore] | Two-instance evidence: RUN-AP-01 (P5 permanent-red pair, GovernanceBoundaryError, distinct newtype `ReceiptId ≠ ObjectId`) + RUN-FS-01 (candidate — E35) | E35, RUN-AP-01 §P5, `EXPERIMENT-BACKLOG.md` §6i |
| E38 | **AP-OC-7 — lexicon drafts (`ing.croft.ap.*`) for the outbound-delivery run (2026-07-20)** — deferred until the delivery run (E34) needs them (a lexicon draft describes a fact shape, and outbound facts are the delivery half). Not sketched this run per RUN-AP-01 §5 stop rule 1 (no owner decisions taken speculatively). Once E34 lands, the lexicon draft has concrete field-shape evidence to land against; mirrors how RUN-ATTEST-03's `ing.croft.attest.*` lexicons land against real fact-shape evidence from that lane's atproto matchup. | [doable] | `alpha/experiments/ap-ambassador/AP-AMBASSADOR.md` §4 (AP-OC-7) | E34, RUN-ATTEST-03 lexicon precedent, `attest-family/lexicons/` |
| E33 | **Croft.ing website + serverless plot surface (2026-07-10)** — the product/website *realization* of the atmospheric-web thread (E1): "GeoCities-2027-on-atproto" made concrete as **Croft.ing**. Distilled **directly into beta** → `beta/croft/croft-ing-the-website-and-the-plot.md` + `beta/socialization/visual-identity-and-the-progressive-depth-website.md` + `beta/socialization/logo-croft-ing-wall-to-gate.md`. **Settled design direction:** the **Plot·Shed·Gate** spatial model; the **single-renderer "safety deposit box"** (`plot.croft.ing?user=` handle *or* DID → same PDS-backed canvas as `name.croft.ing`; domain is an alias, DID is the anchor); the serverless social engine mapped onto **standard atproto records against an Anchor Post** (footprints = likes, guestbook = replies, media = PDS blobs via CDN, themes = CSS-as-JSON) — **no custom AppView** (that is the Cohost cost/rug-pull failure mode); the **plot-tender delight catalog** (Lantern Vigil + pull-up-a-stool, Stile, Stone Circle, Specimen Book, Greenhouse/Seeds, interactive homestead map, sketch-blooms-to-watercolor default); the **Bluesky integration** (Gate Feed custom feed, subdomain signpost, starter pack, thread-embeds); the **tectonic visual system** (granite/ruddy/moss palette, Lora+Inter) and the **progressive-depth website IA** (elevator-pitch → one-pager → library). **Open work-list:** (a) **[decision] abuse/moderation model for public-XRPC-write widgets** (footprints/guestbook) — what the plot filters client-side (valid-DID, Stone-Circle allow/deny); (b) **[decision] user-HTML/CSS sandbox-safety posture** given the "no JavaScript" stance; (c) **[doable] automate subdomain mapping via atproto** (`_atproto` TXT / A-record on managed DNS) — the DNS-propagation limbo the single-renderer sidesteps; (d) **[backport] confirm the [confirm-before-publish] facts** — rug-pull figures (GeoCities/MySpace/Cohost), exact XRPC method names, spores.garden/Hypha + link-in-bio project mechanics (`ECOSYSTEM.md` §5c-2); (e) **[decision] ship-scope of the delight catalog** (default template vs optional widgets); (f) **[backport] finalize palette hex + choose primary logo** (wall-to-gate wordmark vs drystone-stacking badge) — the T4 brand gate. Reuses **E3** (gateway-HTML XSS surface), **E11** (relay/deep-link reality), and the **C6/"grow your own"** brand naming. | [explore] / [decision] | `seeds/transcripts/raw/croft-website-plot-tender-design-dialogue-2026-07-10.md` | **E1/E2/E3**, **E5** (app layer), **E11**, **C6**, `beta/croft/croft-ing-the-website-and-the-plot.md`, `beta/socialization/` (visual-identity, logo), `ECOSYSTEM.md` §5b/§5c-2, `COHESION.md` §43 |

| E39 | **Aggregator pond (atproto path) = the intended next pond build (2026-07-20)** — direction set, **organizing not building**: extend the proven arecipe/skylite atproto-pad lineage with a Bluesky/Mastodon/Lemmy aggregator pond that inherits a population (no empty-room problem) and needs no deep-link resolver. Converges with the meetup/community beachhead (D11) and the dating-fit report's rec #2 (RSVP+roster primitive reusing the Smoke Signal strong-reference pattern) — an aggregator/events pond is where those meet. The iroh-native ponds (games, native Croft Group pond) remain gated on the unbuilt tier-zero resolver (`thinking/app/ponds/build-order.md` §0.2); not this pass. Full build-state cross-cut in `BUILD-INVENTORY.md`. | [explore] / [decision] | `BUILD-INVENTORY.md` (Current direction) | D11, E6 (games pond), `thinking/app/ponds/build-order.md`, `research/dating-friendship-meetup-fit-2026-07.md` |

| E40 | **Utility-representative social graph as a low-noise overlay (2026-07-20)** — design question layered on the already-built attest primitives: how should verified "knows-you" attestations + scoped descriptors ("friend from school") *surface* as a graph representative of actual utility rather than happenstance (same high school + volleyball league ≠ real overlap), without inference-fatigue — a permanent, low-noise overlay that informs only on meaningful connection, with the person controlling openness about self and connections. Mechanics exist (`experiments/attest-family/PRIMITIVES-ATTEST.md`: attestation/vouch/scope/resolvability/anchor-persona); this is the surfacing/UX question, not a new primitive. | [explore] | `thinking/social-layer.md` §7 | COHESION §45, `experiments/attest-family/PRIMITIVES-ATTEST.md`, S3/S4 |
| E42 | **Formally publish the human-adjudication design (RFC-style) (2026-07-20)** — the user's stated interest: publish Drystone's "legitimate questions the machine isn't meant to answer" mechanism (fork-not-verdict, the algedonic channel, escalation to the local authority) as a formal contribution, given the precedent that formal specs *do* encode required-human-input (RFC 8126 designated-expert / Expert Review, W3C Permissions consent gates, RFC 7282 rough consensus, Beer's algedonic channel). The vocabulary is settled and codified (conventions §A.11); this is the packaging/venue question. Folds toward the existing publication/defensive-disclosure thread. | [explore] / [decision] | `beta/drystone-spec/conventions-and-decisions.md` §A.11 | `thinking/drystone-publication-and-defensive-disclosure.md`, A14, COHESION §50 |
| E41 | **Berjon ap-at walkthrough — AP atop an ATProto PDS, indie-AP-with-custom-domains focus (2026-07-20)** — walk `berjon.com/ap-at` (ActivityPub could run atop an ATProto PDS; the Actor document as the indirection point → the two protocols may not compete at the same layer), with special attention to indie ActivityPub sites using custom domains, then decide what to capture (likely into `beta/cairn/activitypub-atproto-and-the-defacto-standard.md`). Open request from the 2026-07 dialogue; deferred, not done. | [explore] | `beta/cairn/activitypub-atproto-and-the-defacto-standard.md` (Berjon section) | `beta/socialization/kindred-work.md` (berjon-robin), COHESION §48 |

| E43 | **Virtual group cards / guestbooks pad — public via bare atproto records, private via sealed-or-followers tier (2026-07-21)** — a GroupGreeting-style pad (many signers → one recipient, scheduled reveal) plus the broader card/guestbook family; a simple atproto-pad-class use case, similar in utility to the arecipe/skylite pads (E39/E8). Motivation: incumbents (e.g. GroupGreeting) are weak on UI and functionally basic; the mechanism is a near-free fit for Croft. Maps onto the two-axis tier model (`appview-infra/GROUPS.md`) and the degeneration principle (`appview-infra/PUBLICATIONS.md`): **(a) public card / open guestbook** = open|gated membership + open|named-set write → bare chained atproto records, no machinery (already the Croft.ing model: guestbook = replies on an Anchor Post, footprints/media = records/blobs); **(b) private card** = confidentiality required, two arms the user named — **sealed** (attach to an MLS group, E2EE; signers are members, recipient added at reveal) or **"followers-level"/gated** (the trusted-gatekeeper backplane tier, RUN-16: roster-gated "private but not E2EE", browser-first, AppView gates *offering* by verified roster membership) — the followers-level arm is the natural privacy level for the AP integration. **Reveal wrinkle:** a card is a write-restricted scope (`named-set` signers) with a **time/event-gated read grant** to the recipient, who must stay blind until reveal — the RUN-18 "withheld TAIL undetectable until newer arrives" reception-completeness limit is a *feature* here (blind-until-reveal), not a bug. Distinct from the already-filed guestbook cases (tier-2 side-history in `side-histories-and-threading.md`; the Presence & Ritual guestbook in `presence-ritual-and-composed-ponds.md`): the *card* adds a defined recipient + scheduled reveal + private-by-default. Open calls: **[decision]** default private arm (sealed vs followers-level); **[decision]** public-write abuse model (shared with the Croft.ing footprints/guestbook widget, E33 open-call a); **[explore]** the scheduled/withheld read-grant primitive. **Design note (buildable design, verified atproto facts):** `alpha/thinking/app/ponds/virtual-cards-and-guestbooks.md` — the privacy ladder (bearer / server-blind link-key / MLS-sealed), the no-login constraint (anonymous append is not PDS-native → a content-blind ingest service is required), why the write credential cannot ride in the URL (atproto OAuth = DPoP-bound, short-lived, single-use refresh), per-collection OAuth scope bounded by revocation not time, and the PDS-holds-state / shim-is-a-time-boxed-blind-writer decomposition. **Productization underway (2026-07-21): `greetings.croft.ing`** — the 1:1 link-delivered slice (public bare record + server-blind link-key sealed card, AES-256-GCM in-browser) is being built as a static PWA in `CroftCommunity/greetings_site` per plan `alpha/plans/2026-07-21-greetings-croft-ing-mvp.md` (Phase 1a shipped live; Phase 1b shell+PWA next). The anon-multi-write / guestbook / registry arms and the scheduled reveal remain deferred (they need the DPoP-scoped mediating shim). | [explore] / [decision] | conversational intake 2026-07-21 (GroupGreeting UI/functionality critique; no transcript) | `alpha/thinking/app/ponds/virtual-cards-and-guestbooks.md` (design note), E8 (ponds/pads catalog), E39 (aggregator pond), E33 (Croft.ing guestbook/footprints + abuse-model open call), `alpha/experiments/appview-infra/GROUPS.md` (two-axis tiers), `alpha/experiments/appview-infra/PUBLICATIONS.md` (degeneration principle / write-restricted scopes), `beta/impl/mls/side-histories-and-threading.md` (tier-2 guestbook), `beta/croft/presence-ritual-and-composed-ponds.md` (guestbook), `beta/croft/croft-ing-the-website-and-the-plot.md` (Anchor-Post guestbook) |

| E44 | **The `account.croft.ing` client-side kernel (new body, 2026-07-22)** — one shared origin, reached by every `*.croft.ing` subdomain via an embedded iframe, holding **session broker + encrypted-prefs cache (`ing.croft.account.prefs`, ciphertext synced via the PDS) + `repo-mirror` (local CID-verified PDS mirror over `getRepo`/`getBlocks`/`listRecords`, Sync-v1.1 causal-log) + write outbox**; one SharedWorker sync loop (Web-Locks fallback), postMessage RPC, BroadcastChannel fan-out. Subdomains become **stateless skins**. Includes **recipient-facing Web Push** via the shrunk helper (push + session-attestation only; Pushover ops-only). **Open work-list:** (a) **[decision, TENTATIVE CONCLUSION] untrusted-renderer domain separation** — the "open any card" renderer lives on a *separate registrable domain* (or host-only session + kernel-brokered), revisitable; cheap now, migration once 3 apps depend on it; (b) **[explore] single-origin quota/eviction posture** (`persist()` the outbox; the mirror is a rebuildable cache); (c) **[explore] kernel = highest-value compromise target** → keep tiny, dependency-light, most-reviewed. **Thinnest-covered area in the corpus** (nearest: `client-architecture-adr.md`, `research/atproto-private-data-architecture.md`). Staged `beta/OPEN-THREADS.md`. | [explore] / [decision] | `seeds/transcripts/raw/croft-encrypted-prefs-repo-mirror-and-account-kernel-2026-07-22.md`, `...card-maker-webxdc-packaging-and-push-2026-07-22.md` | E43 (cards), E33 (Croft.ing), `thinking/app/client-architecture-adr.md`, `research/atproto-private-data-architecture.md`, `thinking/app/pwa-spa-best-practices.md`, `COHESION.md` §53, `beta/OPEN-THREADS.md` |
| E45 | **Card-maker packaging model — the output ladder + collaborative card (2026-07-22)** — the maker is a **hosted PWA** (`greetings.croft.ing`, a page you arrive at); the card is the **portable artifact**. Three-rung output ladder — **image** (max compat) → **single self-contained HTML** (animate/respond, no runtime) → **`.xdc`** only when shared state accumulates across people (the collaborative group card, the case that earns webxdc). Collaborative card **cannot be file-only** (`.xdc` is inert without a host; worst recipient profile) → a hosted URL opens the same card with the same accumulated signatures, `.xdc` is the enhancement, link is the floor; **Croft Chat is the natural host but must stay optional**. **Carry the static rendering as a declared manifest asset** (don't derive via headless capture); collaborative snapshot needs regeneration + a "taken-at" timestamp. Extends the shipped greetings MVP (E43); the collaborative/multi-writer arm is the **same deferred anon-multi-write primitive** E43 left out. | [explore] | `seeds/transcripts/raw/croft-card-maker-webxdc-packaging-and-push-2026-07-22.md` | **E43** (greetings/cards), `plans/2026-07-21-greetings-croft-ing-mvp.md`, `thinking/app/ponds/virtual-cards-and-guestbooks.md`, `beta/croft/product-the-garden-of-ponds.md`, E44 (renderer domain gate), `COHESION.md` §53 |
| E46 | **Ponds/games roadmap consolidation — "what to build and how it stacks" (2026-07-22)** — the user's explicit "not ordered yet" ask for `fun.croft.ing`. Consolidate the phased game build plan (**P1–P10**: determinism-first golden-vectors/rules-doc, **P10 Sustainment** as a standing drill with a compatibility matrix + annual byte-identical regeneration + exit artefacts, **verifiable clean-clear** via move-list replay vs P1 state-hash, **follow-chain-not-global leaderboard** per the user's scope cut, **solitaire-P1-first** sequencing) with the existing `thinking/app/ponds/build-order.md` + `build-shape-pass.md` + PRD stubs + `beta/croft/product-the-garden-of-ponds.md`. Browser-native P2P for WASM games ("iroh but browser-native") = **WebRTC/matchbox, already covered** in `beta/cairn/iroh-app-pond-building-blocks.md` (GGRS+matchbox) → cross-reference, not net-new. Games pond is "candidate, not committed" (existing OPEN-THREADS T-thread). **Phase-2 candidate deliverable** (a consolidated roadmap doc). **P1 build STARTED 2026-07-27** — the match-3 determinism foundation is spiked at `experiments/match3-p1/` (Rust workspace `match3-core`): rules doc + tie-break tables, golden-vector corpus, headless deterministic engine (match/clear/gravity/refill/cascade), verifiable move-list-replay-vs-SHA-256-state-hash; red-first, 19 tests green. P1 decisions locked: Rust→wasm, plain match-3 (no specials), one layered blocker; cascade multipliers/par/specials deferred as owner balance decisions; native+wasm cross-build determinism test is next. **PHASE-PLAN AUTHORED 2026-07-28** (Pass 1+2+3 complete, execution-ready; no code written yet) — `alpha/plans/2026-07-27-games-pond-fun-crofting.md`: promote `match3-core` into a **new standalone repo `CroftCommunity/fun`** (local checkout `CroftC/fun`, remote via `github-personal`/chasemp) and stand up **`fun.croft.ing`** as a game **shelf**, order **solitaire (Klondike draw-1) → match-3 → cribbage**. 9 phases (0 Discovery → 1 repo+promote → 2 cross-build test → 3‖4 match-3-hygiene‖solitaire-P1 → 5 P2 `pond-docformat` → 6 P8 `pond-outcome` → 7 shelf+solitaire live on GitHub Pages → 8 match-3 par/levels; **Phase 9 cribbage GATED to its own plan** on P2P transport + fair-reveal). Resolved: P5/P7/P9 unrecoverable → designed-when-reached (definable spine P1/P2/P3/P4/P6/P8/P10); host GitHub Pages; envelope designed in Phase 5; keep `experiments/match3-p1/` as tombstone; skip feel-spike. **Decision PENDING:** solitaire-par/solver (agent rec = match-3-only). **FRONT-END COMPANION PLAN 2026-07-28** — `alpha/plans/2026-07-28-games-drawer-solitaire-ui.md` (Pass 1+2; expands master Phase 7): build **solitaire as the first game in a slide-out drawer** (+ full-screen + open-in-new-tab → per-game URLs → chrome-agnostic game-module mount contract) to establish the overall drawer UI/UX. Stack matches croft-pwa (vanilla TS + esbuild + Vitest + Playwright + axe + SW + tokens); own playful identity on croft-pwa's token architecture; tap-to-move first (drag as fast-follow); wasm-bindgen + serde-JSON board state for the `solitaire-core`→browser binding. 7 phases (0 spikes → 1 drawer chrome → 2 design system → 3 wasm binding → 4 solitaire playable → 5 PWA/a11y → 6 deploy; P7 drag fast-follow). **`fun.croft.ing` domain + GitHub Pages LIVE 2026-07-28.** **EXECUTION STARTED 2026-07-28 — master-plan Phase 1 SHIPPED:** repo `CroftCommunity/fun` created (local `CroftC/fun`), Cargo workspace with `match3-core` promoted (self-contained, `cargo test --workspace` 19 green; fmt+clippy clean) + solitaire-core/pond-docformat/pond-outcome/match3-wasm/solitaire-wasm stubs + a minimal static web skeleton (`npm run build`→dist/); both plans copied to `fun/plans/`. `match3-p1` experiment tombstoned as frozen provenance. **DELIVERY SLICE (`alpha/plans/2026-07-29-playable-solitaire.md`) SHIPPED THROUGH PHASE D 2026-07-29 — solitaire is PLAYABLE at `/solitaire/`:** A `pond-docformat` (808df73) → B `pond-outcome` (85df812) → C wasm binding (ff55c0d) → S Klondike solver + winnable-daily pack (e52da18) → **D board UI** — tap-to-move with core-driven legal-target glow, daily deal (UTC) + free-play, undo + declare-assistance, "I'm stuck", verification-forward win screen (re-verify by replay) + deflated `?r=` share link that re-verifies on open. Web: typecheck/lint/vitest(19)/Playwright(10, incl. axe) green; Rust workspace 52 green. **Then ✅ Phase E (playful identity, `c95bb6f`):** a green-felt / ivory-card / brass-accent identity on croft-pwa's token architecture (`tokens.css` only-file-with-hex, WCAG-AA recorded + asserted), light/dark theme with a header toggle + pre-paint no-flash script, axe clean on chrome + board in both themes, `docs/DESIGN.md` (vitest 50 / Playwright 14 / Rust 52). **Delivery slice COMPLETE — all phases A–E shipped.** **Post-ship polish DONE 2026-07-30:** new-deal fully redeals; a **standard hints/settings** mechanism (`settings.ts`: Enable hints on-default → Hint points at a legal move / hints-off → "I'm stuck" ends + reports if a move existed; Declare assistance; opt-in auto-play-to-foundations) + a **standard per-game "How to play" guide** (`/how-to/?game=`, pure-data blocks + guide-shots + sync tests) + `docs/BUILDING-GAMES.md` (the games build-guide/standards); drag-and-drop; win cascade; responsive 360px fit; self-hosted Fredoka (OFL); and a **seeds-lean v2 daily pack** (365 winnable seeds + a 131-move fixture, ~5 KB, byte-identically regenerable). Live at `fun.croft.ing`; Rust 53 / vitest 56 / Playwright 24 green (`fun` `bbbe083`). **✅ SECOND GAME SHIPPED 2026-07-30 — match-3 playable (`fun` `db568c4`):** Candy-Crush-style target-score-in-moves with star thresholds (owner-decided objective; `plans/2026-07-30-match3-playable.md`). match3-core deal generator + match3-wasm binding + a board UI (tap-a-gem→adjacent swap, core-driven legal-swap glow, 20-swap budget → 0–3 stars, verifiable score+stars record + `?r=` share) + how-to guide; `pond-outcome` gained additive score/stars + `Outcome::Lost`; 6 colour-blind-safe gem tokens (shape+colour). First real exercise of `docs/BUILDING-GAMES.md` on a 2nd game. Rust 56 / vitest 78 / Playwright 31 green; live. **Next:** cribbage (gated on P2P + fair-reveal); match-3 follow-ups = per-deal par tuning, variant objectives, specials, drag. **NEW AXIS + CHARTER DECISION 2026-07-30 (Gemini games dialogue):** a **single-player, 100%-client-side, static-hostable** candidate axis (distinct from the two 2026-06-21 P2P/social catalogs) filed → `seeds/transcripts/raw/client-side-static-browser-games-catalog-2026-07-30.md` + distilled `thinking/app/ponds/client-side-static-game-candidates.md`. **Owner widened the `fun.croft.ing` charter to a two-tier shelf** (COHESION §62): **Tier 1** Croft-native build-fresh/determinism-first/verifiable + **Tier 2** opportunistic ethical wraps/ports taken as-is (no verifiable record, stated honestly), gated by a 5-point inclusion filter (client-side · non-extractive · redistribution-licensed · fits-our-chrome · honestly-represented; ROM/emulator-host paths disqualified). **Two candidates extracted → two `fun/plans/` docs:** Tier-1 **bubble shooter** (`fun/plans/2026-07-30-bubble-shooter.md`, single-player + **ungated** → parallel-buildable ahead of the P2P-gated cribbage) and the Tier-2 **TuxRacer.js wrap spike** (`fun/plans/2026-07-30-tux-racer-wrap-spike.md`, license/weight/fit gates + the reusable wrap standard). All repo/license claims Gemini-sourced `[UNVERIFIED]`. | [explore] | `seeds/transcripts/raw/croft-games-pond-roadmap-browser-p2p-phased-build-2026-07-22.md` | E6/E8 (games pond, ponds catalog), E39 (aggregator pond), `thinking/app/ponds/build-order.md`, `thinking/app/prds/games-pond.md`, `beta/cairn/iroh-app-pond-building-blocks.md`, `COHESION.md` §53, `beta/OPEN-THREADS.md` |
| E47 | **Heardle-shaped music-guessing pond (candidate, 2026-07-22)** — a local-first PWA daily music-guessing game (1s→16s clip ladder, 6 tries, constrained-autocomplete, emoji-grid share) publishing to `app.heardle.result` on the user's PDS (leaderboards read the firehose, no DB server). Design: unified audio-adapter layer (HTML5/CC offline-no-login + Spotify SDK + YouTube), per-backend daily-manifest channels, **crypto-monotonic/CID-chained anti-cheat** (user decision, replacing clock-trust), guest-first onboarding, Home-Screen-install for persistent storage (stacks with the Web-Push decision, E44), a hidden-issues catalog (autoplay unlock, Safari 7-day eviction, OAuth-vs-app-passwords, lexicon schema-drift → optional-fields, SW cache invalidation → content-addressed filenames, multi-device sequence divergence → CRDT/LWW). **Heardle's Spotify-acquisition→shutdown is a literal ENS/ephemerality case** — the product argument *for* local-first, tying this pond to E49 (ENS) and P10 Sustainment. | [explore] | `seeds/transcripts/raw/gemini-atproto-clientside-search-heardle-pond-kudoboard-2026-07-22.md` (Part B) | E46 (ponds roadmap), E44 (Web Push / Home-Screen), E49 (ENS), E50 (licensing gate), `beta/croft/product-the-garden-of-ponds.md`, `COHESION.md` §53 |
| E48 | **Client-side-search research — full fact-check (2026-07-22)** — ✅ **DONE 2026-07-22** → `research/atproto-clientside-search.md` (verified, em-dash-free, not committed). Fact-checked Part A of the Gemini report against primary sources: **~30 claims — 20 CONFIRMED, 3 PARTLY, 7 UNVERIFIED, 0 REFUTED** (unusually accurate for Gemini). All named engines (FlexSearch/MiniSearch/Orama/uFuzzy/SQLite-FTS5), VFS (IDBBatchAtomicVFS/OPFSCoopSyncVFS/AccessHandlePoolVFS in `rhashimoto/wa-sqlite`) and tooling (`atcute` `@atcute/car`+`@atcute/cbor`, `coi-serviceworker`, `sql.js-httpvfs`, `sqlite-wasm-http`) are real; Jetstream/`getRepo`-CAR/DRISL/the MST depth-formula/Tap/BM25 all check out. **Corrections (owner-relevant):** (1) **"SearchFn" appears not to exist** (classic Gemini invented-name drift; substitute FlexSearch's own IndexedDB persistence) — the one to double-check; (2) Jetstream "60–150 KB/s filtered" overstated ~6–15× (real ~850 MB/day ≈ 10 KB/s; raw >200 GB/day confirmed); (3) uFuzzy ~7.5 KB not ~3 KB; (4) MiniSearch is BM25+ not BM25. UNVERIFIED/caution: the whole bundle/memory/latency benchmark table (Gemini's own), Tap "Collection Signal Mode" (likely Gemini-coined), `journal_mode=truncate` tuning (VFS-dependent), Chrome-incognito 100 MB cap. atproto/iroh cite the FACTCHECK SoT. Closes the manifest's flagged truncated-report gap. **Remaining (optional):** cross-reference the settled mechanics into a cairn doc. | [validation] ✅ | `seeds/transcripts/raw/gemini-atproto-clientside-search-heardle-pond-kudoboard-2026-07-22.md` (Part A) | `research/atproto-clientside-search.md`, E44 (repo-mirror/kernel uses these mechanics), `research/atproto-private-data-architecture.md`, `beta/cairn/atproto-*`, FACTCHECK SoT, `COHESION.md` §53, `beta/OPEN-THREADS.md` T60 |
| E49 | **ENS multi-vantage framework — CANDIDATE THINKING (2026-07-22)** — a polite, FOSS-tradition re-expansion of "enshittification": the recursive **"ENS is Not Service"** wrap over three lenses of one arc — Business = *Extractive Neglect Syndrome* (cause), Systems = *Eventual Non-utility State* (mechanic), User = *Erosive Nudge Syndrome* (symptom). **Not settled law** (user decision) — the concept and the specific expansions are candidate; **do NOT write to `NAMING.md`**; staged `beta/OPEN-THREADS.md`. Reasoning home when settled: `beta/fenced/aggregation-theory-and-the-enshittification-shield.md` (`beta/socialization/coop-messaging-research.md` already flags the acronym-re-expansion move). | [explore] / [decision] | `seeds/transcripts/raw/ens-backronym-multivantage-framework-2026-07-22.md` | `beta/fenced/aggregation-theory-and-the-enshittification-shield.md`, `beta/activism/`, `beta/socialization/coop-messaging-research.md`, E47 (Heardle = ENS case), `COHESION.md` §53, `beta/OPEN-THREADS.md` |
| E51 | **The "helper" tier — optional enhancement services with a shared contract (2026-07-22)** — a growing class of server-side components that *enhance* but are **not required for acceptable baseline usage**; tie them together under one contract so the serverless/local-first baseline stays real and the trust/attack surface stays enumerated + isolated. **Contract (5 rules):** optional · declared degradation · isolated trust surface (own origin, minimal state) · replaceable/self-hostable · reads stay direct. **Register (first pass):** custom AppView (baseline = public AppView + direct getRecord; pdsview proves AppView-less) · BFF/token (baseline = per-app login) · AP-followers restriction (baseline = public or sealed) · Web-Push helper (baseline = in-app/on-open) · content-blind ingest shim (baseline = named-set writers, no anon append). Also helpers (reclassified 2026-07-22 to the honest floor): **iroh relay** (baseline = same-network LAN direct P2P; relay extends to cross-network/NAT) · **deep-link resolver** (baseline = manual ticket/code join; resolver = smooth deep-link + growth — *strategically* huge but not a functional need) · **meer / Delivery Service** (baseline = direct delivery to online peers; DS = store-and-forward for offline). **Base-base case (the floor):** 2 people, same network, direct connection, manual join code — zero helpers, zero servers. **Carried forward from Drystone** (protocol→product): peer-equality floor + `P-Durable-Enablement` + exitability; helpers = the bounded, route-around-able unequal-peer roles above the equal-peer floor. Doc: `thinking/app/helpers-register.md`. | [explore] | conversational intake 2026-07-22 (user surfaced the pattern) | `thinking/app/helpers-register.md`, `thinking/app/estate-architecture-and-browser-constraints.md`, E43 (ingest shim), E44 (BFF/kernel), ECOSYSTEM §5f (AppView), `crystallized/principles.md` (P-Durable-Enablement), `COHESION.md` §53 |
| E50 | **Music-game licensing gate — BACKLOG + candidate thinking only (2026-07-22, NOT-LEGAL-ADVICE)** — the hybrid shape is decided (few CC0/public-domain tunes + original **"sounds-like" pastiche** + auth-gated official integrations, guest-first, per-backend challenges). The residual legal question — **is pastiche too close, and Nintendo/SEGA/JASRAC rights** (no reuse/re-arrangement of Mario 1-1 / Sonic Green Hill / Mario 3 Bowser; composition copyright bites even for re-arranged chiptune) — is **not a resolved gate**; the 6-channel starter set (3 CC0/PD: Grieg "Hall of the Mountain King", Kevin-MacLeod "Pixel Rush", Pachelbel "Canon in D"; 3 pastiche archetypes) is a candidate proposal. Legal review is the user's; surface, don't resolve (PLAYBOOK §5). | [decision] (candidate) | `seeds/transcripts/raw/gemini-atproto-clientside-search-heardle-pond-kudoboard-2026-07-22.md` (Part B, licensing) | E47 (Heardle pond), `COHESION.md` §53, `beta/OPEN-THREADS.md` |
| E52 | **Verify the Bluesky autonomy-origin claims (2026-07-23)** — ✅ **DONE 2026-07-23** → `research/2026-07-23-batch-factcheck.md`. Verdicts: announced Dec 11 2019 ✓; Graber hired Aug 2021 ✓; incorporated Oct 4 2021 / PBC Feb 2022 (~$13M Twitter funding, tie severed late 2022) — "spun out as PBC" loosely right; Musk Oct 28 2022 ✓, X rebrand July 2023 ✓. **Scale PARTLY:** 41.41M **registered accounts** end-2025, >45M mid-2026, DAU ~4.5M — "~40M" was accurate late 2025, now understated, and is *registered not active*; "40M-accessible-scale" is the author's framing. The beta asset (`the-autonomy-origin-of-the-substrate.md`) updated to say **registered accounts**. | [validation] ✅ | `seeds/transcripts/raw/bluesky-atproto-autonomy-origin-story-2026-07-23.md` | `research/2026-07-23-batch-factcheck.md`, `beta/socialization/the-autonomy-origin-of-the-substrate.md`, ECOSYSTEM (`Bluesky PBC` rows), `COHESION.md` §55 |
| E61 | **Phase-2 fact-check of the 2026-07-23 arecipe + measurement claims** — ✅ **DONE 2026-07-23** → `research/2026-07-23-batch-factcheck.md` (~30 claims; mostly CONFIRMED). **Corrections that change a build:** (1) RUN-MEASURE-01 §E5 — the **sendBeacon rates are mis-sourced/inverted** (real ~82% at unload/pagehide, ~92% at page-load; 95.8% unsourced) and the **64 KiB cap is a Fetch-keepalive/browser behavior (shared quota on Chrome), not a Beacon-spec mandate** → fix E5 before running (feeds E60). (2) The **Wikibooks `{{Recipe summary}}` has no `category` param** (auto via difficulty) and the **pipe-trick is not policy-mandated** → the importer maps difficulty→category and must not assume pipe-trick (feeds E56). Browser/PWA/DNS (E53–E55 substrate) all CONFIRMED (keyboardLock exit = SHOULD; Escape reaches an ordinary page). | [validation] ✅ | `research/2026-07-23-batch-factcheck.md` | E56 (Wikibooks importer), E57, E60 (measurement kit), `COHESION.md` §57/§58 |
| E53 | **arecipe timer page + seasonality boost (RUN files in `timers.zip`, 2026-07-23)** — build the timer page (**store absolute end timestamps, never remaining seconds**; 8-hour-sleep zero-drift test; no faked background alerts) and seasonality (**boost-only, never a drag, toggleable**; boost-never-reorders-down test + off = deep-equal-to-baseline; region explicit, no geolocation; grep against out-of-season copy). RUN files delivered as `timers.zip` — **not captured**; capture verbatim under `seeds/*-unpacked/` at Phase 2 if provided. | [build] | `seeds/transcripts/raw/arecipe-cname-pwa-keyboard-camera-ai-import-timer-seasonality-2026-07-23.md` | `seeds/arecipe-unpacked/`, `COHESION.md` §57 |
| E54 | **arecipe LLM recipe-import experiment (2026-07-23)** — the safe rule: **model selects spans, never writes**, enforced by a **verbatim-substring invariant** (a returned string absent from the input rejects the extraction wholesale); deterministic JSON-LD wins first (model only fills a gap); the editor is the confirmation surface; desktop-only. **Instrument the deterministic miss-rate and the CORS ceiling BEFORE adding a model** (with no backend, un-fetchable share URLs are a permanent bound). RUN delivered in `timers.zip` (exp-import-extraction) — not captured. | [explore] | `seeds/transcripts/raw/arecipe-cname-pwa-keyboard-camera-ai-import-timer-seasonality-2026-07-23.md` | `seeds/arecipe-unpacked/RUN-RECIPE-IMPORT-INSTRUCTIONS.md`, `COHESION.md` §57 |
| E55 | **arecipe guide helper — deterministic→LLM with deep-link anchors (2026-07-23)** — Layer A ships alone; Layer B = hand-written question phrasings; a **Phase-3 stop-gate** (if curated phrasings hit top-1 accuracy, Layer C / the model is never built — the better outcome); the helper returns **deep links to exactly where the user needs to go**; anchor-validation mirrors the import invariant (a model may rank/summarize retrieved text, may not invent a destination); **no query logged or transmitted**. RUN delivered in `timers.zip` (run-guide-helper) — not captured. | [explore] | `seeds/transcripts/raw/arecipe-cname-pwa-keyboard-camera-ai-import-timer-seasonality-2026-07-23.md` | `seeds/arecipe-unpacked/`, `COHESION.md` §57 |
| E56 | **arecipe Wikibooks corpus importer CLI (2026-07-23)** — a local CLI pulling `en.wikibooks.org/wiki/Cookbook` recipes into a structured local representation with **intelligent delta handling** (rerun in 6 months): `categorymembers` enumeration (authoritative over the ToC), `{{Recipe summary}}` as a sparse map, pipe-trick ingredients = link-parse, **store last revid per page + refetch only changed**; **NOT recentchanges** (`$wgRCMaxAge` 30d would silently miss 5 months → full enumeration + batched revision sweep); Action API not deprecated dumps (User-Agent + ≤3 concurrent + `Retry-After`); CC-BY-SA source-URL/revid/license stamping; `--reparse` walks parser improvements offline. **Open owner calls:** separate repo vs `tools/`; the licence/attribution string; ShareAlike on user-edit; the PDS account hosting the corpus. RUN in `wiki.zip` (run-wikibooks-corpus) — not captured. | [build] / [decision] | `seeds/transcripts/raw/arecipe-wikibooks-corpus-import-and-static-precache-2026-07-23.md` | `seeds/arecipe-unpacked/RUN-SHOPPING-LIST-INSTRUCTIONS.md` (ingredient parser), `COHESION.md` §57 |
| E57 | **arecipe recipe meta-strip — serves/time/difficulty (2026-07-23)** — surface serves + time + difficulty (currently only prep time) as **3 rows hanging off the bottom of the image**. Opens with a **D0 discovery gate**: recipes are `exchange.recipe.recipe` owned by `recipe.exchange`, so where `serves`/`difficulty` live is **not arecipe's unilateral call** — report what exists, then branch (recommendation: open-world fields on arecipe-authored records + an upstream proposal in parallel). Infobox values are free text → display-plus-hint, never rewrite the source string. RUN in `wiki.zip` (run-recipe-meta-strip) — not captured. | [build] / [decision] | `seeds/transcripts/raw/arecipe-wikibooks-corpus-import-and-static-precache-2026-07-23.md` | `seeds/arecipe-unpacked/`, `COHESION.md` §57 |
| E58 | **arecipe bundle-precache / static-list release process (2026-07-23)** — speed first-load and PDS load for the mostly-static followed-cooks list: **bundle a build-time snapshot + the rev it was taken at, render instantly, then one tiny `getLatestCommit` rev check per followed repo in the background, pull only when rev moved** (`getRepo since rev` → CAR of MST blocks, needs a CAR/CBOR decoder; per-repo not per-collection; fall back to `listRecords` to keep the bundle thin). **Treat the bundle as a cold-start cache, never authority** (names/handles go stale). RUN in `wiki.zip` (run-bundle-precache) — not captured. | [build] | `seeds/transcripts/raw/arecipe-wikibooks-corpus-import-and-static-precache-2026-07-23.md` | `seeds/arecipe-unpacked/RUN-COOK-FOLLOWS-INSTRUCTIONS.md`, `research/atproto-clientside-search.md`, `COHESION.md` §57 |
| E59 | **arecipe empty-tile chip — mobile vertical real estate (2026-07-23)** — pictureless tiles as an **inline chip at single-column** (media zone retained at multi-column), fixing the 3:2-vs-16:7 mismatch; D0 discovery gate, TDD red-first, a falsifiable Phase-4 measurement table (stop if tiles-per-screen at 360px doesn't improve). **Owner-review item:** the multi-column ratio correction is a genuine bug fix that was *not* explicitly ruled — isolated in one revertable rule, flag before shipping. RUN-EMPTY-TILE-CHIP is preserved verbatim in the raw. | [build] / [decision] | `seeds/transcripts/raw/arecipe-empty-tile-chip-mobile-2026-07-23.md` | `seeds/arecipe-unpacked/RUN-RESET-SURFACE-V2-INSTRUCTIONS.md` (shared SVG helper, touch-target floor), `COHESION.md` §57 |
| E60 | **Usage-measurement kit — proof-out EXECUTED + fact-check done (2026-07-23, NEW HELPER)** — ✅ **BUILT as arecipe PR #58** ("Add measure-proof", branch `claude/measurement-kit-proof-out-dqsmdc`, OPEN, 56 files, +4481/−2): `measure-proof/` runs RUN-MEASURE-01 E0–E8 (registry→client+panel+fixtures; seeded corpus E0; re-linkage attacks A1–A3+mitigations E3; first-order Markov flow E2; Litestream/R2 math E7; rounding-impact E8; Playwright PWA harness E5); **48 unit + 4 e2e green**, findings pinned in `measure-proof/findings/*.json`, `RUN-MEASURE-01-SUMMARY.md`. Proof-of-concept, not a product build — extraction into the app is a later explicit step. Fact-check (E61) applied: sendBeacon rates REFUTED (real ~82% unload/~92% load), 64 KiB = Fetch-keepalive shared quota — **E5 measured synthetic visibility events**, so those corrections bound the real-world interpretation, not the harness. Remaining: review PR #58 findings (esp. E3 residual re-linkage, E2 falsification pair, E8 rounding threshold); the kit **name/home** + rounding-vs-exact (E8) calls. **Owner calls:** ✅ **default local-history retention = bounded retention + visible clear control** (user, 2026-07-23; mirrors the Skylite sponsor-vs-child stance). Still open: the kit's **name and home**; **rounding vs exact counts** (gated on E8); whether `edge` sits inside the three-event ceiling; the **pilot** (recommendation: arecipe, Skylite last). **Pre-run fix (E61):** correct §E5's sendBeacon expectations (~82% unload / ~92% load, not 95.8%) and the shared-quota 64 KiB behavior. | [explore] / [decision] | `seeds/transcripts/raw/usage-measurement-edge-counter-privacy-kit-2026-07-23.md` | Skylite (sponsor-vs-child retention), the existing Caddy/SQLite/Litestream/R2 kit (E7), E49 (ENS/ephemerality), `COHESION.md` §58 |
| E62 | **"Social Tree" — Reddit/forum + Google-Reader-RSS lens PWA over Bluesky/atproto (2026-07-27, product/UX synthesis)** — a long product dialogue that grows one coherent client: a **local-first, client-side PWA/SPA that reframes atproto activity as a Reddit/Discourse-style forum fused with an RSS reader**, where a "board" is a saved query over the firehose (no cold-start), posting = a smooth "seed a discussion" from the reader, and a **degree-based radius model (r=0 personal / r=1 mutuals / r=2 mutuals-of-mutuals / r=∞ global)** tunes *in place* who you read and reply to. **This is E39 (aggregator pond — "a lens that inherits a population") taken to full product/UX depth**, and it sits on E48 (WASM client-side search substrate — cite, don't re-derive), E24/E42 (sovereign AppView: private feeds, blocking-as-experience-shaping, mod-list mirroring, multi-source incl. RSS), and E44 (PWA sync / encrypted vault / Web-Push / Home-Screen). Durable spine: the user's tenet **"machines compute provenance, never utility"** → strict split (machine = signatures/ZK/routing/graph-math; human = subjective utility via private local tags + radius + source-trust); an **anti-extraction / utility-over-attention** thesis actualized as finite feeds, scope-based intentionality budgets, continuous local export, zero-take direct-to-creator economics, and an **ethical WebRTC-relay commercial tier** (sell bandwidth/latency, not attention; Privacy-Pass anonymous metering); **"PWA is the floor, native follows"** on a shared Rust/WASM core. **Fact-check DONE 2026-07-27** → `research/2026-07-27-social-tree-factcheck.md` (~40 load-bearing claims, unusually accurate; **5 REFUTED, several PARTLY**). **Corrections that change the build (baked in here, do not carry the errors):** (1) **no native atproto E2EE DMs / "Privately Shared Data"** — native DMs are server-visible; E2EE is third-party only (Germ/MLS, already in `ECOSYSTEM.md` §6) → the r=1/private plane needs a **sidecar transport** (MLS/Matrix/"Dark PDS"/ephemeral WebRTC), never plaintext on the public PDS — the dialogue self-corrects to this, re-deriving invariant **S5** (`thinking/social-layer.md`); (2) **no `app.bsky.feed.hideReply` method** — hidden replies = the `hiddenReplies` array on `app.bsky.feed.threadgate`; (3) **threadgate has no native "mutuals" rule** — only following/follower/list/mention. **Mutuals-only as a *feed/read-view* is absolutely possible** (compute follows∩followers client-side, or a feed generator that does it server-side — such feeds already exist in the wild) — that's the r=1 lens and it honors S5 (a filter, not confidentiality); the *only* gap is **network-enforced reply-gating** by mutuals, which you approximate with following+follower or a curated `listRule`; (4) **`site.standard.*` is real but a COMMUNITY lexicon** (Leaflet/pckt.blog/Offprint, already `ECOSYSTEM.md` §5b), not Bluesky-governed → carry a `$version`/renderer-registry hedge; (5) **Chromium-only / iOS-Safari gaps** (File System Access, `share_target`/`protocol_handlers`/`file_handlers`, Background/Periodic Sync, `navigator.connection`) → "continuous export" degrades to **OPFS-only, no live folder-mirroring on Safari/Firefox**, and iOS sync is foreground-only + Home-Screen-install + `storage.persist()` to dodge the 7-day eviction (**the 2026-07-27 batch sharpens this — see E71**: OPFS ≠ durability/ownership, the SW-continuous-export wiring is wrong twice on the pickers, so the honest shape is desktop-only folder-mirror + a *separate* mobile durability story = second-device / peer-re-admit / identity-export); (6) WebRTC E2EE-through-SFU needs **SFrame** (IETF draft); Privacy Pass = **RFC 9576/9577/9578** (not "9152/W3C"); WebAuthn PRF detect via `getClientCapabilities()` not `supportsPrf`; Web Monetization = WICG draft not W3C. CONFIRMED clean: getFeed/searchPosts/getPostThread(depth), quoteCount, getMutes/getBlocks, modlist+listitem, cdn.bsky.app variants, `public.api.bsky.app` unauth CORS, pinnedPost, Jetstream, **atproto OAuth granular per-lexicon scopes (shipped Aug 2025)**, Digital Credentials API + Chrome 141/Safari 26 (late-2025 timing correct), OPFS, `chrome.storage.sync` 100KB/8KB, Web Locks, MLS RFC 9420, OpenID4VP cross-device, WebGPU, ThumbHash/BlurHash, Yjs/Automerge/HLC. **Open calls (surface, don't resolve — PLAYBOOK §5):** **[decision]** private-comms / "private room to discuss a public article" is **OUT OF SCOPE for the forum** (user, 2026-07-27) — the private/E2EE plane is the **Drystone messaging plane** (`beta/drystone-spec/part-2-certifiable-design.md` §6 "the three planes + the encryption stack" / §6.1.2 MLS plane / §6.2.2 Layer-B MLS `PrivateMessage` / §7.6.3 / §10.2); a **separate project leverages Drystone**, the forum does not re-invent a sidecar; ties **T7** + the beta MLS work; **[decision]** the ethical WebRTC-relay + Privacy-Pass business model (owner/sustainability call — relates **D5**/E25); **[decision]** does a stateless RSS-CORS relay worker violate the "$0 / no-backend" framing?; **[decision]** publish the downvote lexicon (`app.forum.feed.dislike`) or keep dislikes local-only; **[explore]** r=2 graph-resolution at scale (two-tier cache / Bloom filter); **[already settled — no lift needed]** the "machines compute provenance, never utility" tenet is **not novel**: it is the established Drystone bedrock **"razor: compute provenance, never utility"** (`crystallized/principles.md` §"The deeper foundation (2026-06-20)"; also `beta/philosophy/epistemics-provenance-and-verification.md` and `commensurability-and-the-two-ledgers.md`) — this transcript *restates* it, and it is already carried up through the Croft ecosystem. | [explore] / [decision] | `seeds/transcripts/raw/bsky-reddit-forum-social-tree-pwa-2026-07-27.md` | **E39** (aggregator pond), E24/E42 (sovereign AppView club), E44 (account kernel / PWA sync), E48 (client-side search), `thinking/social-layer.md` (S5), `research/2026-07-27-social-tree-factcheck.md`, T7, D5, `ECOSYSTEM.md` §5b/§6/§5i, `COHESION.md` §59, beta MLS side-histories, **`plans/2026-07-27-read-first-forum-mvp.md` (MVP build plan — Pass 1–3 + Phase 0 done)** — **LAYERING (user 2026-07-27): "Social Tree" = the Croft app *backbone* (the shared `feed-core` substrate — normalize/window/sort/graph/radius/local-first, in the `croft-group` workspace, E5/E19), NOT the forum; the forum is the *first app on it*, homed at `forum.croft.ing` (`*.croft.ing` convention).** Canonical integrated framing (links every plan): `ROADMAP.md` § "The Social Tree backbone → apps". Phase-0 live probes: graph endpoints unauth → radius needs no OAuth; `searchPosts` 403 unauth → hashtag boards are login-gated (guest boards = feed/author/list) |
| E63 | **Croft's "small-world" positioning + tree-as-relational-UI design criteria (2026-07-27)** — the landscape framing: **Bluesky/atproto = "big world"** (global scale → *scoped-open* public data, never operator-confidential), **ActivityPub = "medium world"** (trust brokered at the instance-operator layer), and **no cohesive "small world" option that folds up into the others** — Croft's slot, **two-people-as-base-case working up**. Reframe "small" (reads pejorative) via the **life-world vs system-world** distinction: different scope, not lesser (open call: better non-pejorative framings). The **tree-as-relational-UI is the small-world made legible** (extends E62). Research reframes "trees never took off" from the *failure* literature: the hyperbolic-tree perf-null (Lamping/Rao/Pirolli CHI'95) + the **information-scent conditional** (Pirolli/Card/Van Der Wege, ACM TOCHI 2003 — high-scent → tree faster, low-scent → list wins); every historical test used worst-scent labels (paths/URLs/org-charts); **Croft's labels are faces+names = highest-scent → the failure condition is structurally absent, the winning condition permanent.** Corroborants: 2024 ego-network study (layered node-link preferred, matrix worst); **Path** (2010; ~50 cap *inspired by* Dunbar-150, not equal to it; "home inside Facebook's city"); Multitrees (Furnas & Zacks CHI'94 → shared people = one node/two edges). **10 design criteria:** no node without a face; launcher-not-analysis-tool; layered+quantized rings not force-directed; ring-one 5–9; **distance derived-not-declared** (kills Circles + intimacy-leaderboard); re-rooting + home key; **pads-as-leaves = the tree's justification**; shared = one-node-two-edges; search always a bypass; full-parity list view. **Sequencing (honors "prove one pond before three"): list+search+great-avatars ships first and tests the thesis; the tree earns phase two.** **Social Tree = the lens that ROOTS the Croft thinking (user 2026-07-27): the continuum social tree → social networking → social media = the small→med→big-world expansion, rooted personal graph as the base.** It is the **backbone that computes scope** (tree + radius r=0/1/2/∞), which **many apps consume**: the same "tree folks" set powers a private MLS chat (Drystone app) AND restricts forum content (`forum.croft.ing`'s radius filter). The forum is the *first app on the backbone*, not the backbone itself. Fact-check CONFIRMED (nuances: Pirolli co-author; Path-50 ≠ Dunbar-150). | [explore] / [decision] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T1,T3) | **E62**, `thinking/social-layer.md`, `NAMING.md`, `research/2026-07-27-social-tree-factcheck-2.md`, `COHESION.md` §60 |
| E64 | **did:webvh watchers / bounded-staleness / "federation of availability" — heartwood identity lineage (2026-07-27)** — verified vs did:webvh v1.0: `versionTime` is controller-asserted (strictly-increasing / equal-rejected / ~5-min non-futurity / `?versionTime=` index) but **load-bearing for validity + the version index, never provenance** — authenticity = **SCID + entry hashes + data-integrity proofs** (a uniformly-backdated log passes the checks). Durable results: **"under-verify, not misverify"** (integrity unconditional → all residual error is **omission** = true-but-old); "nothing newer exists" is unprovable non-interactively → **bounded staleness** (heartbeat re-sign) + a **per-use staleness budget** (casual = hours; credential-acceptance = one heartbeat; a stale head with a since-rotated key is the attack). **Federation unbundled:** split *fidelity* (is-it-true) from *availability* (can-I-get-it/is-it-current); self-certification moves fidelity into the artifact → watchers/relays/PLC/mirrors are **federation of availability only** (attack surface = omission/eclipse; adding a node costs **a disk, not trust** — atproto's wager vs ActivityPub). The **DHT axis** (did:web → watchers → PLC → pkarr/did:dht) = *who answers availability*, identical fidelity throughout; inescapable pinch = **bootstrap**. All CONFIRMED (nuance: "watchers reaching consensus" is the author's gloss — spec = distributed caches / malicious-controller detection; distinct from **witnesses** = approve-before-publish). | [explore] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T4) | **E30/E31** (Drystone), heartwood, `research/atproto-sovereign-appview-club.md`, `research/2026-07-27-social-tree-factcheck-2.md` |
| E65 | **Bluesky blob per-DID scoping ↔ the client-side-E2EE storage contract (2026-07-27)** — why the PDS stores actual blob bytes (seemingly precluding storage dedup): dedup wasn't precluded, it was **deliberately traded away** (Newbold, atproto disc. #1756) — PDS-global-CID → **repo-local per-DID** blobs for **lifecycle** (delete records/accounts) + **per-context moderation**; "very little dedup observed"; `(DID,CID)` tuples; `uploadBlob` takes bytes (no client-hash trust → Dropship-style injection N/A; Dropship = van der Laan Apr 2011; convergent/message-locked encryption is the only dedup-with-E2EE cheat and it's broken — Douceur/Bellare). **The insight:** per-DID scoping + full-bytes-on-upload = **the exact storage contract client-side E2EE needs** (opaque per-account byte strings; CID hashes ciphertext fine; **no cross-account equality oracle / instant-upload timing channel**; Newbold: "we'll probably need to do something different anyways for private/group-private data"). **The user's durable framing:** differing **data contracts for different material in one contract-agnostic engine** — the contract attaches at the **reference layer** (reference-time lexicon validation + per-repo state), not the byte layer; the moderation tension **relocates to the operator layer** (opaque bytes → only delete-by-CID / rate-limit / account-action — a known E2EE-hosting cost, not a design incoherence). Status: private-data side is **prospective** (all atproto blobs today = public contract; `getBlob` unauth). CONFIRMED (nuance: "comes for free" phrasing was the by-CID proposer's). | [explore] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T5) | **E24/E42** (sovereign AppView / private data), `research/atproto-private-data-architecture.md`, `research/2026-07-27-social-tree-factcheck-2.md` |
| E66 | **Co-op low-maintenance / high-utility service menu + the "one encrypted sync service" restructuring (2026-07-27)** — what a Croft-style co-op should host, ranked by the "as-little-durable-state-as-possible" stand + a **maintenance filter** (ops labor = abuse desk + reputation battles + per-user support + release churn → dodge all four). **Charter-material dividing line: *server can't read it, members can't publish to strangers.*** Shortlist: password-vault sync (Vaultwarden, ciphertext-only), feed-reader (**Miniflux**, single Go binary + Postgres), CalDAV/CardDAV, encrypted backup target, cron/uptime dead-man's-switch, ephemeral encrypted file-drop/pastebin (TTL = the maintenance strategy), push relay. **Restructuring idea: one generic per-member encrypted-KV sync service** underneath vault/prefs/bookmarks/feed-read-state/`account.croft.ing` kernel — build once, ciphertext-only enforced in one place. Second tier: stateless caching proxies (CORS fetcher → unfurl/oEmbed/resize/geocode), timestamp notary (RFC 3161 / OpenTimestamps), member DNS/subdomain (= p2p rendezvous), artifact pinning, newsletter-to-feed (highest-maintenance). **Labor sinks to avoid:** full mailboxes, photo libraries, Nextcloud-scale sync, Matrix homeservers, **SMTP relay + VPN** (pooled-reputation / exit-traffic abuse desk = "fund a role" not a utility). Flagship = **the static SPA catalog itself** + the **LTS promise** (the 10-year-domain reasoning institutionalized). Precedents CONFIRMED: Disroot, Riseup (share ~12h / **pads 60d**), CHATONS (Framasoft 2016), CryptPad. | [explore] / [decision] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T6) | **D5** (cooperative/sustainability), E24 (sovereign AppView club), account kernel, `ECOSYSTEM.md` §5j |
| E67 | **Lightning/Nostr "zaps" for small-scale community value transfer (2026-07-27, STRETCH DISCOVERY ONLY — explicitly NOT a near-term goal per user 2026-07-27; captured for completeness, custody+legal-gated, NOT-FINANCIAL-ADVICE)** — a **receipt protocol on top of plain Lightning** (NIP-57): kind 0 `lud16`, kind 9734 zap-request (signed, **unpublished** → LNURL callback), kind 9735 zap-receipt (published by the recipient's wallet server); LNURL advertises `allowsNostr`+`nostrPubkey`; the **description-hash invoice binds who/whom/for-what**; a receipt is a **provider attestation, not proof of payment**; the **transfer rail and announcement rail are independent** — plain-pay the address = money moves with **no public 9735** (quiet gift vs public signal, per payment). Two-fresh-phones onboarding: **Primal** (all-in-one; Strike custody + KYC-lite; instant `name@primal.net`; ~5 min; price = custody) vs **Damus/Amethyst + NWC** (NIP-47) + separate wallet (Wallet-of-Satoshi instant); iOS: Apple made Damus drop post-level zaps (June 2023). All CONFIRMED. **Open calls (surface, don't resolve):** **[decision]** custody + KYC + money-transmission/legal review (the user's); **[decision]** public receipts as feature (social-proof/anti-Sybil) vs leak for the small-world stance; **[explore]** fit vs the provenance-not-utility tenet + the cooperative model. **NOT a goal for now (user 2026-07-27) — parked as stretch discovery.** | [discovery-only / parked] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T9) | D5, `ECOSYSTEM.md` §5j, `research/2026-07-27-social-tree-factcheck-2.md` |
| E68 | **Flat-cost group auth — passkey/DPoP/DBSC legs + TOTP MFA + JWKS mass-invalidation (2026-07-27)** — for the auth-helper broker + account kernel. **Passkey can't be the DPoP key** (WebAuthn signs a fixed `authenticatorData‖SHA-256(clientDataJSON)` envelope, not chosen JWS bytes; per-gesture vs per-request). **The legs:** DPoP binds broker↔PDS; passkey binds user↔broker; **DBSC** binds session↔device (TPM/Secure-Enclave, ~5-min silent challenges *[impl-defined]*, **shipped GA Windows Chrome 146 Apr 2026**; can't stop a resident attacker — same containment-after-not-during ceiling). **Mass invalidation = already built, two flat kill switches:** (1) pull a key from your published **JWKS** → atproto OAuth MUST revoke sessions + reject refreshes (spec-mandated; rotate per-cohort/window to scope); (2) flush the broker's in-memory session store (defense in depth). **Flat-cost MFA = TOTP** (RFC 6238 / HOTP RFC 4226 / HMAC-SHA1; local-HMAC = zero marginal cost; SMS=per-msg, hosted=per-MAU, passkeys=flat+phishing-resistant-but-harder-recovery). **The catch:** a TOTP secret is long-lived + must persist → **reintroduces the at-rest-secret problem** the ephemeral-session design removed; in the SQLite→R2 path the **Litestream-cleartext finding (E60)** means a bucket leak = every second factor → the TOTP table earns **envelope encryption** (encrypted-at-rest blob, in-memory-only decrypt). CONFIRMED (nuances: DBSC-5-min impl-defined; TOTP resync-window = validator's choice). | [explore] / [decision] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T8) | `spike/auth-helper/`, `plans/2026-07-22-account-kernel-spike.md` (K1/KC1), **E60** (Litestream finding), `research/2026-07-27-social-tree-factcheck-2.md` |
| E69 | **arecipe responsive filter/search UI playbook (2026-07-27, 261-source research)** — the mobile-vs-desktop filter-toolbar problem (3 facets meals/cuisine/cook-time side-by-side on desktop, no room on mobile). **Pattern (one state model, two presentations; do NOT merge facets into one dropdown):** desktop = sticky horizontal per-facet disclosure dropdowns (live-update, count-in-trigger, applied chips + Clear-all); mobile = a single **"Filters (n)"** button → native **`<dialog>.showModal()` bottom sheet** (labelled accordion sections + sticky **"Show X results"** batch-apply); applied chips in a scroll-snap row. Behavior: live-desktop / batch-mobile; per-option counts (+ zero-results guard); **URL state** (URLSearchParams + pushState for filters / replaceState+debounce for typing / restore on popstate). Baseline-safe native: `<dialog>` + **container queries** (Baseline-widely) + ARIA disclosure over `<select multiple>`; `popover` = Baseline **Newly** (progressive-enhance only). A11y: WCAG 2.5.8 (24px) + 2.5.1 (swipe ≠ only close); 44px targets. Perf: **MiniSearch** (build-time index → static asset, ~300ms debounce, Web Worker only if it janks). Threshold: >5–7 facets → desktop left-sidebar + search-within-filter. Evidence CONFIRMED (Baymard 58%/78%, 28%/72%; nuance: popover Newly-not-Widely). | [doable] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T10) | arecipe cluster (E53–E59), `research/2026-07-27-social-tree-factcheck-2.md` |
| E70 | **Skylite — borrow PWA patterns from the good open-source Bluesky clients (2026-07-27, user note)** — the user's explicit "we may want to heavily borrow from the good PWA ones for skylite." Closest comparables to a PWA web client, both open source → read for AppView-interaction patterns: **TOKIMEKI** (browser, multi-column + multi-account, PWA-installable, **MIT**, self-hostable) and **SkyFeed** (custom feed builder). Wider landscape (corroboration-tier): deck.blue (TweetDeck-style multi-column, Aug 2023), Ouranos (lightweight web), DarkSky (Windows OSS), Skeets (iOS, paid Pro), Graysky (status unconfirmed), Sora (multi-network). All exist (CONFIRMED; TOKIMEKI PWA/self-host = corroboration-tier). | [explore] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T7) | Skylite, `ECOSYSTEM.md` §5j |
| E71 | **OPFS + File-System-Access "continuous export" — the honest correction (2026-07-27)** — sharpens **E62**'s continuous-export claim. **The parts are real, the wiring is not, and the failure lands on the platform Croft cares about (iOS).** OPFS is real+Baseline but **not durability** (not user-visible; same eviction/quota regime as IndexedDB; deleted on clear-site-storage → speed, not ownership/persistence). **`showDirectoryPicker()` is desktop-Chromium-only** (Firefox/Safari = OPFS only; Chrome-Android = OPFS only) → **folder-mirrored continuous export does not exist on iOS or Android.** The Service-Worker pipeline is **wrong twice** (pickers need a user gesture, SWs have none; background wake needs Background/Periodic Sync = Chromium-only). The "72h-export = dark pattern for ad-indexing" close is unsupported editorial (also serves GDPR Art. 20) — don't repeat it. **Honest shape:** OPFS for local perf (incl. iOS) but never sold as ownership; folder-mirrored continuous export as a **desktop-only** differentiator; a **separate mobile durability story** = second-device / peers-who-can-re-admit / explicit-identity-export. All CONFIRMED. | [backport] / [decision] | `seeds/transcripts/raw/croft-landscape-small-world-tree-ui-webvh-blob-coop-2026-07-27.md` (T2) | **E62** (continuous-export claim), E44 (PWA sync), `research/2026-07-27-social-tree-factcheck-2.md` |
| E72 | **Cross-origin content-fetch for a static Croft PWA — the extension model (2026-07-29)** — how does a zero-backend static PWA/SPA reach arbitrary **reader / API content** the browser's same-origin policy blocks? The transcript's first framing was a **user-run local proxy** (shell one-liner, PWA pings `localhost:PORT/health`, pairing token as a CSRF guard). **User decision 2026-07-29: go with the browser-extension model instead** — an extension grants the cross-origin fetch and messages content to the page; the "user-friendly workflow" becomes install-an-extension, not run-a-command. **Spike (greenlit, Playwright available here):** an MV3 extension proving a static PWA receives content its own `fetch()` is CORS-blocked on — assert direct fetch fails, then succeeds with the extension loaded (`--load-extension` persistent context). **`[UNVERIFIED]` to settle in the spike:** `http://localhost` mixed-content exemption from an HTTPS page, current Chrome **Private Network Access** preflight rules, Safari's stance. Honest alternatives it displaces: hosted proxy/Cloudflare Worker (infra+trust — the thing to avoid), atproto AppView (CORS-friendly for Bluesky reads only, not arbitrary RSS/HTML), Tauri/packaged webview (kills CORS but no longer a PWA). **SPIKE GREEN (hermetic, 2026-07-29)** → `discovery/alpha/experiments/extension-content-fetch/` (in `discovery/alpha/experiments/`; the standalone experiments repo is archived/read-only): MV3 extension + two localhost origins, 3/3 — direct PWA fetch of a no-CORS origin is blocked (TypeError), the extension bridge delivers it. **Finding: the extension model sidesteps the mixed-content/PNA surface the proxy model has** — the page never makes the cross-origin/insecure request, the SW does (host_permissions), and content returns in-process via `postMessage`; so PNA (public→localhost) really only bites the *rejected* proxy model, not this one. **Edges walked hermetically (v2):** secure-context (HTTPS page) mixed-content sidestep, per-host **consent allowlist** enforced in the extension, extension-presence **detection** (install-flow core). **`@live` real remote reader — GREEN:** the extension SW read 11442 real RSS bytes from `https://news.ycombinator.com/rss` (200) while the HTTPS page's own fetch was CORS-blocked — public→public, PNA does not apply. (Egress note: the general sandbox "browser egress blocked" caveat did **not** hold for a plain SW `GET` — the block is narrower/host-specific than assumed.) So the **croft-pwa "content-fetch" standards chapter is now UNBLOCKED** (gate passed) — draft it when wanted. **Parked (user 2026-07-29):** Firefox MV3 parity (Chromium-first "to get started"). | [validation] / [spike] — **GREEN incl. @live; chapter unblocked** | `seeds/transcripts/raw/croft-pwa-content-fetch-extension-graze-bluebird-2026-07-29.md` (Thread A) | **E62** (RSS-reader-as-discussion-seed), **E71** (platform reality), `croft-pwa` "content-fetch" chapter (unblocked), `discovery/alpha/experiments/extension-content-fetch/`, COHESION §61 |
| E73 | **`graze.ing` ACQUIRED — forum homed there; "Graze by Croft" working name (2026-07-29; acquired 2026-07-30)** — the forum (`2026-07-27-read-first-forum-mvp`, E62) gets working name **Graze by Croft**, path **`/f/`**, commons framing, pastoral theme in edge copy only. **`graze.ing` is acquired (user, 2026-07-30) and is now the domain**, superseding the earlier `forum.croft.ing` placeholder. The **Graze behavior-scale mock** (persona-switch PWA, no backend — the mock tier) is deployed there — repo `CroftCommunity/graze`; method `thinking/behavior-scale/`, spec `thinking/app/build-specs/graze-persona-switch-spec.md`. **[decision] the name is a working name, not finally settled** — surface, don't lock. **UPDATE 2026-07-31: name SUPERSEDED → "Amble by Croft" (working name, clearance CONTESTED — see A20); `graze.ing` disposition now OPEN (retire / redirect-to-amble / hold) since the name moved; the built `graze` mock is unrenamed until Amble clears.** | acquisition DONE; [decision] name→Amble (A20) + graze.ing disposition | `seeds/transcripts/raw/croft-pwa-content-fetch-extension-graze-bluebird-2026-07-29.md` (Thread C); `seeds/transcripts/raw/stellin-graze-behavior-scale-sessions-2026-07.md` | `NAMING.md` (Forum-layer naming), `plans/2026-07-27-read-first-forum-mvp.md`, **E62**, **E80**, COHESION §61, §63 |
| E74 | **RUN-BLUEBIRD — the in-repo Skylite→Bluebird transformation (local, pending) (2026-07-29)** — the org/domain rename is **done** (`github.com/CroftCommunity/bluebird`, `https://bluebird.croft.ing/` live), so the local `skylite/` working copy's git remote is **stale** and the in-repo run has not landed. **RUN-BLUEBIRD (belongs in the bluebird repo, not discovery):** red tests first incl. a **dist-wide "no `skylite` string"** test + a **banned-vocabulary lint**; then **lexicon renames `ing.croft.skylite.*`→`ing.croft.bluebird.*` with a trust-tier enum (green/blue/black = trust distance, not maturity) baked in while schemas are still record-free**; pages (Lodge/Trail Map/locker/patrol/ski-school), manifest, tokens, assets, copy, **oauth client-metadata URL** (the silent-breaker — atproto OAuth fetches it from the app's own domain), gate, summary. Reads new domain/repo from `CNAME`+remote and **halts if either still says `skylite`**; Skylite-era `CONCEPT.md`/seeds stay as **frozen seed** (README one-line bridge, no history rewrite). **[decision] "Bluebird" lockup clearance** (Amex Bluebird card, Bluebird.js) `[UNVERIFIED]` — run a live TM/handle pass before any store filing; brand as **"Bluebird for Bluesky"**. | [doable] / [decision] | `seeds/transcripts/raw/croft-pwa-content-fetch-extension-graze-bluebird-2026-07-29.md` (Thread D) | `NAMING.md` (Skylite→Bluebird), **E70** (was "Skylite" = now Bluebird), `plans/croft-stack/README.md` (threads `bluebird`), COHESION §61 |
| E75 | **arecipe passthrough cache — backlog, NOT critical path (2026-07-29)** — a demand-driven, TTL'd passthrough cache under `cache.arecipe.app` that accelerates arecipe's friends-scoped PDS reads (authed paths use the Phase-7 broker). **Explicitly not critical path:** Phase 8 ships the **bluebird-cache track first** (public reads, no broker dependency); arecipe-cache is picked up later, once wanted. Never load-bearing (the pad works with the cache off). Fork-4 in the Phase-8 scope is resolved to this ordering. | [build] / [hold→Phase 8] | `plans/croft-stack/08-cache-server.md` (Scope, 2026-07-29 decision) | Phase 8 (`08-cache-server.md`), `plans/croft-stack/README.md` roadmap |
| E76 | **relay network-namespace isolation — reusable pattern (2026-07-30) — DONE (LIVE 2026-07-30)** — isolate external-facing services (relay first) in their **own network namespace** so a compromised service can't reach host localhost/other services (e.g. the broker on `127.0.0.1:8001`). Ingress via nftables **DNAT** of the service's public ports (no inbound SNAT — preserve client source); egress masqueraded + **denied back into the host/RFC1918**. Ship as a parameterised `netns_service` Ansible role (ns/subnet/ports) → reusable across the estate + eases moving a service to its own box. **Phase 0 PASSED (2026-07-30):** a relay behind DNAT still yields 5/5 **direct** connections AND is isolated from the host (broker unreachable) — B2 not forced; build the role (Phases 1-2). Session: `croft-stack/sessions/2026-07-30-netns-phase0.md`. Full version: a network namespace (Phase 1) PLUS deepening the unit sandbox with the other namespaces (PID/mount/user/IPC/UTS/cgroup) + syscall filtering (Phase 1b) — as isolated from the host as possible. Owner declined the cheap systemd-only version. | [build] / [decision] | `plans/croft-stack/netns-isolation-plan.md` | `relay-mode-b-plan.md` (B2 fallback), `06-iroh-relay.md` |
| E77 | **light relay load/perf test (2026-07-30)** — now that direct handoff is proven (5/5), get a feel for the load the relay puts on the box under concurrency: throw **tens then hundreds** of connections at it (spawned `relay-loadtest generate` processes and/or `--count`/`--concurrency`), and watch what it costs the **relay box itself** — the `iroh-relay.service` cgroup metrics (`memory.current`/`memory.peak`, `cpu.stat`, `pids`) via the telemetry poller, against its governed envelope (512M / 200% CPU / 512 tasks). Informs the "when to split the relay to its own box" (mode-B **B2**) call. Light/exploratory, not a benchmark suite. | [doable] | (new) | `relay-mode-b-plan.md`, telemetry (`croft-stack/telemetry/`), `06-iroh-relay.md` |

| E78 | **harden the other deployed services toward the relay 1.7 baseline (2026-07-30) — DONE + REBOOT-PROVEN (2026-07-30)** — all units hardened via one reusable least-privilege baseline (contract `croft-stack/HARDENING-BASELINE.md` + test-gate library `tests/hardening.bash` + `identity`/`hardening` roles + tenant template): caddy 8.8→1.9, broker 4.7→1.5, telemetry 4.4→1.2, canary 4.7→1.5, relay 1.7→1.5; certs C1 (admin API→unix socket) + C2 (relay key→tmpfs); identities remapped to a clean 640–644 block; broker OAuth-scope bug fixed. Survived a cold reboot with everything hardened + `changed=0`. Plan `plans/croft-stack/service-hardening-plan.md`. ORIGINAL: — `systemd-analyze security`: caddy **8.8 EXPOSED** (all caps + NoNewPrivileges=no; holds every LE key), broker/canary 4.7, telemetry 4.4. Apply the relay hardening set per unit with documented exceptions (caddy keeps CAP_NET_BIND_SERVICE; telemetry keeps cross-unit cgroup visibility / no cgroup-ns; tenants get it via the template as an overridable default; broker also a netns candidate). Factor one reusable hardened baseline. Apply incrementally + verify each service stays active+functional. | [build] | `croft-stack/reviews/2026-07-30-service-hardening-review.md` | `netns-isolation-plan.md`, relay `hardening.conf` |
| E79 | **model workloads as portable "tenants" (2026-07-30) — PARKED: kept Model A (self-contained stackable Ansible roles composing the shared identity/hardening roles); the tenant/generator consolidation is not worth the added refactor rigidity at two bespoke services. render.py is a build-time generator (no daemon on the box either way). Revisit if the workload count grows. Decision record + ready design: `croft-stack/plans/tenant-platform.md`** — today the relay and broker are bespoke Ansible roles; `canary` is the only thing shaped as a tenant (manifest → `render.sh` → units). Direction: express every long-lived workload as a **tenant manifest** (ports, resource envelope, cert-needs, hardening carve-outs, netns) that the platform stamps out — so all workloads inherit the hardened template (see Phase 4), are governed/observed uniformly, and become **relocatable to another box / horizontally scalable** because a tenant depends on its cert being *delivered to a known path* (the copy/tmpfs model, E-hardening) rather than being co-located with Caddy's store. Reinforces why cert **copy** beats a shared-group into Caddy's store (portable, least-privilege, decoupled from Caddy internals). Bigger refactor than the hardening rollout; do after. Raised by owner during the hardening work. | [design] | `plans/croft-stack/service-hardening-plan.md` (cert model), `config-templates/service.service.tmpl` (tenant template) | E78, `06-iroh-relay.md` |
| E80 | **Behavior-scale methodology + the two built mocks filed & wired in (2026-07-30; renamed 2026-07-31)** — the reusable **behavior-scale / persona-switch** method (was "the behavioral twin"; renamed 2026-07-31 since "twin" implied exactly two deployments — it is one model across a **mock → small → medium → large** backend continuum) was an unindexed island; now filed and cross-linked: `thinking/behavior-scale/` (methodology + persona-switch prototype + README/registry + the seed→spec→build pipeline), build specs and competitive research for both builds, and the raw session seed. Two **mocks** are **built and deployed**: **Stellin** (`stellin.app`, professional-networking, was "Meridian") and **Graze** (`graze.ing`, forum). **[decision] — surface, don't resolve:** the **Graze build's assumed large-tier backend is Next.js + Postgres** (from the aggregation build spec), but the corpus's active forum plan (**E62**, `plans/2026-07-27-read-first-forum-mvp.md`) chose a **read-first lens over the public Bluesky AppView** — reconcile the two before Graze grows an `api` substrate. Stellin's mock↔large-tier seam is cleaner (large tier = the atproto "Stellin by Croft" AppView, **A18**/RUN-14/15). | [backport] + [decision] | `seeds/transcripts/raw/stellin-graze-behavior-scale-sessions-2026-07.md` | `thinking/behavior-scale/README.md`, **E62**, **A18**, COHESION §63, ECOSYSTEM §5c-3, `NAMING.md` (Stellin + Forum-layer) |
| E81 | **Forum pad: subjective-consensus reputation + bare-link discovery design (2026-07-31)** — the forum pad's reputation model (graph-weighted **subjective consensus**: piggyback-`like` upvotes + a contained `com.graze.feed.downvote`, Depth-1/2/3 + tenure weighting, Network/Tree two-score UI, subjective Hot/Rising/Controversial, Ozone-labeler moderation, graph-amplified awards, organic shadowban) + the **bare-link "wish someone would recommend to me" view** (multi-fidelity link rendering incl. on-device AI summary; generated-image flagged fraught) + the **two-mode local-first** architecture (Global via Jetstream / Subjective via client-side `getFollows` — **no mandatory AppView**) + better-than-Reddit features (BYO-mod labelers, algorithmic sliders, zero-cut tipping, community forking, offline IndexedDB archives) + titleless-post bridging. Distilled → `thinking/app/forum-subjective-consensus-and-reputation.md`. **New data point on the E80↔E62 fork:** subjective consensus needs no AppView → lowers the fork stakes (ships with no backend), does not resolve the large tier. **[verify]** atproto/lexicon mechanics + atwork.place/Sifa (Gemini-sourced). **[open]** do not mint `com.graze.*` NSIDs into records until the Amble name holds (A20). | [explore] / [decision] | `seeds/transcripts/raw/graze-forum-subjective-consensus-gemini-2026-07.md`; `seeds/transcripts/raw/amble-naming-coop-metering-dialogue-2026-07-31.md` | `thinking/app/forum-subjective-consensus-and-reputation.md`, **E62**, **E80**, COHESION §63/§66, ECOSYSTEM §5b, A20 |
| E82 | **Cooperative metered-storage lane — kickoff + v0 sketch (2026-07-31)** — the lane for turning the proven item-storage protocol into a **usable, dogfoodable** co-op storage service (the **D5** charging layer). v0 definition-of-done: "point a real object store at it → metered bytes with bilateral signed receipts → a monthly balance-forward statement I verify from my own manifest." Scope = the **E0–E9 core**; SEAMs to close in order: (1) network boundary, (2) real S3-compatible blobstore, (3) real CIDv1/DAG-CBOR; E10 erasure + E11–E14 financing/funder-diligence **deferred**. Experiment is **green-real** (standalone runs 81/81 E0–E11). **Committed (user 2026-07-31): build as an experimental product** — a **Rust** custom-PDS-like metered store with an **S3-compatible interface**, extrapolating from **`rsky-pds`** (Blacksky, Rust + (per-actor) SQLite + S3 — Phase 0 D1: the repo README's "Postgres" is stale vs `main`'s `rusqlite`; ECOSYSTEM §5e — "closest to Croft's stack") + Cocoon/official-PDS for reference; blob backend Garage/SeaweedFS/R2 (not MinIO — archived). Deployed via **croft-stack** on the VPS as a service to test against. **It doubles as the substrate for the MLS history-convergence server** (content-blind meer, Phase 10 `10-drystone-layer.md`) — one store under both PDS-hosting and convergence. **Open design Q:** S3-put/get interface vs the atproto PDS API surface (`com.atproto.sync.*`/`getBlob`) — resolve by studying rsky-pds + the atproto PDS spec (do not guess). **Next:** prior-art/design-grounding pass → phase-plan → TDD build → croft-stack deploy. **[2026-07-31: BUILD UNDERWAY — Phases 0–6 SHIPPED in `experiments/item-store/`; the **E0–E9 ledger core is complete** (identity, items+manifest, receipts+ledger, statements+SQLite, audit+dial, seal/tombstone+grace); 88 tests, mutation gate green across E0–E9. Next: Phase 7 S3-metering boundary + `main.rs` (network surface, incl. the E87 graceful-shutdown seam) → Phase 8 atproto blob API → Phase 9 deploy (gated on the name A21 / repo-IP / VPS-kernel-probe decisions). See the build plan Outcome Summary.]** **[2026-08-03: Phase 7 (S3 metered boundary) SHIPPED — axum PUT/GET metered end-to-end + pluggable BlobStore (mem/FS) + manifest surface + runnable binary (graceful-shutdown/socket-activation seams); 106 tests, mutation gate 58/0, live-binary curl validated. Then GRADUATED to its own repo `CroftCommunity/CISS` — Croft Item Storage Server (fresh import, crate renamed `item-store`→`ciss`, discovery copy removed). Resolves A21 (name → CISS) + repo/IP home (→ CroftCommunity/CISS). Phases 8 (atproto blob API) + 9 (croft-stack VPS deploy) continue in CISS; only remaining Phase-9 input = the VPS-kernel probe (E84). Ship commit `fb2a1c7`.]** **[2026-08-03: Phase 8 (atproto PDS blob surface) SHIPPED in CISS (`1bf3c0d`) — `uploadBlob`/`getBlob`/`listBlobs` as a thin layer over the *same* metered byte-path (atproto meters identically to S3); the CID `SEAM:` is CLOSED via a new `cidv1.rs` (real CIDv1 `raw`+sha-256 `$link`, in-corpus `ipld-core` path); mock-bearer auth `SEAM:` on uploadBlob (getBlob/listBlobs public). 115 tests + `wiring_pds_blob` gate, scoped mutation gate 0 real survivors, live-probe confirmed the D2 shape + shared metering (2 receipts up==down). Only Phase 9 (VPS deploy) remains.]** **[2026-08-03: Phase 9 (croft-stack VPS deploy) SHIPPED — v0 is LIVE at `https://ciss.croft.ing`. VPS-kernel probe resolved (Debian 13 trixie / kernel 6.12 / systemd 257 / cgroup v2 / ext4 → E84 reflink N/A on ext4, E87 lean graceful-drain; socket-activation/SO_REUSEPORT deferred). CISS binary made contract-compliant (`CISS@0b65108`; `--data-dir`/`--listen`, `/healthz`, provider seed persisted in canonical SQLite; 122 tests + a contract wiring gate) + published as release v0.1.0; croft-stack gained `services/ciss.toml` + a `tenants`-role real-binary fetch (`croft-stack@4565382`). Converge idempotent; live HTTPS metered round-trip + atproto uploadBlob verified; governed unit (`systemd-analyze security` 1.5) + cgroup-within-envelope + no key leakage. Caddy request-retry (`lb_try_duration`) wired same day (`croft-stack@5ce5e4e`; 120/120×200 across a live restart). Deferred: R2 backup activation (SET ASIDE for now, user 2026-08-03), netns, socket-activation blue-green (E87 stretch). v0 dogfoodable — the E82 definition-of-done is met in production.]** **Open:** legal-review + funder-diligence transparency commitment deferred (D5); R2 backup mirror (set aside). | [build] / [decision] | `plans/2026-07-31-coop-storage-metered-hosting-lane.md`; `seeds/transcripts/raw/amble-naming-coop-metering-dialogue-2026-07-31.md` | `thinking/cooperative-social-union-model.md`, `experiments/item-storage-protocol{,-standalone}`, ECOSYSTEM §5e, `research/atproto-private-data-architecture.md`, `plans/croft-stack/10-drystone-layer.md`, **E25**/**D5**, A21, COHESION §65 |
| E83 | **Per-DID compute/mem/io observability for the metered store — "watch in place" (2026-07-31)** — off the D7 cgroup finding: leverage the **cgroup v2 primitive** (not the admin poller) to attribute per-repo/per-DID **CPU/memory/IO** load. **Operational, NOT billing** — billing stays transfer (postage) + storage (rent); compute is for (a) capacity/scaling signals, (b) per-user load attribution, (c) an **anti-gamesmanship cross-dimension** (catch abuse that games transfer — tiny transfers driving heavy compute — via a second orthogonal observation). **Mechanism:** `Delegate=yes` on the unit → service creates a per-DID child cgroup for a *heavy* op (CAR export, MST rebuild, audit sampling, seal ceremony), reads `cpu.stat`/`memory.peak`/`io.stat` deltas **directly** — the service is its own consumer of the primitive, **independent of** the croft-stack telemetry poller (no cross-repo dep; poller-glob change only for an optional admin dashboard). Cheap ops (blob PUT/GET) never scoped (spawn cost > compute). CPU exact (cumulative counter), memory sampled/high-water. Machine-measured ⇒ **Unilateral** provenance (moot, not charged). **v0 = only the Phase-7 op-dispatch `SEAM:`** (forward-compat); capability itself post-v0. **[open]** whether compute ever becomes a billed axis — no for now (economic model / **D5**, user's call). **Data model (decided 2026-07-31):** separate record linked by receipt/op id, co-located in the per-user SQLite, **unsigned to start**, separately rolled-up/purged; **never** embedded in the signed receipt (non-deterministic + Unilateral would break its co-attestation); if signed later → provider-signed **Unilateral** at op close-out, a hook that could underpin delegating/authorizing burden-heavy ops. | [build] / [decision] | `plans/2026-07-31-1-plan-coop-metered-storage-service.md` (Phase 7 seam; Open Questions; Review Log Decision 2026-07-31) | **E82**, **D5**, `plans/croft-stack/telemetry-client-plan.md`, `plans/croft-stack/03-governance-telemetry.md` |
| E84 | **Kernel-performance tier for the metered store — compliant-kernel primitives (2026-07-31)** — the metering logic is irreducibly **L7/userspace** (parse HTTP/S3 → CID over bytes → signed receipt; unlike LVS/IPVS/XDP which are L3/L4), but the **transport + storage** paths can be offloaded to the kernel. Hot path is ~90% I/O + one SHA-256 pass. Edges, by stage: **accept** → `SO_REUSEPORT` (kernel connection LB, the LVS-spirit primitive); **socket I/O** → `io_uring` (batched async, registered buffers, `IORING_OP_SEND_ZC`; Rust `monoio`/`glommio`/`tokio-uring`) — the biggest edge; **GET/serve** → `sendfile`/`splice` + page cache (`mmap`) = zero-copy reads + free hot-blob cache; **PUT/first write** → no zero-copy (must hash) → single-pass SHA-256 with **SHA-NI/ARMv8 crypto ext** (`sha2` asm / `ring`); **temp→permanent move + content-addr dedup** → `copy_file_range`/`FICLONE` **reflink** (near-instant CoW on XFS/Btrfs; same-CID blobs dedup for free); **"when to step in"** → **PSI** per-cgroup pressure (ties E83); **per-DID net bytes** → `cgroup/skb` eBPF (anti-gamesmanship cross-check, not billing). **Tension:** `io_uring` vs the hardening baseline (`systemd-analyze security` ~1.2–1.5, seccomp; hardened kernels set `kernel.io_uring_disabled=2`) — hardening likely wins v0; io_uring is a measured upgrade vs the seccomp profile. **v0:** stay simple/portable (`tokio` epoll + std I/O + SHA-NI sha-256); adopt only `copy_file_range`/reflink early (cheap, no hardening cost). The `BlobStore` + Phase-7 op-dispatch seams are the attach points. **[probe, Phase 9]** confirm the VPS kernel version — io_uring/PSI/kTLS/copy_file_range need 5.x+. | [build] / [explore] | `plans/2026-07-31-1-plan-coop-metered-storage-service.md` (Phase 7 blobstore/dispatch seams; Open Questions; Review Log Decision 2026-07-31) | **E82**, **E83**, `plans/croft-stack/service-hardening-plan.md` |
| E85 | **Object grouping / index structure — flat commitment vs MST vs RBSR (2026-07-31)** — how stored entries are grouped/indexed and what mutation/proof/sync guarantees we need. **Three distinct set-structures, don't conflate:** (1) **blobs** (our v0: uploadBlob/getBlob/listBlobs) are a **flat `(DID,CID)` set** — *not* in the MST even in real atproto (blobs are referenced by records, stored separately, enumerated by listBlobs); no tree needed. (2) the **billing manifest (E2)** is a **flat sorted Merkle root** — already deterministic over a set (sort ⇒ replay-order-independent) + tamper/omission detection; adequate for v0 (periodic co-signed snapshot, not per-op mutated). (3) atproto **repo records** are an **MST** keyed by `collection/rkey` — deterministic shape from the key set + **bounded add/remove** (O(log n) nodes) ⇒ compact CAR diffs, firehose sync, O(log n) inclusion proofs; a **spec requirement** if we ever host records (full-repo scope, out of v0). (4) the **convergence consumer** (Phase 10) uses **RBSR** (range-based set reconciliation over envelope hashes, iroh-docs) — per FACTCHECK explicitly **not** an MST (Gemini conflated them). **Thinking:** v0 flat is correct (blobs + manifest); adopt an MST-like content-addressed **object index** only if/when (a) we host atproto records, (b) the flat manifest's O(n) recompute/re-sign hurts at scale, or (c) we need compact audit-inclusion / sync-diff proofs. Keep manifest/index addressing **pluggable** (same seam as D4 / `HS OC-2` CID-vs-envelope-hash); RBSR stays its own primitive behind that seam. | [explore] / [decision] | `plans/2026-07-31-1-plan-coop-metered-storage-service.md` (Phase 2 manifest; Open Questions) | **E82**, **D4**, `experiments/hist-atproto-spike` (`HS OC-2`), `seeds/transcripts/raw/atproto-atmospheric-web-iroh-mobile-FACTCHECK.md` (RBSR≠MST) |
| E86 | **Test-hardening for the metered store — negatives + mutation + e2e-abuse (2026-07-31)** — beyond per-phase wiring tests, this crypto/metering engine earns three layers: (1) **paired rejection tests** — every verify path gets a should-fail-for-sure negative (apply now, per phase); (2) **mutation testing** (`cargo-mutants`) + **property tests** (`proptest`: canonical round-trip, Merkle root order-independence over random sets, sign/verify round-trips) — periodic, operationalises the Pass-3 mutation-resistance gate (a surviving mutant = a test that doesn't constrain behaviour); (3) an **end-to-end abuse suite** that drives the whole engine and tries to break it (forge/replay receipts, inflate manifest, tamper at rest across the boundary, walkaway, double-count audit, malformed input) — real version at **Phase 7** (HTTP engine), in-process E0–E9 abuse suite before that. Not a v0 blocker; strengthens validation throughout. **[2026-07-31: E0–E3 mutation gate GREEN — `cargo-mutants` 117 mutants → 103 caught / 0 missed / 13 unviable / 1 benign timeout; paired-rejection negatives added (killed 24 initial survivors incl. a security-relevant `leaf_hash`-ignores-input); trivial field accessors excluded via `.cargo/mutants.toml`. Layer-3 e2e abuse suite still pending at Phase 7. **E0–E4 full-crate re-run (post-Phase-4): 172 mutants → 145 caught / 1 missed / 24 unviable / 2 timeouts; the 1 "missed" is an equivalent mutant (`rent_cents` `*`↔`/` with numerator=1), excluded — zero real survivors. **E0–E6 (post-Phase-5): 241 mutants → 11 survivors, all in `rng::next_f64` bit-mixing (a PRNG mixer is constrained only by determinism+uniformity); killed with a golden-vector parity test pinning mulberry32 to the TS oracle (scoped re-run 31/31 caught) — zero real survivors. **Phase-6 (E7–E9, scoped seal+grace): 37 → 32 caught / 0 missed / 5 unviable; six survivors (always-true predicates + untested Debug) killed by paired negatives + a Debug-redaction test + removing an always-true `nets_to_zero`. Zero real survivors across the whole E0–E9 core.**]** | [build] | `plans/2026-07-31-1-plan-coop-metered-storage-service.md` (Open Questions; Phase 7 validation) | **E82** |
| E87 | **Zero-downtime upgrade + TLS termination for the metered store — spike (2026-07-31)** — raised by the user: upgrade the running store with **zero downtime from the start**, as an **experiment spike** (explore options + measure), not a blind pick. **TLS/reverse-proxy = Caddy** (matches croft-stack's existing Caddy + the official Bluesky PDS installer default on 80/443); forward `Host` (for `/.well-known/atproto-did`) + websocket `Upgrade`/`Connection` (for `subscribeRepos`); Caddy request-retry masks a restart. **Reference — how Bluesky upgrades:** container image swap (watchtower pulls the new tag → recreates the container) — a **brief restart, not connection-preserving**; state survives on a bind-mounted dir (per-actor SQLite + FS blobs); the firehose resumes from a **client-held cursor** (protocol-level handoff). **Favorable for us:** durable state is all on disk (per-user SQLite WAL + FS blobs) + firehose resume is a client cursor ⇒ little bespoke in-memory handoff state (the listening socket is the thing to preserve); SQLite WAL permits a **safe brief two-process overlap** (readers concurrent, one writer serialized) ⇒ blue-green feasible. **Options to measure:** (1) stop-start (`systemctl restart` — the Bluesky-equivalent floor); (2) graceful drain (SIGTERM → stop-accept → drain → WAL-checkpoint → exit) + Caddy retry; (3) **systemd socket activation** (`.socket` `ListenStream=`; inherit fd via `sd_listen_fds` → kernel accept-backlog buffers the swap → near-zero refused); (4) **`SO_REUSEPORT` blue-green** (old+new bind same port; start-new/health-gate/drain-old; WAL overlap safe) — true zero-downtime incl. in-flight, most orchestration. **Lean:** (3)+(2)+Caddy-retry; (4) as stretch. **v0 = only the Phase-7 `main.rs` graceful-shutdown + fd-inheritance seam** (build-from-the-start hook; capability + chosen strategy at Phase 9). **Deliverable:** measured comparison (dropped conns / refused requests / firehose gap per option) → picks the Phase-9 strategy; may graduate to its own harness (like relay-loadtest). Not a v0 blocker. | [explore] / [build] | `plans/2026-07-31-1-plan-coop-metered-storage-service.md` (Phase 7 `main.rs` seam; Phase 9 Caddy+upgrade; Open Questions; Review Log Decision 2026-07-31) | **E82**, `plans/croft-stack/service-hardening-plan.md`, `config-templates/service.service.tmpl` (tenant template; **E79**) |
| E88 | **arecipe cookbook durability — copy recipes into the cookbook owner's PDS vs reference the author's (2026-08-06)** — raised by the user while planning arecipe's bundled-snapshot work. **Current state (verified 2026-08-06):** a cookbook is a **pure reference set**. `app.arecipe.cookFollow` stores only `{ subject: <followed cook's DID>, createdAt }` (`docs/LEXICONS.md`; `src/social/cook-follows-pds.ts`); recipes are read live from each author's repo (`src/recipes/read.ts` `createRecipeReader`, `src/pages/browse.ts` `entriesByDid`). **Nothing copies a recipe into the follower's repo** — `write.ts` create/putRecord is for authoring your own. **Failure modes this exposes:** an author deleting a recipe, deactivating, migrating PDS, or going offline silently removes content from *your* cookbook — and the app **actively prunes** it (`revalidate.ts` 4xx → `'gone'` → `browse.ts` `onGone` drops the cook from the live view). Your curation survives; the content does not. **The tension:** copying buys durability + offline completeness, but (a) forks provenance, (b) duplicates storage across every follower, (c) raises licence questions — the Wikibooks corpus is CC BY-SA 4.0 and **O3 chose fork-on-edit**, so a copy is a derivative that must carry lineage (`RUN-WIKIBOOKS-CORPUS-SUMMARY.md`), and (d) needs a staleness story if the author later edits. **Shape questions:** copy-on-add vs copy-on-demand ("pin"/"save" gesture); whether a copy is a *distinct* record (e.g. `app.arecipe.savedRecipe` = snapshot + source ref + retrieved-at, keeping "mine" and "saved" separable) or a full `exchange.recipe.recipe` in your own repo (durable but muddies authorship in Browse/Cookbook); and whether the existing `attribution`/`sourceUrl`/`sourcePermalink` provenance fields already carry enough lineage to make a copy honest. **Note the parallel:** the 2026-08-06 bundled-snapshot plan already copies the corpus **into the client** for exactly this durability/offline instinct — same motivation, different layer (device cache vs the user's own PDS). Deciding this may subsume part of that. | [explore] / [decision] | arecipe planning session 2026-08-06; `arecipe/docs/LEXICONS.md` (`app.arecipe.cookFollow`) | `arecipe/plans/2026-08-06-2-plan-snapshot-sharding-and-image-cache.md`, `arecipe/plans/2026-08-06-3-plan-incremental-revalidation.md`, `arecipe/RUN-WIKIBOOKS-CORPUS-SUMMARY.md` (O3 fork-on-edit) |
| E89 | **Cost ceilings + the prepaid meter as an equity instrument (2026-08-07)** — extends the proven **meter-the-boundary** charging mechanism (§65/E82) with the *cap* dimension. A **synchronous** meter (receipt-per-byte-crossing) makes a **hard spending ceiling** a ledger-comparison-before-serving — impossible under incumbent batch billing. Dial-pattern setting: co-signed "spend stops at X," **throttle/defer past X, never mint debt**; rent can't spike (`manifest × tariff`) so the cap governs postage; a **client cost twin** enforces it pre-flight (two-sided). The prepaid meter's dual history (UK 2022–23 forced-prepayment scandal `[UNVERIFIED]`) → a **5-rule checklist**, each a ledger fact not provider policy: tariff parity · mode-change-only-with-customer-signature · ceiling-never-mints-debt · **exit unconditionally cap-exempt** ("they can never keep your furniture") · throttle-count as a governance metric. **Open work (bug-vs-design-gap per `CISS/CLAUDE.md`):** (a) **[DONE 2026-08-07]** exit-exempt self-egress written as **invariant B6** in `CISS/docs/SECURITY-POSTURE.md` (+ checklist row; client twin honors it — see E90 M5); (b) **[DONE 2026-08-09]** co-signed ceiling/mode **BUILT** as the self-assertion dials (CISS PRs #29–#33, released v0.7.0): I5-style dial records (customer-signed, provider-acked, seq-CAS'd), the bilateral-receipt seam **unstubbed** (countersign endpoint; doubly-signed facts verify offline under published keys), 402 refuse-with-quote, drawdown mode, B6 + D-series POSTURE invariants; ADR 0004 Accepted-as-amended; rent-in-ceiling awaits the statement-scheduler SEAM; (c) **[decision/governance]** tariff parity + throttle-count-to-governance = co-op bylaw, not server code; (d) **[backport]** a cloud-metering-opacity `research/` note to ground the `[UNVERIFIED]` incumbent facts (McCarthy/Waldrop wording, EU Data Act dates, Ofgem/British Gas figures). Distilled → `thinking/cost-ceilings-and-the-prepaid-meter.md`; principles → `crystallized/principles.md` (2 new Tier-3 bullets). | [doable] / [build] / [decision] | `seeds/transcripts/raw/ciss-cost-ceilings-and-prepaid-meter-equity-2026-08-07.md` | `thinking/cost-ceilings-and-the-prepaid-meter.md`, `thinking/cooperative-social-union-model.md` (grace ledger), **E82**, **E90**, **D5**, **E25**, COHESION §67, `CISS/docs/SECURITY-POSTURE.md` |
| E90 | **CISS file-sync client — cloud-storage sync over CISS (+ iroh), files-as-items (2026-08-07)** — a new build lane: a sync client that treats every file as CISS items, judicious with bandwidth (deltas + metadata-only), authing via a **bsky app-token** (the existing `did:` service-auth JWT path) *[reconciled at M1: the **write path** structurally requires the **namespace key** — the keep-set `Manifest` must be signed by it (`id:` session, `derive_id(key)==DID`), so backup/restore auth is the `id:` session; the app-token/`did:`-JWT path is the read-side surface]*, optimizing **client host storage**. **Grounded against the real CISS API (v0.5.6, mapped this session):** 2 MiB per-object cap ⇒ **chunking is mandatory** (FastCDC, avg ~256KB–1MB; chunk-level resume replaces byte-range resume — CISS has no Range/resumable/multipart/HEAD/304); `du`/`listBlobs` = a **have/want set for free** (no new endpoint); the signed keep-set **`Manifest` already carries monotonic-seq CAS (I5)** ⇒ **safe multi-device commit with no server change**; content-addr + read-reverify (`Tampered`) = the integrity anchor; receipts are **Unilateral-only** (`Bilateral`→`501`) and there is **no pre-flight estimate endpoint** (client prices `du`×tariff). **Client invents one artifact:** a filesystem manifest (`path → {mode,mtime,chunk cids,size}`) serialized as its own content-addressed blob; the CISS `Manifest` keep-set = ∪ chunk cids + fs-root cid, `seq++` = atomic tree commit. **Both transports as options** via a `BlobTransport` trait + a **sha256(CISS)/blake3(iroh) dual-name** bridge in the manifest (one hashing pass); iroh's real prize = **peer-fetch → less server egress → less metered cost** (ties E89). **DECISIONS (user 2026-08-07):** CISS stays **indifferent** ⇒ **encryption is a client-only layer, zero server impact** (fork collapsed, plaintext v1); sync is **bidirectional + conflict-copy**, built on shared primitives with a **one-way mirror as the first shippable milestone**; **timestamps are an assertion, never authoritative between nodes** (correctness rides on content hashes). **Proposed phases:** P1 chunk+content-address core (client-only) · P2 transport trait + CISS/HTTP data plane + one-way mirror · P3 client storage optimizer (CA cache, pin/evict, online-only placeholders) · P4 bidirectional reconcile + conflict-copy · P5 iroh transport · P6 cost twin + ceiling. **RESOLVED 2026-08-07 (design settled; plan written → `CISS/docs/plans/2026-08-07-file-sync-client.md`):** (a) **rename detection** = client reconciler over the per-client base, **exact chunk-set match only** (partial match deemed not useful — zero-byte if missed, mis-attribution risk), optional polish in M3. (b) **HEAD = a per-device signed `Frontier` folded locally**, never an asserted/server-minted pointer — grounded in a corpus research pass (own-device pool exchanges self-verifying frontiers, folds-or-forks; the meer is a content-blind helper; set-reconciliation is the shared primitive but NOT canonical merge; CISS is already spec'd as one-store-two-consumers). Model: `DeviceHead` blobs + a `Frontier{heads}` in the seq-CAS'd manifest slot; commit is **non-lossy** (each device writes only its own `heads` slot); current tree = a **client-side deterministic fold** (per-path 3-way merge vs greatest-common base, conflict-copy, content-address tiebreak). **Identity decision:** shared account key now (no server write-auth change), graduate to MLS/lineage per-device keys later as an option. **Only server change in the whole ladder = one additive owner-signed `Manifest.heads` field at M3** (under existing I5 seq-CAS; server stays blind). Milestones M1 (backup+restore) → M2 (bounded local footprint) → M3 (2-device converge + conflict-copy) → M4 (iroh peer-fetch) → M5 (cost twin). New crate `ciss-sync`. **This is the first concrete/tractable (dataplane) instance of the corpus's parked frontier machinery** (`LocateLatest`/completeness-ahead). Cross-lineage Croft-group sync remains the parked L3 frontier (out of scope). **Phase plan lives in `CISS/docs/plans/2026-08-07-file-sync-client.md` (not discovery).** **M1–M5 ALL SHIPPED 2026-08-07 (ladder complete)** — `ciss-sync` crate + `ciss-ctl sync backup|restore|evict|hydrate|status|converge` merged to CISS main (PRs #14/#16/#17; CI gate added #15). M2: bounded local footprint (budgeted chunk cache, placeholders, the logical-tree no-data-loss invariant). M3: **the frontier is real** — the one server change (owner-signed `Manifest.heads` bound into the signing preimage, back-compat structural), self-verifying `DeviceHead`s, slot-discipline non-lossy commit (race-proven), and the pure clock-free fold: two devices converge to the same fs-manifest cid with conflicts preserved as `<path>.conflict-<device>` on both. M1 detail: (PR #14): byte-identical backup/restore proven end-to-end (have/want dedup via `du`, keep-set under I5, verified restore with cold discovery via the manifest's `kind` self-tag, tamper fail-closed); execution plan + a Drystone/Croft foundations review in `CISS/docs/plans/2026-08-07-file-sync-m1-chunk-and-backup.md`. M4: **the iroh prize is collected** — new `ciss-iroh` crate: `IrohPeer` (BlobTransport keyed by sha-256, blobs stored/fetched by blake3 with Bao verified streaming; sha-256 re-verified on receipt, so a poisoned alias degrades, never corrupts) + `PeerFirst` (peer-preferred reads, origin writes/keep-set — a restore served every chunk from a peer, origin egress = 1 get) + `MeshPeer` (gossip frontier on `TopicId = derive(account key)` per `thinking/multi-device.md` §10, newest-signed-counter-wins per device slot) — `ciss-ctl sync p2p share|converge` converged two real devices to the same fs-manifest cid **with the server offline**, conflict-copy intact. Server change: none. M5: **the cost twin is live** — `sync price` quotes pre-flight via the server's own linked `pricing::postage_cents` (the twin IS the meter's function); `sync ceiling` defers an over-ceiling sync WHOLE before any byte moves (never partial, never bills; 0¢-marginal syncs never blocked); the client spend ledger prices cents over TOTAL bytes exactly as a statement would. **Exit-exempt landed as CISS POSTURE invariant B6** (E89 lane (a) done): no billing state may ever gate self-egress of one's own manifest+blobs; regression-tested (restore under an exhausted ceiling). The ladder is complete; remaining follow-ons live in the CISS plans (persistent iroh store, relay posture, per-profile ceilings, co-signed ceiling via the E82 bilateral seam). | [build] / [decision] | live design session 2026-08-07; `seeds/transcripts/raw/ciss-cost-ceilings-and-prepaid-meter-equity-2026-08-07.md`; CISS API map (v0.5.6) | **E82**, **E85** (flat manifest vs MST — sync uses the flat keep-set + a client fs-manifest), **E89** (cost twin), `thinking/realtime-media-over-iroh.md`, `thinking/multi-device.md`, `CISS/docs/plans/` (phase plan TBD), **D5** |
| E91 | **The meer as a custodian queue over CISS — MLS store-and-forward re-scoped (2026-08-07)** — the meer (Part 2 §6.6.2 D-meer) stops being a service with its own store and becomes a **thin gateway over CISS custodian chains**. **Prior-art finding: there is no MLS delivery service to copy, and the absence is structural** — RFC 9750 deliberately leaves the DS unspecified, so every shipped DS is bespoke to its product and *is* the server half of a messenger (identity, group creation, KeyPackage hosting, Welcome distribution, policy, ordering); OpenMLS ships a labelled PoC, `phnx-im/air` is AGPL-3.0, MIMI standardized the role by reintroducing a per-room ordering hub. Strip ordering + group-state validation and a **mailbox** is what remains — which nobody ships standalone, and which is why the shim is small. **Unblocked now** (unlike the history store, which Phase 10 gates on the fold): the per-author index is sealed, so the meer cannot order and needs zero fold knowledge. **Design (settled in session):** CISS gains **custodian chain mode** — a chain's **kind** (`queue`/`file-sync`/`history-convergence`) is declared in the **owner's manifest slot declaration, never in a head** (a head-borne kind would be self-authorizing), **fixed at genesis**, bound into the signed preimage as `heads` already is, with **per-kind preimage prefixes** so cross-kind signature reuse is structurally impossible; **`queue` is the only custodially-writable kind and unknown kinds fail closed** (the load-bearing test is exhaustive over the kind enum); the slot carries **kind + custodian + an owner-declared ceiling**; **custodial writes go to a separate custodian-signed record**, so B1/B3 on the owner's manifest keep their proofs and revocation is an ordinary owner write inheriting B3 anti-rollback. **Per-DID queues default** — this *retires* rather than satisfies the state-portability anti-entrenchment guard ("the mail never left home"; portability is a promise a helper gives your data back, ownership is not having handed it over) — with **meer-owned pooled queues as a custody dial** (sibling to the confidentiality dial) for bootstrap, idle-heavy scale, and anyone wanting the meer to know less about them. **Drain authorizes on CISS account identity, never MLS identity** (group credentials to a blind store would leak group membership; entitlement is enforced by the seal, not the drain gate). **No cursor** — the have/want diff over content-addressed blobs is the position (§6.8.1: "the cursor and the gap-detector are the same object"); **deliver-once is correct because §6.6.5's device-Group is the fan-out**, with race-mode when no device group is present. Retention = **14 days as a ceiling, not a floor** ("14 days or until drained"), with a per-recipient **purge watermark** so expiry is a loud, nameable gap (§2.2 no-invisible-loss). **Meter ≠ bill:** meter transit and at-rest independently (different evidence — custodian attestation vs the owner's own signature); keeping transit unbilled also defuses the "spam costs the victim money" consequence of mail landing in the recipient's namespace, leaving the ceiling to defend. The **transit meter IS the offline-data fraction** — the number `meer-superpeer-design.md` names as the only thing that sizes a meer fleet and lists as unmeasured — so it closes an open edge as a byproduct; **meter retention must be bounded**, or the mail purges at 14 days while a per-account profile never expires. **Generalisation:** the reusable thing is a **typed-chain substrate** in CISS with **thin gateway services** above it (meer, mirror-history, file, chat) — protocol adapters stay outside because CISS speaks HTTP and a QUIC listener inside the store that holds everyone's data widens its exposure (**verified: the CISS server binds no iroh listener today — `ciss-iroh` is depended on only by `ciss-cli`**). Custodian mode is what lets any such gateway meter honestly. **Next: Phase 0 spike — SPECIFIED, not yet run** (`experiments/meer-queue/SPIKE-SPEC.md`): M1 offline drain+decrypt, M2 byte-identical forwarding incl. the negative re-framing arm, plus 8 shape-learning scenarios; runs against **plain CISS as it exists** with 3 registered stand-ins, and **extends `iroh/crates/mls-welcome-over-iroh`** (real OpenMLS 0.8.1) so no new MLS client is needed. **S8 is the measurement that may reshape the design:** MLS object sizes vs CISS's `MAX_OBJECT_BYTES` = 2 MiB (refused on put *and* get; **not arbitrary** — it came from the 2026-08-03 security review's memory-exhaustion finding, so raising it re-opens a closed vulnerability unless streaming replaces buffering). `GroupInfo` with the embedded ratchet tree is **O(N)**, so `Welcome` is the object at risk; three options (out-of-scope for v0 / transparent chunking via `ciss-sync` FastCDC / **ship the tree out of band, which Part 2 §6.9.1 already mandates for the broadcast tier**) and the measurement picks. **Open:** who mints the custodian grant (per-persona lean, per §5.4, vs Group-collective); meter retention policy; custody-dial default per Group. **[2026-08-10: Phase-0 spike findings that shape the lane — `experiments/meer-queue/TEST-LOG.md`.] (a) Make the meer its OWN CRATE in lane Phase 2.** The spike asserts "the meer cannot name what it carries" with a source-level **lint**, because `cargo tree` resolves crates and the meer is a module in a crate that also holds the MLS layer — the plan's stated mechanism does not exist. A separate crate with no openmls in its manifest makes it compiler-enforced, and the gateway is a separate process there anyway. **(b) The client must preserve TYPED MLS errors.** `SecretReuseError` (benign duplicate) and `TooDistantInThePast` (unrecoverable loss) are one variant apart; the spike's `MlsError::Process(String)` flattens them — fine for a result log, wrong for a client, since string-matching an upstream message breaks silently on a library bump. **(c) The custody dial has an unnamed cost dimension:** dedup is per-namespace (`blocks/{did}/{cid}`), so one message to 50 recipients is 1 stored object under a meer-owned pool and **50 under per-DID queues, the stated default** — per-DID buys ownership and legible accounting and costs at-rest storage linear in fan-out. **OWNER READ (2026-08-11): this is a strong argument — possibly the strongest — for the meer owning its OWN chain rather than writing into per-user chains.** It compounds with the owner's earlier judgement that \"it never left home\" does not carry weight as user-facing value: if the ownership story is not doing work and per-DID multiplies storage by fan-out, the **pooled mode stops being the bootstrap fallback and becomes the default candidate**. Not yet decided; recorded as the leading position. **(d) Duplicate handling: keep both** — an in-memory delivered-hash cache as the fast path, `SecretReuseError` as its repair path, so the cache needs no persistence and the processed-but-not-acked crash window closes for free.**]** | [build] / [decision] | live design session 2026-08-07; `seeds/transcripts/raw/meer-store-and-forward-custodian-queue-2026-08-07.md` | `thinking/meer-as-custodian-queue.md`, `plans/2026-08-07-meer-lane.md`, `experiments/meer-queue/SPIKE-SPEC.md`, `thinking/meer-superpeer-design.md` (storage half superseded), `plans/croft-stack/10-drystone-layer.md`, **E82**/**E90** (CISS), Part 2 §5.4/§6.6.2/§6.6.5/§6.8.1/§6.8.5/§6.9.1, `beta/cairn/mls-and-mimi.md` |
| E92 | **[Rung-A follow-up] Does the §6.6.5 device-group fan-out actually rescue deliver-once? (2026-08-10, from meer-queue spike S4)** — the meer spike **falsified naive deliver-once at Rung A**: with no device group, a second enrolled device drains its own queue and gets **zero** messages (`experiments/meer-queue/TEST-LOG.md` S4). The hypothesis doc asserts deliver-once is *correct, not a compromise*, **because** §6.6.5 guarantees every enrolled device eventually sees what any one device received. **That compensating mechanism is not built, so it was NOT tested** — standing something in for it would substitute for the exact mechanism the claim is about (the methodology's canonical forbidden move), so the arm is reasoned in prose only and the question stays open in the residue. **Also measured, and it reframes the dial:** racing across 2 enrolled devices costs **1 deposit, 1 stored object, 2 queue entries** — i.e. the fallback is nearly free at the meer, so the §6.6.5 dependency is buying less than the doc's framing implies. The real trade is *racing leaks device count to the meer and holds N entries until all ack* versus *deliver-once depends on a fan-out that does not yet exist*. **OWNER DECISION (2026-08-11): this is a dial, not a defect** — deliver-to-one versus deliver-to-all is a setting negotiated between the provider and the person, and the spike's starvation result is what the without-device-group end of that dial looks like. History convergence is simply not on in this environment. **Status: PARKED with a retirement condition**, not open work. **MATERIALLY NARROWED 2026-08-12 — the starvation is likely an ARTIFACT of the same divergence.** Per-recipient queues are what starve a second device, and they exist only because the spike's meer is **addressed** (`meer-spike-addressed-deposit`). In the Part 2 §5.4 fabric model the meer holds the **topic's** traffic and every member drains the same content-addressed set by have/want digest diff: **there is no per-device queue, so there is nothing to starve and no prune-on-ack race.** Deliver-once versus race stops being a dial and becomes a non-question. **Retire by:** confirming that at Rung A against a fabric-shaped meer. If confirmed, this item closes without ever needing the §6.6.5 fan-out it was blocked on — which would also retire the dependency the hypothesis doc leaned on. | [confirm] | `experiments/meer-queue/TEST-LOG.md` (S4), `experiments/meer-queue/tests/s4_multi_device.rs` | `thinking/meer-as-custodian-queue.md` §"Cursors and delivery", `beta/drystone-spec/part-2-certifiable-design.md` §6.6.5, **E91**, `plans/2026-08-07-2-plan-meer-queue-spike.md` |
| E93 | **[normative] Part 2 §6.6.2 — two corrections the meer spike earned (2026-08-10)** — both are **rationale/《language》 errors, not requirement errors**; the requirements stand. **(1) The byte-identical `MUST` names the wrong hazard.** §6.6.2 attributes it to *re-framing* (decode/re-encode). Measured at Rung A: a re-frame is **byte-identical** (TLS-codec serialization is canonical — 189 B application message and 490 B commit, same sha256 both ways), so it cannot be the hazard. The real hazard is **re-sealing**, which requires a group key a blind forwarder does not hold. The `MUST` is also *stronger* than stated: openmls gates both re-frame conversions behind `test-utils` with an explicit "MUST NOT be made available outside of tests", so a production build cannot re-frame at all. **(2) "MLS applies a duplicate idempotently" is false for application messages.** That reasoning holds for *commits* (a duplicate no longer matches current state and is dropped) and does not carry: openmls 0.8.1 **errors** on a duplicate application message (`SecretReuseError` — the per-message secret is destroyed after first use for forward secrecy). Consequence for implementers: content-hash dedup must happen **before** MLS processing, or a normal delivery race manufactures an error that looks like tampering. **Retire by:** editing §6.6.2's rationale text (spec edits are not a spike's call — flagged, not applied). | [decision] | `experiments/meer-queue/TEST-LOG.md` (M2, S3, S3b) | `beta/drystone-spec/part-2-certifiable-design.md` §6.6.2, **E91**, `thinking/meer-as-custodian-queue.md` |
| E94 | **Should a deposit be unlinkable? The meer learns the communication graph (2026-08-10)** — the spike enumerated the meer's leak profile from measurement rather than assumption (`experiments/meer-queue/TEST-LOG.md` → "Standing finding"). Most of it is **forced by the function**: that mail exists, deposit time, object size, fan-out width, drain time. Content, group membership and ordering are never visible. **One row is neither forced nor documented: `(depositor → recipients)` edges.** A deposit names its recipients and the depositor is identified by the authenticated connection, so the meer holds the communication graph. This sits oddly beside the design's care on the *drain* side — the doc reasons at length that drain must use account identity and never MLS identity because group credentials would reveal *which groups you are in*, while deposit hands over *who talks to whom*, a superset of that concern for most threat models. Deposit currently gets one clause ("gated at the meer's admission policy") about authorization, not about what admission reveals. **Unlike the rest of the table it is not forced:** the meer must know who to queue **for**, not who **deposited** — that identity arrives free off the QUIC connection rather than from necessity. **Open costs if made unlinkable:** admission policy, abuse control and the metering/attestation story all currently lean on knowing the depositor. **Owner's framing (2026-08-10):** nothing can queue for an absent recipient without learning the shape of the task; the obligation is an **enumerated, subject-readable** profile (the metadata-transparency guard, `meer-superpeer-design.md` item 6), not a minimised one. **OWNER DECISION (2026-08-11): note the posture; unlinkable deposit is not scoped now.** Folded into E96's documentation pass. **MATERIALLY NARROWED 2026-08-12 — the graph leak is an ARTIFACT, not a property of the meer.** Re-reading Part 2 §5.4 showed the spike tested a topology the spec does not describe: the spike's meer is **addressed** (publish carries an explicit recipient set), while §5.4 has it *\"participate in a Group's delivery scope the way any swarm node does, in the gossip fabric, carrying and seeing the sealed envelope and its routing metadata as it passes.\"* **In the fabric model there is no recipient set** — the meer sees envelopes on a topic and never learns who they are for — so `(depositor → recipients)` edges are not disclosed at all. The row that made this item interesting (*\"unlike the rest of the table it is not forced by the function\"*) was correct, and the reason is stronger than filed: it is not forced because **the design already avoids it**, and the spike introduced it. Registered as `meer-spike-addressed-deposit`. **What survives:** the rest of the enumerated profile (existence, timing, size, fan-out width, drain time) and E96's cleartext `group_id` linkability, which is a swarm-participant property and genuinely forced. **Retire by:** folding the corrected profile into Part 2 §6.4 alongside E96 — and stating that deposit-side linkage is an artifact to be designed out, not a cost to be accepted. | [decision] / [explore] | `experiments/meer-queue/TEST-LOG.md` (Standing finding; S2, S4) | `beta/drystone-spec/part-2-certifiable-design.md` §6.4, `thinking/meer-superpeer-design.md` item 6, **E91**, E92 |
| E95 | **CISS has no object DELETE — the meer's retention window cannot be honoured at the storage layer (2026-08-10, meer-queue spike S5)** — measured at Rung A: after the meer sweeps an expired queue entry, the queue serves nothing **and CISS still holds the object**. CISS's object plane is `PUT`/`GET` only (routes: objects, manifest, assertion, receipt-countersign, meter, du — **no delete on any**), so the meer has no mechanism to remove what it stored. **Three consequences the design does not currently state:** (1) the 14-day promise is about **serving**, not holding — \"here is what is gone\" is false as written, \"we stopped serving it\" is true, and the user-facing wording must not claim a deletion the substrate cannot perform; (2) **storage grows monotonically**, so the queue-mode deployment profile (*\"high write rate / tiny objects / 14-day churn / no backup\"*) rests on a churn that does not happen and its sizing follows from a false premise; (3) **ciphertext outlives its window** — sealed and unreadable, but a harvest-now-decrypt-later surface and a durable metadata surface (sizes, timing, counts, plus the graph in E94) that retention was supposed to bound. **Owner's call (2026-08-10): build it — keeping every queued message forever will not work, and the PDS-compat surface implies deletion anyway.** Confirmed: CISS ships `uploadBlob`/`getBlob`/`listBlobs` and **no record surface**, so nothing can ever *become* unreferenced — in real atproto, blobs are collected once the records referencing them are deleted. **This is a lifecycle gap in the PDS story generally, not only a meer problem.** **Design A — manifest-driven reclamation (no DELETE endpoint, and none needed).** The manifest is *already* the owner's signed statement of what should exist: it binds every leaf (B1) and carries a monotonic `seq` refused on rollback (B3, enforced at `src/server.rs:1299`). An owner signing manifest `N+1` **omitting** a leaf is an authenticated, replay-proof \"I no longer claim this\"; the server reclaims objects no manifest references. **Zero new authenticated destructive endpoint**, matches atproto's unreferenced-blob GC, and billing already stops when the leaf leaves the manifest, so the meter needs no change. **Limitation:** it needs the owner to sign, i.e. to be online — which the meer case cannot assume, since the owner being asleep is the entire premise. **Design B — owner-declared retention on the `queue` chain kind (the meer's answer).** The planned slot declaration already carries *kind + custodian + an owner-declared ceiling*; add a **declared retention window**. The owner pre-authorises expiry **once, at enrolment** — which is exactly the \"one line in your inventory\" story — and the server enforces it with no per-delete signature and no owner presence. The policy lives in the owner's signed, genesis-fixed slot declaration, so it **inherits B3 anti-rollback for free**, the same way revocation does (clear the custodian field, bump `seq`). The custodian never sets it, so it cannot be abused by the party it constrains. **This also sharpens what the `queue` kind IS:** not merely \"the kind custodians may write to\" but **the kind whose contents expire by owner-declared policy** — a real axis separating it from `file-sync` (indefinite retention), rather than an authorization footnote. **OWNER DECISION (2026-08-11): scope BOTH A and B as real CISS work** — they are different jobs and both are needed. A is the atproto-compat half (a path by which anything ever becomes *unreferenced*); B is the meer's retention promise. Neither substitutes for the other. **Plan: `CISS/docs/plans/2026-08-11-object-lifecycle.md`.** **Key implementation finding (2026-08-11): B does not need the meer lane's typed-chain substrate.** CISS already has a typed, owner-signed, `seq`-monotonic **assertion** surface (`PUT /{did}/assertion/{kind}[/{subkey}]`) whose `kind_fold` refuses unknown kinds — *\"kinds are code, not data\"* (`src/server.rs:1440`), with `POLICY_KIND`, `CEILING_DIAL_KIND` and `PERIOD_DIAL_KIND` already riding it. A **retention dial** is one more kind on that surface, following `CeilingDialBody`'s pattern exactly. That is what makes B bounded, and it means B is **not** blocked on meer-lane Phase 1. **Retire by:** implementing A (the general PDS lifecycle fix) and B (the meer's retention), in that order — A is independently owed by the PDS-compat claim, and B is what makes the 14-day promise true. | [decision] / [build] | `experiments/meer-queue/TEST-LOG.md` (S5), `experiments/meer-queue/tests/s5_expiry_watermark.rs` | `CISS/src/server.rs` (routes), `thinking/meer-as-custodian-queue.md` §\"Cursors and delivery\", **E91**, E94, `plans/2026-08-07-meer-lane.md` |
| E96 | **A carrier can link messages by conversation — `group_id` is cleartext, and the design's stated mitigation does not cover it (2026-08-10, meer-queue spike S7)** — measured at Rung A: `group_id`, `epoch` and `content_type` sit **beside** the ciphertext in the MLS `PrivateMessage` framing (`openmls-0.8.1/src/framing/private_message.rs:33-38`) and are readable through the public API **with no key**. This is RFC 9420 framing, not a CISS or meer choice. **The design's reasoning is defeated by the payload it protects:** `thinking/meer-as-custodian-queue.md` argues drain must authorize on account identity and never MLS identity because *\"presenting group credentials to a blind store would tell it which groups you are in — metadata the blindness exists to prevent\"* — but the meer already holds `group_id` on every message it stores. The drain gate is still correct (it avoids **adding** a credential-based disclosure) and must stop being presented as preventing group-linkability. **What a meer can do with no key and no broken seal:** partition its store by conversation; count per-conversation traffic per recipient; watch epoch advancement, which signals commits and therefore membership/key changes. Also measured: a non-member's refusal is a **routing** check (*\"Message group ID differs from the group's group ID\"*) — the library declines before attempting decryption, so confidentiality holds but that refusal does not exercise it. **The corpus already owns the fix and has not applied it here:** the history-convergence store is specified with **nested double-sealing**; the meer is not. An outer seal over the MLS message closes this at the cost the history store already pays. **OWNER DECISION (2026-08-11): note the metadata posture; do NOT build nested sealing now.** **And the scope is wider than filed:** the owner observes that a meer sits in the iroh swarm in the real deployment, so it sees traffic **whether or not anyone enrolled with it** — which means cleartext `group_id` linkability is a property of **any swarm participant**, not a meer-specific exposure. Filing it as a meer finding understated it. **Retire by:** correcting §6.4's leak profile and the drain-gate rationale to match the measurement, scoped to swarm participants generally. Nested sealing stays available and unscheduled. | [decision] | `experiments/meer-queue/TEST-LOG.md` (S7 + Standing finding), `experiments/meer-queue/tests/s7_carol_carries.rs` | `beta/drystone-spec/part-2-certifiable-design.md` §6.4, `thinking/meer-as-custodian-queue.md` §Reasoning, **E91**, E93, E94 |
| E97 | **Meer enrollment has an unbuilt third party: how do senders learn where to deposit? (2026-08-10, meer-queue spike S1, Rung C)** — the design frames enrollment as Bob ↔ meer, *\"a one-line, revocable permission to add to one pigeonhole on your own plot\"*. That is accurate for the **grant** and is not the whole enrollment: **Alice has to know which meer to deposit at, and nothing in the design says how she finds out.** The spike could not see this because the test handed Alice the address directly. It is a **discovery** problem with at least three candidate homes — a service endpoint in Bob's DID document (atproto-native, the natural fit), a KeyPackage extension inside MLS, or out of band — each with different revocation and privacy properties. **Consequence for revocation, which is currently one-sided:** clearing the custodian field and bumping `seq` correctly stops the **meer writing**, but does nothing about **senders still depositing**, because they hold the stale announcement. A revoked meer keeps receiving deposits it cannot act on and Alice's messages go nowhere until she re-resolves; the window is however long announcement propagation takes, which is unspecified because announcement is unspecified. S6 tested re-pointing and **could not** test revocation propagation — there is no announcement channel to propagate through. **Also unnamed at enrollment:** the device-group presence assertion S4 requires (a starving device and an idle device are indistinguishable at the meer, so presence cannot be inferred and must be asserted here), and the fact that the meer needs **two** identifiers — a CISS account for the custodian field and an iroh `EndpointId` to be reachable — bound together by nothing. **OWNER CORRECTION (2026-08-11): the revocation half was overstated.** A person can unilaterally stop using a meer — you do not need one to be in the group, so declining it is your call and needs no cooperation. What remains real is (a) the **announcement** channel, so senders know where to deposit, and (b) the observation that in the real deployment a meer sits in the **iroh swarm** and therefore sees traffic regardless of enrollment, which is E96's exposure rather than a revocation failure. **RESOLVED 2026-08-12 (owner). There is no announcement channel, because there is no discovery problem — the spike invented one.** Part 2 §5.4 already specifies the shape: a meer's presence in a scope is a **fabric-level fact** (it is in the gossip swarm like any node, carrying and seeing sealed envelopes as they pass), and it is **not revocable by a single persona**; what *is* per-persona is whether to **use and rely on** it — §5.9's exit right, needing *\"no threshold and no governance round.\"* Senders never look a meer up because they never address it. **The one genuinely new layer** is a **Group-level nomination**: the Group declares which meer it relies on, carried as an MLS **`GroupContextExtensions`** entry (RFC 9420 proposal type 0x0007, so it changes by **commit** and is therefore governed, not ambient). Owner's framing: *the Group's shared meeting point — not one person dictating, because exit is preserved for each member individually.* **Semantics to pin, or someone will build the wrong thing: the nomination is an ENDORSEMENT, not a PERMISSION.** The meer sees the fabric regardless; the extension says *\"this is the one we have agreed to rely on\"*, never *\"this one may see our mail.\"* **Costs, named:** a group-context change is a commit — an epoch change with O(N) cost — so a Group cannot cheaply flip meers; and a Group with a flaky meer pays a governance round to move. **Also resolved:** the revocation half of this item was overstated (a persona simply stops calling), and the two-identifier concern is moot in a fabric model where the meer is not addressed. **Retire by:** specifying the group-context nomination in Part 2 §5.4 and §6.6.2, and building it in meer-lane Phase 2. | [decision] | `experiments/meer-queue/TEST-LOG.md` (S1) | `thinking/meer-as-custodian-queue.md` §Approach, **E91**, E92, E95 |

| E98 | **Croft's own content-licensing posture — architecturally open is not legally reusable (2026-08-09)** — surfaced by the BookHive comparison, and it lands directly on Croft because Croft has the same shape. Two properties get conflated under "open data": **openly licensed** (anyone may legally reuse) and **openly available** (anyone can technically get it all). BookHive/atproto have the second and not the first — every review is a public schema'd record in the author's own repo, streamed on the firehose, **architecturally unencloseable**, while the review *text* carries no CC0/CC-BY grant and sits in implied-license fog. Open Library has the first and not the second in the sense that matters (CC0 1.0 required on all contributions + bulk JSON dumps, but "Community Reviews" are **aspect tags, not prose**). **Nobody has both**, and no open project holds anything like Goodreads' 15-year corpus (Amazon owns it; the public API closed years ago). **The open item for us:** Croft's records are heading for the same place — publicly readable by construction, legally unspecified. Decide the posture deliberately rather than inheriting the fog: does a Croft lexicon or ToS attach a reuse grant to user-authored content, and if so which (CC0 waives moral/attribution interests some users will want; CC-BY keeps attribution but complicates aggregation)? Note this **interacts with the sovereignty thesis** — "your data is yours" and "the corpus is reusable by anyone" are not the same promise and can conflict. Also note BookHive's honest structural caveat, which we would inherit: its `hiveId` canonical-book layer is **its own**, so a successor app gets the reviews free but must **re-resolve what each one points at** — the same trap awaits any Croft-side canonical-entity id. | [decision] | `seeds/transcripts/raw/bookhive-open-data-and-iroh-endpoint-lookup-2026-08-09.md` (Part A3) | **E62** (aggregator pond), `ECOSYSTEM.md` §5k, `crystallized/principles.md` (compute provenance, never utility), COHESION §70 |
| E99 | **Emoji Wars into `fun`: the reshape, the tier, and what is still unscoped (2026-08-09 → 08-11)** — the owner decided Emoji Wars (`CroftCommunity/levelforge`, live at `levelforge.croft.ing`) **belongs in `fun`**, that the player and builder are inseparable **as a product** though separable **as code**, and that the shape it needs is **frontstage / backstage / lobby**. That framing names the defect precisely: `src/main.ts` is **3,721 lines — 39% of a 9,642-line codebase** — with **14 inline `level.meta.mode ===` branches and no `GameMode` interface**, and **no lobby module at all**, because the distinction was never in the code. **SETTLED:** the tier question — a deterministic engine was **measured, found to work, and declined** (Rapier spike `eb70cff`; native==wasm bit-identical, and the control shows only *native* moves, so `enhanced-determinism`'s `libm_force` drags native onto the libm wasm already had). Ratified as **`fun/docs/BUILDING-GAMES.md` §11 Tier-3 — engine-backed originals** (commit `10df5fe`): ours like Tier-1, non-verifiable like Tier-2, neither of them; **share inputs, never outcomes**; **the data/sim line must be visible in the directory structure**, with the data side keeping full Tier-1 discipline and the sim side pinned by **tolerance probes** recorded from the engine's own run-to-run variance. **STILL OPEN:** (a) the **two other canvas/media use cases** are unnamed, which blocks scoping the library extraction — recommendation on record is *extract a library now, defer the dialect until a second use case shows what is common*; (b) frontstage/backstage/lobby has **no plan**; (c) the **physical move into `fun` is unscoped** — Emoji Wars has no Rust core, so `fun`'s per-game layout (`crates/<game>-core`, `crates/<game>-wasm`) does not fit it and §11 covers standards, not file layout; (d) the `slingshot→sling` / `drive→bounce` rename + schema migration (v0.8) is scheduled by §11's existence but unplanned; (e) **the code does not yet know Tier-3 exists** — `src/contract.ts` is a union of `tier?: 1` and `tier: 2`, and `wrapped-banner.ts` returns `null` unless `tier === 2`, so shipping a Tier-3 game without widening both would put an **unmarked non-verifiable game on the shelf**. | [build] / [decision] | `seeds/transcripts/raw/emojiwars-levelforge-three-modes-and-reshape-2026-08-09.md`; `seeds/emojiwars-unpacked/` | `fun/docs/BUILDING-GAMES.md` §11, `fun/plans/2026-08-09-emojiwars-physics-parity-harness.md` (SUPERSEDED), `experiments/rapier-determinism/RESULT.md`, **E46**, COHESION §71 |

| E100 | **croftcall Android client crashes immediately on launch — ROOTED 2026-08-12** *(was: cause unknown, two candidates)* — `CroftCommunity/connect`'s Android half does not survive start-up. **CAUSE ESTABLISHED 2026-08-12 by reproducing it on an emulator and reading the log.** `java.lang.UnsatisfiedLinkError: dlopen failed: library "libiroh_ffi.so" not found` at `MainViewModel.<init>(MainViewModel.kt:22)` → activity cannot start → process dies. **Root cause: `computer.iroh:iroh:1.0.0` is a DESKTOP JVM artifact, not an Android one.** Its jar ships `darwin-aarch64/libiroh_ffi.dylib`, `linux-aarch64/libiroh_ffi.so`, `linux-x86-64/libiroh_ffi.so`, `win32-x86-64/iroh_ffi.dll` — and **no Android ABI directory at all**. Android loads from `lib/<abi>/`; the only `libiroh_ffi` reaching the APK is the **macOS .dylib**, at a path the loader never searches. Every real `.so` in the APK belongs to JNA or AndroidX. **The assumption is written in `android/app/build.gradle.kts` itself** — *"this artifact bundles libiroh_ffi.so for every Android ABI (no NDK)"* — and the same comment already names the fallback ("build iroh-ffi from source"): the scaffold predicted its own failure mode and nobody checked the prediction. **FIX PATH (not attempted):** build `iroh-ffi` per ABI with `cargo-ndk` and package as `jniLibs`, or use a genuine Android AAR if one is published. **Candidate ranking was WRONG:** Two candidates, in order of suspicion, both from the artifact's own honesty ledger. ~~(1) The unresolved `VERIFY` markers~~ — **REFUTED.** `connect` resolved them in `5fa0258` against n0's reference app, with a follow-up fix `7433238` (`readExact` takes `UInt`, not `ULong`, CI-confirmed); the one remaining grep hit reads *"VERIFIED 2026-08-02 (was TO-VERIFY)"*. Historic detail: `CallPeer.kt` calls seven iroh Kotlin API names that were **inferred from a "the API maps 1:1 to Rust" promise, never checked** — `ep.accept()`, `conn.acceptBi()`, `stream.send(...)`, `conn.remoteId()`, `ep.connect(EndpointId.fromString(...), ALPN)`, `conn.openBi()`, `stream.recvExact(2u)` — while the bind/identity/lifecycle calls *were* verified against retrieved Kotlin docs. An immediate crash is consistent with a `NoSuchMethodError` on first call. The build instructions' **first Android task** was to resolve these against the Dokka reference and n0's `hello-iroh-ffi` sample `IrohPeer.kt` (names only, architecture untouched, diff recorded in an ADR) — so the open question is whether that step was skipped or done. **(2) The packaging landmines — THIS WAS IT.** the same instructions name up front: JNA `@aar`, Kotlin 2.2, and the ABI fallback path, with a note on which source to trust if the Maven-artifact-on-Android conflict bites. **Method note, worth keeping:** the item was deliberately filed with no cause asserted, and the ranking it did carry turned out inverted. Reading the log took minutes once an emulator existed; guessing would have sent the fix at the resolved-VERIFY-markers path. Note the client is also **foreground-only by design** (staying reachable in the background needs a foreground service + push-to-wake, its own phase), and ships an `adb` one-liner to fire the deep link without the web page, which isolates whether the crash is at launch or at intent handling. | [bug] — **cause found, fix open** | owner report 2026-08-09; `seeds/callpds-unpacked/croftcall-android-unpacked/.../net/CallPeer.kt`; `seeds/callpds-unpacked/croft-call-build-instructions.md` | `CroftCommunity/connect`, `plans/2026-08-07-1-plan-croft-relay-tiered-admission.md` (Phase 10), COHESION §70, **E98** |

| E101 | **CISS lacks the OAuth *resource-server* surface — "no OAuth" was scoped against the wrong role (2026-08-11, PDS-gap walk-out)** — the capability-gap doc's "OAuth: No — bsky is the AS" is correct about the **authorization-server** role and silent about the **resource-server** obligations, and upstream shows those are separable: **every reference PDS serves `/.well-known/oauth-protected-resource` (RFC 9728) even when an entryway is its AS** (`bluesky-social/atproto` `packages/pds/src/auth-routes.ts:31` — `authorization_servers` points at the entryway). CISS serves no RS metadata and verifies no DPoP-bound OAuth access tokens; it accepts service-auth JWTs and `id:` sessions only. **Consequence:** no third-party atproto app can hold a user's OAuth grant to CISS — every client pathway must route through the broker. That is fine *while the broker is the only pathway*; it is a **scoping assumption, not a non-need**, and it silently constrains the ecosystem story (any future "bring your own app to your storage" requires this). **Retire by:** when a non-broker client pathway is wanted: serve the RS metadata document + verify DPoP-bound tokens against bsky's AS. Until then this row is the recorded reason the constraint exists. | [decision] / [build-later] | `CISS/docs/notes/pds-capability-gap.md` (2026-08-11 walk-out, OAuth row split) | `CISS/docs/notes/pds-feature-audit.md` §1.7, `CISS/docs/notes/atproto-integration-model.md`, ADR 0001, **E82** |
| E102 | **CISS request path has no per-DID / per-route rate limiting — the byte-path is metered, the compute path is only globally capped (2026-08-11, PDS-gap walk-out)** — CISS's protections are a global in-flight cap + request timeout (V1/V4/V5) + storage ceilings; the reference PDS additionally rate-limits **per lexicon method and per IP**. A hostile-but-authenticated client can burn CISS compute (signature verification, DID resolution, sqlite work) at line rate *inside* the global cap without moving a metered byte — the meter never sees it, and one noisy DID can starve the rest of the cooperative's members within the shared in-flight budget. **This is a hardening gap under the chosen model today**, not a PDS-ambition item — it is adjacent to seam **E83** (per-DID compute observability: all requests already route through the single dispatch boundary in `src/server.rs`, so the attach point exists), and observability-then-limits is the natural order. **Retire by:** (1) per-DID request/compute counters at the dispatch boundary (E83), (2) per-DID and per-route rate limits fed by them, with the limits themselves owner-visible (the cooperative posture suggests declared, not hidden, limits — cf. the dials pattern). | [build] | `CISS/docs/notes/pds-capability-gap.md` (2026-08-11 walk-out, rate-limiting row) | `CISS/docs/notes/pds-feature-audit.md` §1.7, `CISS/docs/ARCHITECTURE.md` §7 (E83), `CISS/src/server.rs` (dispatch boundary), **E82** |
| E103 | **CISS operational self-description document — extend the existing well-known surface, decided shape (2026-08-11, PDS-adoptable-features walk-through)** — CISS already serves the identity/crypto half of self-description: `/.well-known/did.json` (`src/server.rs:846`) publishes both provider keys with roles (`#assertion-ack` — offline proof an assertion took effect, D2; `#receipts` — the receipt/billing key, D4) and a typed `CissItemStorage` service entry, consumed by `ciss-ctl`. The **operational half is served nowhere**: auth modes accepted (`id:` session, service-auth JWT + `aud`), the blob lexicon implemented, ceilings and body limits, pricing posture, contact. **OWNER DECISION (2026-08-11): build it as a separate native document — `did.json` stays pure identity+keys** (it is the document customers pin for key verification; mutable operational facts do not belong in it), with the existing `service` entry pointing at the capabilities doc — the shape the DID spec expects. The lexicon-shaped `com.atproto.server.describeServer` projection is explicitly **not** built now (its fields are invite/link-shaped; CISS would serve an empty lexicon body with all content in extensions — compat theater); it can be added later as a thin projection if a concrete consumer appears. **Also owed when touching the handler:** the `SEAM:` comment in `well_known_did_handler` ("publishing CISS's provider key here … is a tracked follow-on") is stale against its own body, which already publishes both keys. | [build] | `CISS/docs/notes/pds-adoptable-features.md` §1 | `CISS/docs/notes/pds-feature-audit.md` §1.1 (describeServer ◐), `CISS/src/server.rs:846`, **E82** |
| E104 | **getBlob Content-Type echo — mime rides the upload receipt, design settled (2026-08-11, PDS-adoptable-features walk-through)** — `uploadBlob` sanitizes and *reports* the mime in its response but persists it nowhere, so `getBlob` always returns `application/octet-stream` (declared seam, `CISS/docs/ARCHITECTURE.md` §7; `src/pds_api.rs`). The reference PDS stores `mimeType` in its `blob` table; CISS's analogue store is the **upload receipt** — the mime is a fact about the transfer, which is what receipts attest, and `getBlob` derives the echo the same way `listBlobs` already derives cids from receipts. **The design question ("does adding a field to the receipt body perturb the canonical serialization / hash chain?") is answered and the precondition is built:** add `mime: Option<String>` to `ReceiptCore` with `#[serde(default, skip_serializing_if = "Option::is_none")]` — absent-field receipts (native object plane, all historical) re-canonicalize byte-identically, so **no schema rev, no migration**. The pattern is proven: walking this candidate surfaced that `account_mode` (commit `e53caab`) had been added with parse-compat only (`#[serde(default)]`, no skip), silently making every pre-tag signed receipt fail `core_matches()` → read as *tampered* by any honest verifier; **fixed 2026-08-11** (unreleased window — no tag contained `e53caab`) with `skip_serializing_if = "AccountMode::is_active"` + a permanent RED-first guard (`receipts.rs::a_receipt_persisted_before_the_account_mode_tag_still_verifies`). Mime lands as the same increment shape: guard test first, field + `skip_serializing_if`, store at `uploadBlob`, derive at `getBlob`. **Standing rule this bought:** a field added to any *signed* record body must ship with `skip_serializing_if` on its default, or it breaks verify-compat for everything already signed. | [build] | `CISS/docs/notes/pds-adoptable-features.md` §2 | `CISS/docs/ARCHITECTURE.md` §7 (Content-Type seam), `CISS/src/receipts.rs`, `CISS/src/pds_api.rs`, **E103**, **E82** |

---

## Maintenance

When an item closes, mark it here and in its origin doc, and add the COHESION row if it closed a
seam. When a new open edge is surfaced (in a transcript, proof, or review), add a row with its
origin `file:line` so this stays the single place to scan "what's up for consideration." Re-anchor
line numbers opportunistically — section headers are the durable key.

**Beta-staging view (2026-06-25).** The subset of these items that is *beta-bound but not yet settled*
is staged at the beta gate in `../beta/OPEN-THREADS.md` (with a promotion target + gates per thread), so
it is tracked toward beta without polluting the resolved themes: T1 = Drystone governance/peer model
(E30); T2 = governance at scale (D9/D8/E16); T3 = moderation/abuse design body (D3/D6/E18); T4 = brand
chapter (C6/A7); T5 = protocol-at-scale / group-chat failure modes; T6 = per-platform trust-model doc
(E13); T7 = atproto private-data watch-item; T8 = forward-only revocation under irreversible commitments
(D10); T9 = 01/02 primary-source publication pass; T10 = media-layer hardening. This list is the general
backlog (any stage); OPEN-THREADS is the beta-gate lens on the beta-bound slice.
