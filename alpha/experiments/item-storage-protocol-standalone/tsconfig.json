{
  "//": "For editor tooling only. The suite RUNS with no build step via Node's type stripping (node >=22.6). erasableSyntaxOnly keeps the source compatible with that (no enums, no parameter properties).",
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "nodenext",
    "allowImportingTsExtensions": true,
    "erasableSyntaxOnly": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
