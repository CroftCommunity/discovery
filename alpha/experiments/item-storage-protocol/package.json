{
  "name": "item-storage-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Protocol-level cryptographic model of the co-op item-storage offering: signed manifests, bilateral receipts, balance-forward statements, spot-check audits, seal/tombstone tiers, a grace ledger, and an erasure-coded upgrade path. Zero external dependencies; Node built-in crypto + node:test.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "run": "node scripts/run.ts",
    "verify": "node scripts/verify-ledgers.ts",
    "test": "node --test"
  }
}
