//! `ds-cli` — RED skeleton: refuses every op. GREEN: the CLI the P5 Node
//! loop driver shells out to for its QUIC legs.

fn main() {
    eprintln!("ds-cli: unbuilt (P4 red)");
    std::process::exit(1);
}
