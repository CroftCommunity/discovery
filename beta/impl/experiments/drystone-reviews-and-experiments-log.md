# Drystone: reviews and experiments log

`Purpose: a durable record of external feasibility reviews and convergence-experiment runs against the specs, each with its verdict and the spec changes it produced. This is a catalogue, not an authoritative spec. The three authoritative docs are Part 1 (principles), Part 2 (mechanics), and the terms-and-definitions file (conventions-and-decisions).`

## 2026-07-04, Feasibility review, first pass

Scope: the two-part spec, but the reviewer could not read the spec files and reconstructed internal-mechanics findings from a description, tagging them [Design, spec-unverified]. The independent primary-source verification was complete and is the load-bearing contribution.

Verified accurate against primary sources: RFC 9420 and RFC 9750 (application-data carriage, ordering in §15.3, the vector ceiling, padding, external commits, ReInit non-atomicity, epoch_authenticator, FS and PCS, metadata, the DS and AS trust split), CALM Theorem 1, CRDT strong-eventual-consistency, the Matrix CVE-2025-49090 and MSC4289 / MSC4291 / MSC4297, BLAKE3, and iroh 1.0.

Verdicts: Bar 1 (implementability) conditionally feasible, blocked only by the open `[gates-release]` encodings. Bar 2 (cryptographic and security soundness) sound on the MLS substrate. Bar 3 (load-bearing open problems) the completeness beam is closable but unearned, and the §4 / §7 hash split is low-difficulty.

Spec effect: none directly. The internal-mechanics findings were carried into the second pass to be checked against the text.

## 2026-07-04, Feasibility review, second pass

Scope: read Part 1 and Part 2 in full, and re-verified the external claims.

Corrections to the first pass: withdrew the forward-secrecy-versus-durable-history tension (W1), since §8.1 is correct and scopes forward secrecy to the key schedule rather than to ciphertext. Downgraded the metadata-floor finding, since §6.4 is drawn more precisely than the review was.

Cross-check outcome: most internal-mechanics findings were already handled in the text. The beam was classified as liveness-only and safety-monotone under a fail-closed gate, which matches §7.3.8. Carry-not-bind (E6) was already stated completely in §6.2.2. The §4 length-extension check was run and found no §4 use is a secret-prefix MAC, so re-basing on BLAKE3 is a clean substitution.

Spec effect: one addition. The §4 length-extension check and the bundle-and-re-prove wire-freeze caution were recorded in the Part 2 Appendix B hash entry. Everything else the review recommended was already present, often more thoroughly than the review's own draft.

## 2026-07-04, Convergence experiment v2

Fold under test: a faithful reference fold implementing R1 through R4 and the A12 layered fold, not a production fold, of which none exists in the repository.

Stage 1 (R1 through R4 and A12, properties A through F with D6 and D7): 27 tests, all pass, including the discriminating D6 and D7 that a flat id-only fold fails. Establishes permutation-invariance and the resolution rules as reference-model logic.

Stage 2 (gap-detection): referenced-gap detection passes, where a node holding a fact whose referenced predecessor is absent returns a gap error rather than folding an incomplete set as complete. The unreferenced-tail case is the documented limit, which is the completeness-ahead beam. Convergence-after-fill passes for a single gap and for multi-node reconciliation.

Stage 4 (finality, groups G through K): 16 tests, all pass. Quorum folding (A1), non-exclusive recognition (A2), the ceiling (A3), and the now (A7).

Stage 3 (adversarial scheduler, equivocation surfacing a fork, bounded model checking): specified, not implemented.

Beam status: unchanged, `Load-bearing, unearned`. The experiment demonstrates completeness behind a checkpoint (referenced-gap detection) but not completeness ahead (the unreferenced tail), which is the open half.

Spec effect: R1 through R4, A12, and the A1 through A3 and A7 finality mechanics are now reference-model-demonstrated. Two Part 2 status upgrades from `Design` to `Modeled`: the A12 tier-boundary projection consistency (§7.3.2) and the R3 no-fold-time-rejection with referenced-gap detection (§7.3.2). The experiments-consolidated file §1 records the full v2 result. System-level order-independence stays `Load-bearing, unearned` pending the beam.

## 2026-07-13, Real-substrate spikes, reconciliation landing

What ran: the imported real-substrate spike corpus now in `alpha/experiments/` (the re-plant and iroh testbed line of work), reconciled against Part 2 via the reviewed diff set `beta/drystone-spec/proposed-changes-2026-07-experiment-reconciliation.md` (items F1 through F8). The load-bearing spikes: `local_storage_projection` and `croft-chat` (RuleChange quorum enforcement and the `Session` governance emit API), `mls-replant` and `replant-continuity` (re-plant mechanics and the membership-continuity invariant, driven through the real fold-ingest path on real openmls 0.8.1), `croft-chat` plus `iroh` (loopback gossip convergence to identical fingerprints), and the `iroh` conformance-core (categories 7/8/9 recorded not-yet-emitted).

Reconciled deltas (three, tags retired). `x2-backfill`: the per-tick whole-log re-flood stand-in was replaced by sync-on-connect resync (`iroh_bus` re-broadcasts the retained log once on `NeighborUp`, O(log) per join, not per tick). `rulechange-quorum`: the RuleChange threshold is now enforced via a content-hash approval subject over the distinct-personae-by-lineage path, not an Owner-role proxy; RED to GREEN with a manual mutation gate. `handcrafted-assertions`: `Session` now emits well-formed RuleChange and Approval facts (`propose_rule_change` / `approve_rule_change`), closing the capability gap.

Active delta (one). `hermetic-gossip`: the two convergence tests run on loopback (`RelayChoice::LocalDirect`), not the real n0 relay plus holepunch path. That path is X1, genuinely needing real NAT, unreproducible where Internet UDP is blocked. Green does not prove the relay path.

Spec effect. F1 lands the one genuinely new mechanism, R7 (content-bound quorum for policy changes), in §7.2 at `Modeled`, pending the X3 cross-package mutation sweep, and tightens §8.2(e) (policy-change enforcement now test-run; the §7.4.2 freshness precondition still not over live transport). F2 upgrades §7.6.2's re-plant membership-continuity half to `Verified` (the message-continuity half stays open pending the dataplane hash structures). F3 (§6.8.1), F4 (§11.11 measurement #1), F5 (§8.2(a)), and F7 (§10.5 conformance-vector footnote) are caveats that bound possible over-reads rather than adding claims. F8 records the two-competing-quorums decision (§7.3.2 and §7.6.1): two concurrent quorum-met RuleChanges to the same rule are a §7.6-class genuine contradiction, hard-stopped and human-adjudicated, never content-address-tiebroken. F6 is a deliberate non-change (§7.6.3's ReInit-stranding `[confirm]` stands; E12.6 touches availability, not the intent-before-freeze ordering the discharge needs). Full per-item evidence lives in the Part 2 changelog ("Pass: 2026-07 real-substrate experiment reconciliation"), `SPEC-DIVERGENCE-REGISTER.md`, and `RUN-02-SUMMARY.md`.

## 2026-07-14, RUN-01 (unattended experiment run, branch `claude/experiments-run-01`)

A queued unattended run (brief: `alpha/experiments/NEXT-RUN-INSTRUCTIONS.md`) executing the four runnable-now experiments scoped to this environment (loopback iroh, no boxes, no macOS/iOS hardware). One commit per experiment. Entries below are per-EXP.

### EXP-2 — Automerge 0.7 confirmation (§7.7/§7.9 late-joiner inertness on the ship target)

Fold under test: none — this is a third-party-crate feasibility confirmation. Bumped `automerge-partial-reconstruction` from `=0.6.1` to `0.7` (→ 0.7.4), applied the two documented API deltas (`get_changes` now owned `Vec<Change>`; `get_missing_deps` now `&self`), re-ran the four partial-reconstruction scenarios on Rust 1.94.1.

Result: **PASS** — all four invariants hold identically to 0.6.1. Scenario A (the load-bearing one — later-epoch changes with deps withheld held **inert**, `messages` absent, 0 heads, 1 buffered dep) holds on 0.7.4. Only change-hash *values* differ across versions (a serialization artifact), not behavior. FALSIFY condition not met.

Spec effect: retires the standing proxy caveat `automerge-0.6.1` (SPEC-DIVERGENCE-REGISTER → Reconciled). No new Part 2 mechanism — this hardens an honesty boundary under the existing late-joiner design (§7.7/§7.9); the 0.7 ship target is now the evidence base rather than a 0.6.1 proxy.

### EXP-1 — A4 / M1 fan-out (§11.4/§11.5 "cost scales on the live set"; §11.11 measurement #1, fan-out half)

Method: N local `serve` processes (`scripts/fanout-measure.sh`) converging over real iroh-gossip on the loopback testbed (`relay_mode = "disabled"`), N = 2/4/8/16. Instrumented `iroh_bus`/`serve` to report gossip message counts and two convergence latencies (head = full timeline folded; converged = also `pending == 0`). Captured the curve, not a point (`croft-chat/FANOUT-M1.md`, raw in `fanout-data/`).

Result: **PASS** (curve captured, shape stated honestly). Per-node steady-state gossip cost is **linear in the live set** (`live_sent = 2N + 1`: 5/9/17/33), aggregate O(N²) (flood gossip). Head/state convergence holds at **every N** — identical fingerprints (I5 scales across the fan-out); head latency ~1 s to N = 8, ~3 s at N = 16. **Prominent flag (per the FALSIFY clause):** the connect-time resync path is **super-linear** on the bootstrap hub (creator `resync_sent` 3/15/64/479) and full-settle (`pending == 0`) does not complete past N ≈ 8 in a 45 s window (heads still match — it is the honest incompleteness signal not draining, not divergence). This is *not* a FALSIFY of "scales on the live set" (per-node live cost is linear; heads converge); it is a concrete cost signal for the **already-open RBSR / steady-state-anti-entropy gap** (proposed-changes F3; register note). Register: `fanout-single-run` (proxy-measurement — 1–2 runs/N, ±250 ms tick, star-bootstrap; shape robust, magnitude indicative).

Spec effect: proposed-changes **F4** moves from "per-commit measured, fan-out unearned" to **both halves measured** (fan-out earned in *shape*; magnitude-open at hot-N = 500+ on real hardware). No Part 2 posture change. Scope boundary recorded: in this testbed a membership boundary is a governance fact over gossip, not an openmls commit, so this is the fan-out volume/latency curve; the cryptographic per-commit re-key band stays with `mls-replant` M1.

### EXP-4 — Fold open items (§7.6.1 concurrent contradiction + adjacent)

Method: two focused tests on the §7.6.1 contradiction surface (`croft-chat/croft-chat/tests/competing_quorums.rs`), driven through the real `DerivedFold::ingest` path, folded in both arrival orders.

Results:
- **Contradicted-group byte-head naming — PASS (GREEN corroboration).** When a contradiction hard-stops (mutual expulsion), the surfaced status `contradiction:{hash}` names the group by the deterministic byte-head `min(H(F), H(G))` — proven exactly equal to the computed min-hash and **identical across both fold orders**. §7.6.1's contradicted-group naming is order-independent.
- **Two competing RuleChange quorums — FALSIFIED (refutation), confirming the impl gap vs. the F8 decision.** Two concurrent conflicting RuleChanges on the same rule, each carrying a valid k-of-n quorum, **auto-resolve order-dependently** (order1 → `add_member_threshold=9`, order2 → `5`; both `fork="clean"`, no hard-stop) — a silent I5 violation on exactly the shape §7.6 says must escalate. The fold's contradiction predicate set (mutual-expulsion / removed-then-included / role-thrash) has no competing-RuleChange predicate. **This EXP-4 case is precisely the experiment RUN-02's F8 flagged** ("no evidence tag until the two-competing-quorums experiment runs"): the design is already **decided** (F8, §7.3.2 / §7.6.1 — hard-stop, human-adjudicated, never tiebroken), so this is now an **implementation gap, not a design decision.** Pinned as a refutation (`two_competing_rulechange_quorums`, register `competing-quorum-autoresolve`); which predicate *shape* to build is an implementation choice (backlog §2a).
- **Per-act approver-role granularity — DEFERRED (still-undecided design).** Step 5.6 counts approver personae by lineage **regardless of role**; whether acts should require a per-act approver-role floor is an undecided trust-model call (RUN-02 R7 lists it as an open residual). Options written to backlog §2a; not implemented (stop rule b).
- Skipped the "catching up…" TUI indicator (UX, low value unattended), as the brief directed.

Spec effect: one corroboration (byte-head naming order-independence, §7.6.1) and one **load-bearing refutation** — competing RuleChange quorums are a real §7.6 gap. This is the evidence RUN-02's **F8** said was pending: F8 already *decided* the spec (hard-stop), and EXP-4 confirms the current implementation *violates* it. So F1's R7 enforcement is real but **not concurrent-conflict-aware**, exactly as R7's residual note anticipated. No Part 2 edit; EXP-4 supplies F8's missing evidence and turns the competing-quorum item from "design-gated" into a scoped implementation task.

### EXP-3 — X3 mutation sweep on the auth/governance path (§7.3 / §8.2(g) trust claim; F1)

Method: `cargo-mutants` scoped (via `--re`) to the authority/threshold functions across `fold_auth.rs` + `governance.rs` + `fold_derived.rs` (`PROPTEST_CASES=8`, repo `.cargo/mutants.toml` skips, `-j 2`). Report + raw record: `local_storage_projection/X3-CROSS-PACKAGE-SWEEP.md`, `x3-sweep-data/`.

Result: **PASS on the trust question, PARTIAL on X3 mechanization.** 120 mutants → 54 caught, 61 missed, 5 unviable. **Threshold-counting / quorum arithmetic (`governance.rs`) = 13/13 caught, 0 survivors** — the FALSIFY target (a surviving threshold-counting mutant) is not met. The 61 survivors are **all** authorization-*decision* mutants (`check_authorization` guards, `role_ge_*`, `act_subject`, `rule_change_approval_subject`) whose positive-path coverage lives cross-package in `croft-chat` — the known under-count, now quantified. Demonstrated concretely: hand-applied `rule_change_approval_subject→const` (a substrate-survivor) and watched `croft-chat`'s `approval_for_a_different_change_does_not_count` **FAIL** (killed), then reverted. **No real hole** in the authority/threshold path — the survivors are an instrument artifact (substrate-only can't see the consumer suite).

Spec effect: adds mutation evidence to **F1** (threshold-counting mutation-clean; approval-subject cross-package-killed) — supports `Modeled`, with the *automated* cross-package harness as the residual bar for `Verified` (still open X3). No Part 2 edit.

### EXP-5 — MLS key-distribution + threshold-revoke over the wire (§10.2/§10.3, conformance cats 7/8/9)

Outcome: **BLOCKED at a design gate — stopped, not improvised** (the brief's explicit EXP-5 stop rule; no trust model invented; no iroh code touched). Assessment only. Half 1 (MLS key-distribution over the wire) is **already realized in a spike** (`iroh/crates/mls-welcome-over-iroh`: a real openmls Welcome crosses a real iroh connection; the joiner derives the identical MLS exporter secret + lineage fold from the wire) — the "modeled verifying-key registry" is made real there, not yet wired into conformance emission. Half 2 (threshold-revoke as real k-of-n over the wire) **hits the design gate**: the MD-G5 revoke is a sha-256 MAC stand-in and replacing it needs the **revocation-authority model** (who-may-revoke, the k-of-n dial, key discovery / trust root) — the identity/key-recovery problem (MASTER-INDEX I9). Options A/B/C written to backlog §6d-i; recommendation noted (A: quorum-of-Ed25519 reusing the proven governance k-of-n, which is the one option not blocked on I9) but left for the human.

Spec effect: **no conformance cats moved** — 7/8/9 and the revoke-authority vector stay `not_yet_emitted`, now with a named blocker (the revocation-authority decision) recorded against **F7** in the proposed-changes doc.

## 2026-07-14, Continuity decoupling and reconciliation-horizon design pass

Scope: land the design decisions the owner made after the 2026-07-14 merge (RUN-03). This is a design-and-documents pass with one scoped code change (the F8 implementation gap). The two design calls are owner-decided and were not re-opened.

**The render-over-re-plant resolution.** The long-standing §7.6-versus-projection tension — is a projection (a member's own rendered view of history) a new protocol mode? — is resolved as *no*: projection is render over re-plant. At the protocol layer every fork **is** a re-plant (MLS absorbs members or instantiates fresh groups; there is never a merge; there is always an epoch change), and none of that binds a member's attachment to the Group object, which lives in the lineage (§5.10). Continuity is a lineage property, not an MLS-instance property. Two decouplings follow and are now normative in Part 2: an epoch roll carries no inherent social meaning (standing is read from the decision plane §7.3.5, never from the enforcement plane), and membership history composes over gaps (a depart-and-rejoin is one continuous relationship, §5.11 read-scoped keys making the gap substantive). Divergence is a property of folded state, never of renders.

**The two Part 2 `Design` paragraphs.** §7.6.2 gained the continuity/decoupling passage (render over re-plant); §7.6.9 gained the reconciliation-horizon cadence worked example (epoch-roll OR N-facts, a horizon checkpoint extending the §7.3.3 self-checkpoint with an open-contradiction manifest, co-signed under §7.3.3 corroboration semantics, no wall-clock, the horizon bounding presentation staleness and never resolution). One Appendix B `[gates-release]` item added for the manifest encoding. DR language (conventions A.11) applied: continuity-framed, non-moral. Details in `part-2-changelog.md`.

**The new thinking note.** `alpha/thinking/reconciliation-horizon.md`, companion to `the-shape-of-disagreement.md`: motivation (unbounded re-projection cost, the release valve is a log-derived boundary with no trusted assertion), the Layer-1-continuous / Layer-2-cadenced split, the cadence rule, the objective horizon checkpoint versus the member-private projection checkpoint, decay-not-expiry, and coherence temperature without policy profiling. A resolution note was also added to `the-shape-of-disagreement.md` §10, sibling to the RUN-02 F8 note.

**EXP-H1 (backlog §2b).** The objective, no-policy half of the horizon design, pinned as a determinism test: two members, one contradiction, a horizon boundary driven in both trigger modes, asserting byte-identity of each member's manifest `(frontier head, sorted open contradiction byte-heads)` across members and arrival orders. Runnable today against the mutual-expulsion contradiction; extends to competing-RuleChange once the Phase B predicate lands.

**Phase B — the competing-RuleChange contradiction predicate (F8 implementation gap).** Landed. Closes register row `competing-quorum-autoresolve` by implementing the narrowest F8 form in the fold's concurrent-contradiction predicate family (`fold_derived::detect_competing_rulechange`): two concurrent, admitted RuleChanges on the same `rule_key` with differing `new_value` contradict, surfaced identically to mutual expulsion as `contradiction:{byte-head}` with the order-independent min-hash; the rule keeps its pre-conflict value (no verdict). The refutation pin `two_competing_rulechange_quorums` flipped RED→GREEN and now asserts the fixed behavior — both fold orders yield identical contradiction status (`fork="contradiction:5680676b…"` byte-identical) and identical pre-conflict effective rules (`add_member_threshold=1`, neither contested value 5/9 winning). Two negative cases were added and pass: `concurrent_same_value_rulechanges_are_concordant` (concordant, fork clean) and `concurrent_disjoint_rulekey_rulechanges_do_not_conflict` (disjoint keys, fork clean). Register row moved Active → Reconciled; backlog §2a marks the impl gap closed (per-act approver-role granularity stays open); EXP-H1 can now include a competing-RuleChange manifest entry. Edits confined to the `governance.rs` predicate family (`fold_derived.rs`) and the named test; no wire/envelope or surfaced-status-format change. Full test output in `alpha/experiments/RUN-03-SUMMARY.md`.

## 2026-07-14, Corroboration dials and the beam contract (design pass)

Scope: a markdown-only design pass, no code, landing the owner's framing of what final state costs a node that can never see past its own edge. Two `Design` paragraphs into Part 2, one backlog item, one new thinking note. The framing was owner-decided and not re-opened.

**The quantified-trust framing.** §5.1 is restated as the epistemic floor: everything a local authority holds about the outside world is comparative or asserted, never canonical, so all corroboration is quantified trust and the node's job is not to *know* the edge but to be configured with the terms under which corroboration counts as sufficient. Sufficiency is a setting (the k of §7.4, and the act classes it gates), not a discovery a node can make about a global state it has no vantage on.

**The stamp-as-beacon reading.** The pass turned on the read/write asymmetry. On write a node *does* know the last event it asserted on top of, because every outgoing entry references it; the §7.4.3 governance-generation stamp makes that legible, so every ordinary data-plane message doubles as a passive currency beacon (the "I'm good, you?"), with R6 attributable acceptance (§7.5.1) as its enforcement-side twin. This closes the behind-via-traffic case (an ahead-stamped entry names a sized, known gap the reader fills before enforcing) and leaves exactly the unreferenced tail. The read side is solicitation: a frontier ask reaching the tail no stamp can announce, whose answer is always an assertion at quantified trust, so an empty solicitation is corroborated absence, never established absence.

**The two Part 2 `Design` paragraphs.** §7.3.3 gained the corroboration-dials paragraph after the load-bearing-caveat paragraph: the residual of completeness-ahead once the stamp closes the behind-via-traffic case is a family of R7-governed settings (which act classes require final state; the freshness k or its formula; the solicitation posture), safe at every setting because the fail-closed rule (delay over breach) is not a dial. §7.4.1 gained a final paragraph admitting a formula-valued freshness threshold: a Group MAY set k as a formula over folded state (proportional to folded member count, or role-weighted) provided every input is folded fact, so k stays deterministic and identical everywhere; it moves the dial, not the machinery. DR language (conventions A.11) applied: continuity-framed, non-moral. Details in `part-2-changelog.md`.

**The self-echo exploration (with its two seams).** A node's own stamp echoed back in others' entries is delivery corroboration without receipts, kept exploratory in the new note and *not* promoted to spec, held to two hard seams: positive-evidence-only (no echo means nothing, since silence and partition are indistinguishable) and liveness-signal-only (the moment an echo touches ordering or authority it is a covert clock, which Part 1 §2.0.1 forbids). For anyone else an echo may mean nothing, which is consistent with corroboration being quantified trust.

**The new thinking note.** `alpha/thinking/corroboration-and-quantified-trust.md`, companion to `reconciliation-horizon.md` and `the-shape-of-disagreement.md`: the epistemic floor, the already-built write side, the read side and the unreferenced tail, formula-valued thresholds, the self-echo exploration, and the beam reframed as a dial family plus one intrinsic honest limit (earning the beam means demonstrating the contract, not eliminating the limit).

**EXP-C1 (backlog §2c).** The completeness-ahead contract, loopback-runnable now with no new infrastructure, four RED-able assertions: stall-at-threshold (delay over breach, reads continue), stamp detection (the behind-via-traffic case end to end), solicitation reach (the unreferenced tail surfaced by a frontier ask and folded identically to normal arrival), and formula-valued k (ceil(n/2) over the folded member set, identical across arrival orders). Shares boundary machinery with EXP-H1; discharges at loopback grade part of §8.2(e)'s residual that the freshness precondition on originating such an op is not yet exercised over live transport.

Spec effect: two Part 2 `Design` paragraphs (§7.3.3, §7.4.1) and two §0 map lines; no mechanism or code change, no posture-table change. The demonstration contract is carried as backlog EXP-C1, unrun.

## 2026-07-15, RUN-07 (the code run: X3 automated harness, EXP-H1, EXP-C1, branch `claude/run-07-code-run`)

Scope: one markdown rider phase and three code phases, one commit per phase. Full per-phase detail in `alpha/experiments/RUN-07-SUMMARY.md`; the X3 verdict in `alpha/experiments/local_storage_projection/X3-AUTOMATED-SWEEP.md`. Both suites (`local_storage_projection` 97 tests; `croft-chat` 25 test binaries) and clippy green at every commit boundary, no new warnings.

**Phase A — riders (markdown).** T11 double-tag polish at §7.3.2 (the F8 sentence now carries exactly one status tag). FND-1 refined from the RUN-06 owner call to the explicit range cite `(§7.4–§7.4.2)` in the two living-doc sites (§8.2(e) and the EXPERIMENT-BACKLOG quote), the §7.4.2-hazards table cites left untouched; recorded in the CONSISTENCY-FINDINGS settlement addendum (with the post-hoc ratification of FND-5/FND-6 and FND-8 recorded as mistaken). Changelog entry for A1/A2.

**Phase B — X3 automated cross-package harness (the residual the substrate-only sweep left open).** The clean single-invocation cargo-mutants config is unreachable (the two crates are separate workspaces; cargo rejects an out-of-tree member and cargo-mutants cannot copy a path dep outside the workspace root), so the repeatable shape is the thin harness the X3 report anticipated (`x3_cross_package_harness.py`): patch the substrate with the exact cargo-mutants diff, run `cargo test -p croft-chat`, revert; no production code or manifest changed. Result: the current-code substrate sweep found 61 survivors, and the harness resolves all 61 mechanically as **7 killed, 54 individually justified, 0 unjustified**. The 7 killed are exactly R7's content-bound-quorum count path (`rule_change_approval_subject`, `act_subject` resolution, rule-key decode, membership-admin gate; `rule_change_approval_subject→const` verified by hand-apply against `approval_for_a_different_change_does_not_count`), on top of the already mutation-clean `governance.rs` counting. Load-bearing finding: `fold_auth.rs` is an **off-consumer-path duplicate** — `AuthFold` runs only in its own `#[cfg(test)]` and `surface` uses `fold_derived`, so all 31 fold_auth survivors survive the consumer suite; this refutes the original X3 reading that all survivors were cross-package-covered (new register row `fold-auth-duplicate`). The other survivors are the role-authorship gate (7, which R7 explicitly excludes from the count claim), uncovered Vouch payload validation (10), node-card provenance (3), and boundary/adjacent-rule mutants (3). **Spec effect (B5, condition met):** §7.2 R7 `Modeled → Verified` for the content-bound-quorum *count*, evidence line citing the automated sweep; §8.2(e) enforcement note; register mutation-gate note + `fold-auth-duplicate` row; MASTER-INDEX A2 done; changelog. The tag is scoped to the count; the role model stays R7's open residual, now quantified as unpinned by the consumer suite. Stop rule B.6 not triggered.

**Phase C — EXP-H1 horizon-manifest determinism (backlog §2b).** Landed as pure fold-side functions in `local_storage_projection::horizon` (experiment-grade: no wire format, no persistence, no networking; the `[gates-release]` §7.6.9 encoding untouched) plus `DerivedFold::read_group_state`, exercised cross-package by `croft-chat/tests/horizon_manifest.rs` (4 tests, green). `horizon_manifest(state) -> (frontier_head, sorted open-contradiction byte-heads)` is byte-identical across members and arrival orders for both contradiction shapes — mutual expulsion (`open=[d4196bb5…]` both orders) and competing quorum-met RuleChange (`open=[62584e17…]` both orders) — compared via a test-only serialization explicitly not the release encoding. `frontier_head` is an order-independent digest of the converging state (gov_seq + rules + sorted members); the raw `computed_at_gov_head` is the last-ingested hash and is arrival-order dependent, so it is excluded. The `HorizonCadence` trigger fires on an epoch roll and on N facts since the last boundary (counter reset each boundary), locating the boundary at the same fact position on both members; negatives pin that an absent contradiction is absent and an open one never ages out across horizons. Earns the manifest-determinism claim only; §7.6.9 stays `Design`. Backlog §2b marked done.

**Phase D — EXP-C1 completeness-ahead contract (backlog §2c).** All four assertions landed and green at loopback / fold grade in `local_storage_projection::completeness_ahead` (pure helpers `quorum_k`, `detect_stamp_gap`, `admits_irreversible`; 3 inline unit tests) + `croft-chat/tests/completeness_ahead.rs` (4 tests). None required MLS internals or network transport, so nothing was split out. (1) stall-at-threshold: a node denied a governance fact stalls the dependent irreversible act below `k = ceil(n/2)` (fail-closed, no breach) while serving reads on its best-known prefix; (2) stamp detection: `detect_stamp_gap` detects and sizes an ahead-stamped entry and returns `None` once filled; (3) solicitation reach: an unreferenced-tail fact absent on X is surfaced by a frontier ask (re-delivered into X's persistent live pipeline) and folds to the byte-identical fingerprint of a node that received it live; (4) formula-valued k: `quorum_k` over the folded member count is identical at the same act position across arrival orders. Freshness/stamp values are integers the test seeds; the `[gates-release]` stamp and `(G,D)` cursor encodings are untouched. **Spec effect (D-close, condition met):** the one §8.2(e) touch records the origination precondition "exercised at loopback grade (EXP-C1, RUN-07) ... real-NAT path remains X1"; changelog + backlog §2c done + MASTER-INDEX. No status upgrade beyond the loopback record.

**Follow-up (owner-authorized) — retired the `fold_auth` dead duplicate.** Acting on the Phase B finding (register `fold-auth-duplicate`), and on the owner's explicit go, `local_storage_projection/src/fold_auth.rs` (`AuthFold`, 1685 lines) was deleted and `pub mod fold_auth;` removed. Grep confirmed nothing outside the module referenced it — only three `fold_derived.rs` comments (reworded) and the lib.rs registration — and `fold_derived` already defines its own `FoldError` / `IngestResult` / `check_authorization`, so `surface::LocalStore → fold_derived::DerivedFold` is unaffected. This makes `fold_derived` the single authorization path in the crate, the one the consumer suite pins (the 7 count-enforcement mutants killed cross-package). `local_storage_projection` builds and tests green after removal (88 lib tests; the 9 dropped were `fold_auth`'s own unit tests of dead code), `croft-chat` (25 binaries) + clippy green. Register row moved Active → Reconciled; `X3-AUTOMATED-SWEEP.md` residual and MASTER-INDEX A2 updated.

## 2026-07-15, RUN-08 (build then trace, branch `claude/run-08-build-and-trace`)

Scope: two parts on one branch. Part 1 lands riders, a BIP39 Tier-1 lock spike, and the MLS key-distribution-over-the-wire conformance piece; Part 2 runs the spec-to-experiment traceability pass against the branch's own post-Part-1 state. Full per-phase detail in `alpha/experiments/RUN-08-SUMMARY.md`. Both suites (`local_storage_projection`, `croft-chat`) and clippy green at every commit boundary; zero new warnings. An owner-authorized infra commit folded the `Proofs` corpus into `discovery/alpha/Proofs/` (frozen, canonical) so the `mls-welcome-over-iroh` spike and the conformance-core are buildable in-repo; `lineage-groups` and `conformance` are MLS/transport/proof code and do not touch the I9 trust tier.

**fold_auth deletion authorization confirmed.** The RUN-07 follow-up that deleted `local_storage_projection/src/fold_auth.rs` (the off-consumer-path dead duplicate, register `fold-auth-duplicate`) was owner-authorized; the owner confirmed that authorization on 2026-07-15. Recorded here per the RUN-08 rider.

**Part 1A (riders).** Bannered the original `X3-CROSS-PACKAGE-SWEEP.md` (its 61-survivors conclusion partially refuted by the RUN-07 automated sweep, 31 dead-duplicate `fold_auth` survivors; record otherwise verbatim). Filed the Vouch payload-validation uncovered residual as backlog §2d (the fold's I5 Vouch gate, 10 survivors uncovered by both suites; retirement condition recorded). Added the frontier-head pinning note to `reconciliation-horizon.md` (EXP-H1 found the naive last-ingested head is arrival-order-dependent; the §7.6.9 pinning pass chooses between the converged-state digest and the sorted DAG-tip set).

**Part 1C (BIP39 Tier-1 lock).** New spike `bip39-recovery-roundtrip` (11 tests, clippy clean): recoveryKey to 24-word BIP39 English mnemonic round-trips bit-exact; the standard English KATs pass both directions (canonical 256-bit Trezor vectors) including checksum-failure negatives (a corrupted word, a transposed pair, an out-of-wordlist word, a wrong word count are each rejected, never silently accepted); masterKey secretbox-wrapped (XSalsa20-Poly1305 via `dryoc`) under the recoveryKey unwraps bit-exact, and a wrong key or tamper fails cleanly. Firewall respected: the lock only, no share-splitting, no threshold, no release predicate; the trust tier stays I9. Crate choice stated experiment-grade, not a `[gates-release]` decision.

**Part 1B (key-distribution over the wire).** The reference conformance-core (now folded in) re-proves 66/0 across categories 1 through 9 in-environment, so the §10.5 footnote's "categories 7/8/9 not yet emitted" is superseded for the vectors themselves (cat 7 real Rust, cats 8 and 9 TS-authoritative, cat-5b revoke-authority mechanism). The MLS key-distribution piece was reproduced green-real at loopback grade (a real openmls Welcome over a real iroh connection distributes the group key and the lineage-folded standing; `relay-lab-runs/C-mls-welcome-2026-07-15-run08`). No new over-the-wire vector was authored (that needs a new vector schema, a stop rule) and no iroh was wired into the frozen emitter; the threshold-revoke-over-the-wire and co-sign-versus-vote authority ordering stay gated on the revocation-authority trust model (I9, firewall).

**Part 2 (traceability).** Forward and backward links traced against the post-Part-1 state; no status tag moved. FIXes applied: `Serves:` headers on the five load-bearing reports; `Earns/bounds: Part 2 §X.Y` doc-comments on 21 spec-earning tests (a comment-only code commit); the `handcrafted-assertions` register row gained its retirement landing; backlog pointers completed. The living index `beta/drystone-spec/EVIDENCE-MAP.md` was created (one row per tag at or above Modeled; an index, never a status source), with a pointer sentence in the §8.2 preamble. Seven traceability findings opened (`FND-T1..T7`, 0 HIGH / 3 MED / 4 LOW) in `CONSISTENCY-FINDINGS-2026-07.md`, to get the walk-through and settlement pass the RUN-05 findings received.

Spec effect. Part 1B updated the §10.5 conformance-vector footnote (66/0 emitted; key-distribution `Verified` at loopback grade; threshold-revoke gated on I9) and the F7 annotation, with a `part-2-changelog.md` entry; no mechanism change and no status-tag move beyond the footnote annotation. Part 1C moved backlog §6g to done and added the `open-threads.md §2` Tier-1-lock note. Part 2 added the §8.2 evidence-map pointer sentence and the `EVIDENCE-MAP.md` index; it moved no tag. The full per-item record lives in `RUN-08-SUMMARY.md`, `EVIDENCE-MAP.md`, and the RUN-08 sections of `SPEC-DIVERGENCE-REGISTER.md` / `EXPERIMENT-BACKLOG.md` / `CONSISTENCY-FINDINGS-2026-07.md`.

## 2026-07-15, RUN-09 (the runnable remainder, branch `claude/run-09-remainder`)

Scope: five parts in dependency-and-risk order, each shipping independently (commit + summary section per part). Full per-part detail in `alpha/experiments/RUN-09-SUMMARY.md`. The I9 firewall holds throughout: no release-predicate, threshold-share, revoke-authority-ordering, or co-sign-vs-vote work; X1/hot-N-500, the `[gates-release]` + BLAKE3 pass, and the frozen-emitter integration stay parked.

**Proofs fold-in authorization confirmed.** The RUN-08 infra commit that folded the `Proofs` corpus into `discovery/alpha/Proofs/` (frozen, canonical) was owner-authorized; the owner confirmed that authorization on 2026-07-15. Recorded here per the RUN-09 Part 1 rider.

**Part 1 (riders — traceability settlement).** The five RUN-08 traceability findings owner-confirmed for settlement 2026-07-15 were applied and recorded in the `CONSISTENCY-FINDINGS-2026-07.md` settlement section. FND-T1: the forward-link target is redefined per evidence band (experiment-earned → test/report + RUN; `Verified-RFC`/literature-anchored → primary-source anchor) via one A.9 note, and the ≈40-tag substrate band re-audited under the right form closes as satisfied (no experiment-earned tag lacks a pointer). FND-T4: the `(evidence: …, RUN-NN[, grade])` parenthetical is adopted as the recommended forward form via one A.9 note, not retrofitted. FND-T5: `Reviewer-judgment` in §10.4 de-backticked to plain prose (no eleventh rung). FND-T6: one A.9 note records that the legacy `green-real`/`green-model`/`not_yet_emitted` vocabulary is alpha-tier only. FND-T7: the compound qualifier `real-NAT` canonicalized (hyphenated) in the living docs. No mechanism change; no status tag moved.

**Part 2 (Vouch payload-validation coverage — backlog §2d).** RED-first consumer-path tests (`croft-chat/croft-chat/tests/vouch_payload.rs`, 9 tests) exercise the fold's I5 Vouch payload gate (`fold_derived.rs:447-472`) end-to-end through the real `DerivedFold` (the `surface::LocalStore` path): well-formed accepted, malformed / empty-context / truncated / oversized-trailing / bad-strength decided correctly, read back through folded state (`get_trust_signals`) plus the fold's public accept/reject. GREEN, then proved: the cross-package mutation sweep re-ran over the whole Vouch payload region (`fold_derived.rs` lines 449/461/462/469/470) — the 10 RUN-07 justified survivors plus 9 additional operator mutants — and now kills **19/19, 0 survived** (dated addendum in `X3-AUTOMATED-SWEEP.md`; raw artifacts under `x3-sweep-data/vouch-*`). Backlog §2d retired (landing RUN-09). This closes a coverage residual, not a claim: no spec tag moved. Both suites (`local_storage_projection`, `croft-chat`) + clippy green; zero new warnings.

**Part 3 (B1 → A5: the message-continuity half of re-plant).** Built on the existing `replant-continuity` crate. The membership half of §7.6.2 was already `Verified`; this lands the message half. RED-first: `e12_2_message_continuity.rs` (5 tests) asserted the four continuity claims over a re-plant; the RED was evidenced by stubbing the B1 detection (dup/drop tests failed) before the real fold logic made them pass. GREEN: a minimal B1 dataplane hash structure (`replant-continuity/src/dataplane.rs`) — content-addressed, causally-linked records folding into an arrival-order-independent BLAKE3 digest (test-only serialization, no wire pinning). Proven at loopback: (a) every pre-repoint entry present after, exactly once; (b) in-flight entries land once, in causal order, on the post-repoint group; (c) both members converge byte-identically across arrival orders; (d) an injected duplicate or dropped frame is detected, not absorbed — all driven over the real re-plant membership. Conditional edits (all-green): §7.6.2 message half → `Modeled` at loopback grade with the A.9 evidence parenthetical; EVIDENCE-MAP row; backlog E12.2/E12.7 → done; changelog + MASTER-INDEX A5/B1 → done; crate README. No stop rule triggered — the in-flight (b) case needed no transport machinery at loopback. Real over-the-wire delivery and the `[gates-release]` B1 record encoding remain open. `replant-continuity` suite (8 tests) + clippy green; zero new warnings.

**Part 4 (RBSR steady-state anti-entropy at loopback — §6.8.1's open half).** Connect-time catch-up is proven; recovering a frame dropped *between already-connected peers* was not. RED-first: `steady_state_anti_entropy.rs` establishes two connected members in steady state, then the harness loses one live frame to B with no new join. Because gossip carries no per-recipient ack, B's replicator buffers no stranded successor, so the gap is invisible to live delivery, yet the folds diverge. GREEN: a minimal `croft-chat::anti_entropy` module — a range summary over the `(device, lamport)` key space and a diff-only `missing_frames` reconcile — detects the gap and ships only the lost frame; B folds it through the ordinary `Replicator` and the folds re-converge byte-identically, with no reconnect and no whole-log re-broadcast. RED evidenced by stubbing `missing_frames` to empty (the flow fails to converge). Conditional edits (all-green): §6.8.1 → `Modeled` at loopback grade with the A.9 evidence parenthetical; EVIDENCE-MAP row; backlog M2 row updated; changelog + MASTER-INDEX. It is `Modeled`, not `Verified`: the whole-set range compare is the simplest RBSR form at loopback; the range-partitioned production construction (Willow 3d-range vs Negentropy) and real-transport loss remain open (§5 / Appendix B). `croft-chat` suite + clippy green; zero new warnings.

**Part 5 (fan-out repeated-run measurement).** The loopback-cheap arm of the `fanout-single-run` register row: re-ran the FANOUT-M1 sweep K=5 at N=2/4/8/16 over real iroh-gossip (`--features iroh-it`, `relay_mode=disabled`), reporting per-N spread as a dated addendum to `FANOUT-M1.md` (`fanout-data/repeated-run09.csv`, 150 node-samples; driver `scripts/fanout-repeated.sh`). The spread is narrow: `live_sent=2N+1` reproduced exactly (zero variance across all 5 runs), head-convergence held in every run at every N (`fp_unique=1`, 20/20 sweeps), and the super-linear hub-`resync_sent` shape reproduced with a tight band (N=16: 349–422, median 401 — the single-run 479 refined downward). Full-settle degraded past N≈8 reproducibly (the flag Part 4 answers). Because the spread supports the recorded magnitudes, the conditional fired: `fanout-single-run` **retired (Reconciled, RUN-09)** and the §11.11 measurement-#1 caveat clause updated to record the replicated band; `hermetic-gossip` is now the only Active divergence. The narrow residual carried forward (not a divergence row): the resync magnitude's star-bootstrap topology-sensitivity; hardware hot-N (500+) stays X1-adjacent. Real-iroh build + measurement only; no suite change.

## 2026-07-16, RUN-11 (settle, refresh, and build, branch `claude/run-11-settle-build`)

Scope: two documentation parts (1, 1b) then three build parts (2–4), each shipping independently. Full per-part detail in `alpha/experiments/RUN-11-SUMMARY.md`. First run under the new site gate: `site/build.py` (broken-ref gate + emitted-HTML anchor audit) is part of the definition of green and ran clean (0 hard-gated) at every commit boundary. The I9 firewall held throughout; the parked list is unchanged (I9; X1; hot-N 500+; `[gates-release]` + BLAKE3; emitter integration — now formally deferred by decision, Option C; the resolution-ACL / croft-group L3 design frontier).

**Part 1 (riders — the confirmed rulings + the Map fixes).** The RUN-10/RUN-08 findings owner-confirmed 2026-07-15 were applied and recorded in the `CONSISTENCY-FINDINGS-2026-07.md` settlement section. FND-T2 ratified (RUN-08 §10.5 wording stands; the two senses of "emitted" may split when the footnote is next touched — no spec edit). FND-T3 confirmed (the four inferred test §-mappings stand; doc-comments unchanged). FND-T4 applied narrowly: the `(evidence: …, RUN-NN[, grade])` parenthetical reshaped the existing inline prose in §7.2 R7, §7.3.2, §8.2(e); the fourth candidate, the §7.6.2 **membership half, is FINDING-stopped** — its RUN-NN component is missing (imported as already-`Verified` from the standalone corpus, no discovery-RUN stamp). FND-R10-1: §5.10's communal-namespace key-rotation seam reframed per the seam brief (no whole-namespace secret; decomposes into per-subspace write authority + the fold-gated asset key), `Design`, brief cited. FND-R10-4: one correction banner atop `alpha/Proofs/FROZEN-NOTICE.md` (frozen body untouched) — the folded core emits/re-proves categories 1–9 (66/0), not 1–6. FND-R10-5: the parked design labeled "resolution-ACL (croft-group L3)"; a disambiguation note added at the collision's origin (`the-shape-of-disagreement.md` §4). Emitter decision recorded: `EMITTER-INTEGRATION-BRIEF.md` annotated Option C (defer to `[gates-release]`), Option B fallback, §10.5 residual as-is. Map fixes: the §7.6 back-Map message-continuity clause → `Modeled` at loopback grade (RUN-09); no duplicate line present (8a already clean). `part-2-changelog.md` entries for riders 3/4/8. No experiment status tag moved.

**Part 1b (living-docs currency refresh).** Living docs only. `EXPERIMENT-BACKLOG` execution order rewritten to the new queue (done items struck with landing runs through RUN-11 Part 1; forward order L2a → §2e → message-continuity-over-transport → range-partitioned RBSR → croft-group L2b+ → the parked list verbatim). `MASTER-INDEX` pressing-edges narrative refreshed to the post-RUN-10 board (one Active divergence `hermetic-gossip`; one open design call I9; the parked release pass; the resolution-ACL frontier; everything else `Modeled`-or-better or shaped). `open-threads`: the §5.10 rotation thread notes the seam brief's dissolution; a §6.8.1 steady-state anti-entropy thread added (`Modeled` at loopback RUN-09, range-partitioned form open); the recovery thread was already current (Tier-1 lock landed RUN-08, trust tier the open call).

**Part 2 (build L2a — the MLS-sealed happy-path frame).** Executed backlog §3 L2a per `CROFT-GROUP-L2-READINESS.md`, RED-first, reusing the proven crates. New standalone crate `croft-group/crates/group-seal` (own lockfile; empty `[workspace]` so openmls never enters the pure `group-core` workspace; depends **on** `group-core`, reusing `lineage-mls` — the openmls `Device` `mls-welcome-over-iroh`/`replant-continuity` also reuse). RED→GREEN: all six shaped assertions land as tests; assertion 1 (seal≠plaintext) evidenced RED via a temporary identity-seal stub, then GREEN on real openmls. `l2a_sealed_frame.rs` (6 tests) green; clippy-pedantic + fmt clean; the pure workspace still builds openmls-free. Grade `Verified` at loopback (real openmls 0.8.1 seal/Welcome/PCS re-key; in-process harness delivery, no wire pinning; real-NAT = X1); mechanism half only (R1–R7), R8-tier/R9/R10 firewalled. Conditional edits on green: backlog §3 L2a → done; EVIDENCE-MAP row; readiness-brief landing note; MASTER-INDEX C1.

**Part 3 (the group-principal seam spike — backlog §2e).** Executed §2e per `group-principal-seam.md`, RED-first, at `Design` grade. New standalone crate `alpha/experiments/group-principal-seam` (blake3, test-only serialization). Five assertions green (`tests/seam.rs`; RED evidenced by dropping the authority-generation floor): the Group principal *is* the genesis hash `H(tag ‖ group_id)` (stable across churn); a persona = a self-authorizing subspace; capability issuance downstream of the folded Group Role; **re-issue, never revoke-in-place** across a fold-driven authority change (stale cap fails, pre-change cap superseded not overwritten, re-issued cap succeeds); deterministic on both members, order-independent. clippy-pedantic + fmt clean. Scope wall: bindings stay `Design`; the row's `green-real`-against-real-crypto aspiration and the `SubspaceId` byte encoding (E.1) exceed the Design wall and are **FINDING-stopped**. Conditional edits on green: backlog §2e → done; EVIDENCE-MAP row; one-line evidence note on the seam brief.

**Part 4 (message continuity over real transport — first-to-drop).** **Dropped cleanly at the ship-without rule** after Parts 1–3 landed: re-driving the E12.2 records over real iroh-gossip is the run's heaviest integration (real iroh async + gossip + cross-crate B1 wiring, env-sensitive loopback setup), and the §7.6.2 message-half grade would per the A.9 when-in-doubt rule most likely stay `Modeled` regardless (harness-injected faults, no wire pinning). The RED tests are recorded as the shaped backlog row §2f (the four transport-driven continuity assertions; B1 records riding inside the existing gossip `Frame` payloads; harness injecting only the dup/drop fault). §7.6.2 message half **stays `Modeled`** — no spec tag moved, no EVIDENCE-MAP change.

## 2026-07-16, RUN-12 (provenance ruling, transport continuity, RBSR, Session-emit, EXP-H2, branch `claude/run-12-multi-part-cznuc8`)

Scope: one documentation part (1) then five build/read parts (2–6), each shipping independently. Full per-part detail in `alpha/experiments/RUN-12-SUMMARY.md`. The site gate (`site/build.py` broken-ref + emitted-HTML anchor audit) ran clean (0 hard-gated) at every commit boundary, and its companion-ref allowlist (new this run, Part 1) passed. The I9 firewall held throughout; the parked list is unchanged (I9; X1; hot-N 500+; `[gates-release]` + BLAKE3; emitter integration deferred Option C; the resolution-ACL / croft-group L3 frontier). TDD throughout: RED evidenced before GREEN per build part.

**Part 1 (the import-provenance ruling).** Owner ruling 2026-07-16. A one-sentence A.9 rider (beside FND-T4's evidence-form note): for evidence imported from outside the numbered-run system, the standard parenthetical's `RUN-NN` slot carries **import provenance** (`imported: <corpus> @ <commit>`) instead, and a retroactive RUN number is never invented. The fourth FND-T4 retrofit applied: §7.6.2's membership half reshaped from the RUN-11 re-proof stamp to `(evidence: the e12_7_* tests, imported: replant-continuity @ d52ed6f, Verified)`, closing the RUN-11 §7.6.2 FINDING on the cleaner record; EVIDENCE-MAP row 52 carries the same. The 7 companion/exploratory unresolved refs that passed as a silent soft baseline became an explicit `COMPANION_ALLOWLIST` in `site/build.py` (file, ref, reason); the gate now fails if the companion set drifts in either direction. Settlement recorded; `part-2-changelog` entry. No status tag moved.

**Part 2 (§2f — message continuity over real transport).** Executed backlog §2f as RUN-11 shaped it. New test `croft-chat/croft-chat/tests/iroh_message_continuity.rs` (2 tests): two `IrohGossipBus` nodes at `RelayChoice::LocalDirect`, node A publishes the B1 `Record`s (test-only serialization inside the gossip `Frame` payloads, no B1 wire pinning) and node B drains-and-folds into a `History`; the four continuity claims re-asserted over real gossip delivery, the harness injecting only the dup/drop fault. RED→GREEN evidenced by a no-bootstrap probe (no swarm → records never traverse the transport → the convergence assertion fails). `replant-continuity`'s pure dataplane reused behind the `iroh-it` feature, so the default build stays openmls-free (verified). Grade decision (A.9): §7.6.2 message half **stays `Modeled`** — the records now ride real transport (rationale drops "delivered by the harness"), but the record serialization is test-only so the `[gates-release]` B1 encoding is unpinned, and delivery is loopback with real-NAT the X1 residual. §7.6.2 body + §0 back-Map + EVIDENCE-MAP updated; backlog §2f → done.

**Part 3a (the RBSR construction brief).** Read-and-report brief `beta/impl/drystone-design/rbsr-construction.md` (with `Serves:` header). Willow 3d-range and Negentropy measured against §6.8.1's requirements over the linear `(device, lamport)` key space (both checked against their live published specs 2026-07-16). **Verdict:** for the §6.8.1 history/message anti-entropy surface, adopt the Negentropy-style one-dimensional recursive reconciler — the repair reconciles a linear totally-ordered key space, which Negentropy fits natively (stateless responder, additive-fingerprint omission hardness at the content-blind-store seam, `IdList`/`Fingerprint` mode switch generalizing the RUN-09 whole-set form), while Willow 3d-RBSR pays for three dimensions to serve one. Clarification, not a FINDING: the governance-fact surface (§6.8.5/§7.2) is a distinct Willow-shaped 3d key space — a separate choice; no spec text contradicted.

**Part 3b (the minimal build — gated on 3a, landed).** 3a's recommendation was unambiguous and fit the wall, so 3b built it. `croft-chat::anti_entropy` gained a Negentropy-style `reconcile_partitioned` (count-balanced sub-ranges, an additive-monoid `RangeFingerprint`, an `IdList`/`Fingerprint` mode switch, dep-free), the whole-set `missing_frames` retained as its degenerate case. RED→GREEN: `partitioned_anti_entropy.rs` (3 tests) — diff-only equivalence, fingerprint composition, and a scale case (an 8-record divergence in a 71-record set repaired in 4 rounds ≈ log₄71, shipping only the 8 divergent records); RED evidenced by a neutralized-fingerprint probe. `Modeled` at loopback (test-only fingerprint; `[gates-release]` wire fingerprint unpinned; real-transport = X1). §6.8.1 body + open-threads + EVIDENCE-MAP + backlog updated.

**Part 4 (Session-emit completion).** Closed the RUN-02-era client residual: the fold has understood `MembershipRemove`/`RoleGrant`/`RoleRevoke` since the original spikes, but no client could issue them. `LocalStore` gained `grant_role`/`revoke_role` and an approvals-carrying `remove_member`; `Session` gained propose/approve pairs for all three, mirroring the proven rule-change shape (same R7 content-bound counting, same co-signed-op antecedents, subject = target principal). RED→GREEN: `session_emit_governance_via_api.rs` (3 tests, two-session end-to-end per act kind) — removal at a 2-of-n quorum with the removed persona's later fact rejected; a granted role authorizing an act; a revoked role withdrawing it; converging identically on a second session. Scope wall held: fold/API only — no MLS re-key linkage, no authority-model change, no trust tier. The §7.2 R7 residuals paragraph names the role model and the competing-quorum case, not this client-surface gap, so it was left unedited. Session-emit residual → Closed (register + action-plan); EVIDENCE-MAP row; MASTER-INDEX.

**Part 5 (croft-group L2b — FINDING, dropped cleanly).** The readiness brief defines **no discrete L2b**: L2a is "the whole of L2's mechanism half" (§4), and the entire remainder (R8-tier, R9 = resolution-ACL / croft-group L3, R10 = authorized revocation) is the authority/projection half, firewalled behind I9 with an OWNER-DECISION gate on each. Part 5's scope wall (mechanism-half-only) leaves nothing autonomously buildable inside the wall, so — per its own drop rule — the part was dropped cleanly (no code). Smallest shapeable slice proposed: R10 (authorized revocation over the wire), but it sits outside the wall and needs the I9/OWNER-DECISION revocation-authority call cleared first. Recorded as a settlement FINDING + a shaped backlog row. No tag moved; I9 firewall held.

**Part 6 (EXP-H2 — the horizon checkpoint as a foldable fact).** EXP-H1 proved the manifest computes identically; EXP-H2 lands the fact form. New pure module `local_storage_projection::horizon_checkpoint` (`CheckpointFact { signer, manifest }`, `corroboration_count`). RED→GREEN: `horizon_checkpoint.rs` (3 tests, cross-package via the real `DerivedFold`) — corroboration folds deterministically and identically across members and arrival orders; a non-matching fold records nothing (no false corroboration); an open contradiction persists across successive checkpoints (H1 decay-is-presentation, at the fact layer); RED evidenced by a probe dropping the manifest-match guard. Scope wall: experiment-grade fact shapes, test-only serialization, no wire pinning; §7.3.3 semantics unchanged (a co-signature corroborates an independent identical fold, never a substitute). Grade judgment (A.9): the §7.6.9 worked example **stays `Design`** — EXP-H2 earns the fact-form corroboration-determinism claim (a `Modeled`-grade sub-result in its own EVIDENCE-MAP row), but §7.6.9's `[gates-release]` manifest encoding is unpinned and the full cadence composition is not proven end-to-end, so no tag moved. EVIDENCE-MAP row + backlog §2b′.

## 2026-07-16, RUN-13 (the gradients on Pages: five tiers, the classroom scaffold, Mermaid, review packaging, branch `claude/run-13-gradients-classroom-qg3jev`)

Scope: five documentation/site parts, each shipping independently; markdown + site tooling only — no spec, register-status, or crate edits. Full per-part detail in `alpha/experiments/RUN-13-SUMMARY.md`. The run's sequencing gate ("RUN-12 merged") was honored — the session stopped at the gate (RUN-12 still mid-flight), reported, and executed only after PR #13 merged. Site gate + resolver suite green at every commit; the companion allowlist exact (7/7, unchanged).

**Parts 1–2 (the five-tier gradient + the one-liner workshop).** `visual-identity-and-the-progressive-depth-website.md` generalized from three depths to five tiers (one-liner → elevator → over tea → classroom → library), the whole-truth constraint preserved as the invariant (order/energy/altitude, never truth), per-tier character blocks, the tier-1 test battery (secondhand / hostile / library), and the loop principle (tagline ↔ classroom refrain as siblings). The one-liner candidate table (five seeds, each scored under the battery) is `FOR REVIEW` — **no winner declared; the owner selects**, and the elevator doc's one-liner register (which lives in `sixty-second-pitch.md` and is promoted to tier 1 by cross-reference) updates only after selection.

**Part 3 (the classroom scaffold).** `alpha/classroom/`: the owner-provided arc committed verbatim (`00-arc.md`) + ten chapter skeletons with the arc's beats as headed placeholders, prose bodies `DRAFT-PENDING (written in conversation, not by runs)`, the two seed Mermaid diagrams verbatim in chapters 01/05 (the pinned renderer accepted the HTML label markup — no simplification). Every PROVE-IT pointer verified against the tree (the verification table is in the summary); a backlog docs-track row added (§7).

**Part 4 (Mermaid in the site, gated).** Build-time pre-render chosen empirically: pinned `@mermaid-js/mermaid-cli` 11.16.0 emits inline SVG, so published pages need no JavaScript and no network to display diagrams (the strongest no-network-at-read form), and the parse failure *is* the build failure — the gate extends to diagrams, verified exit-1 naming the file on an injected bad block. TDD: three resolver-test additions RED first, then GREEN (29 tests). Nav: the Gradients page + the Classroom section (draft-bannered) join the published set; the landing page presents the five tiers as reading paths. Audit re-run: RUN-10 scope clean (1233 cross-file anchor links / 1866 same-file, 0 broken); a stricter plain-file-link extension surfaced 5 pre-existing raw `.md` links in three thinking docs → **FND-R13-1** (LOW), recorded not fixed (outside the run's edit surface).

**Part 5 (review packaging).** `beta/socialization/REVIEW-2026-07.md`, published beside Gradients: seven numbered one-line questions (one-liner slate; tier characters; invariant wording; act structure; chapter order; refrain–tagline coupling; the dispatch-desk register) + a one-line respond-how. Deployment: the one-time Pages setup is already flipped (the RUN-12 merge deployed successfully), so the packet goes live at `https://croftcommunity.github.io/discovery/REVIEW-2026-07.html` on merge.

## 2026-07-17, RUN-14 (Stellin AppView caller-identity spikes: service-auth serving, sealed offer-gating, the helper seam, branch `claude/experiments-run-14`)

Scope: three code experiments (EXP-A/B/C), each TDD red-first (test commit `test(run-14): red …` before the implementation commit `feat(run-14): green …`), sequenced by leverage. The one unproven capability behind every Stellin differentiator — *the AppView has never known who its caller is* — is the through-line. Full per-part detail, the red→green commit table, and the predicted-vs-actual table (P-A1..A3) in `alpha/experiments/RUN-14-SUMMARY.md`. Environment: rustc 1.94.1; `bsky.social`/`plc.directory`/Jetstream reachable via proxy; `ATP_TEST_HANDLE`/`ATP_TEST_PASSWORD` **unset** (EXP-A live token leg BLOCKED, DID-doc resolution still confirmed live). No Part 2 or social-mapping edit — the evidence-target status-moves are **staged** in `proposed-changes-2026-07-experiment-reconciliation.md` (RUN-14 section, PROPOSED not landed). The AppView-provisioned scope key was left untouched (stop rule 5b).

**EXP-A (viewer-aware serving via atproto service auth — `appview-validation` phases 8/9 `authserve`, new `serviceauth`/`viewserve` modules).** Four steps, each red→green. Step 1: `verify_service_jwt` — real ECDSA verification of a compact service-auth JWT against the issuer's `#atproto` DID-doc key (secp256k1/p256 multicodec decode; k256/p256 verify), with distinct error variants (bad-sig, expired, wrong-aud, wrong-lxm, unresolvable-issuer) and a stub `KeyResolver` for hermetic tests (10 tests). Step 2: `app.stellin.getProfileView` (experiment-local route name, **no `app.stellin.*` lexicon published** — namespace pending domain purchase) offers `openToWork` only to a verified recruiter at a different employer; anonymous → public view, malformed/wrong-lxm token → 401 not a degraded view; the generic `getRecord` route cannot leak the gated field (it is a computed projection) (7 tests). Step 3: verified reads emit `(viewer, subject, ts)` telemetry in the disposable index (rebuilt empty, never recovered); anonymous reads emit nothing; the subject reads their own count (4 tests). Step 4 (live): P-A3 **CONFIRMED** — `@bsky.app` → `did:plc:z72i7hdynmk6r22z27h6tvur`, `#atproto` key secp256k1, 33-byte SEC1, `decode_multikey` handled the real `zQ3sh…` multibase. P-A1/P-A2 initially blocked on creds, then **CONFIRMED end-to-end** when the owner supplied credentials (RUN-14 follow-up): a real bsky.social account created a session, `getServiceAuth` returned a 3-segment compact JWT (P-A1), the claims carried the predicted `iss`/`aud`/`exp`/`lxm` **plus unmodeled `iat`/`jti`** (a real predicted-vs-actual divergence; `exp−iat=60s`), and `verify_service_jwt` verified the real token against the issuer's real DID-doc key (`LIVE VERIFY OK`). The `aud`=own-DID stand-in `SPEC-DELTA[run14-A4]` was thereby exercised, not left dormant. Declared stand-in `SPEC-DELTA[run14-A2]` (recruiter roster = admission-governance placeholder, R7 territory) registered. **Named non-goal:** interactive OAuth/DPoP (the PWA client-login leg) — a browser hop this env lacks; recorded in the backlog.

**EXP-B (sealed offer-gating — the §H hybrid serve half — `appview-validation` `sealed` bin + module).** Red→green, run under `--features client-seal`. A content-blind store holds opaque ciphertext keyed by `(group_id, seq)` + a roster; `getSealedRecords` offers ciphertext only to a verified roster member; non-member, anonymous, and nonexistent-group return one **identical** flat 403 (no length/existence leak). The blindness is **mechanical, not rhetorical**: the seal/open AEAD (`chacha20poly1305`) is an optional dep behind the `client-seal` feature the server binary never enables — `cargo tree` shows the crate **absent** from the default dependency graph, present only with the feature; `SealedState` carries no key field. The client harness (holding the group key the server never sees) decrypts the served bytes; a wrong key does not. Roster removal stops future offering (403) while already-fetched ciphertext + a retained key still reads — §H's offering-vs-reading distinction, executable (4 tests). PASS.

**EXP-C (the helper seam — new crate `helper-seam`).** Red→green on **real openmls** (path-dep on `group-seal`, croft-group L2a; the openmls stack compiles under helper-seam's own lockfile with no workspace conflict). A `ContentHelper` admitted by a real MLS Welcome decrypts group messages as any member does, normalizes them through the `NormalizedEvent`/`LocalPath` boundary **copied with provenance** from `public-roundtrip/src/bridge.rs` (adding the private-side `from_group_message` constructor #6 left as a seam), and feeds a minimal appview index (copied with provenance) — one search returns both a public-source and a helper-fed hit (the source-agnostic claim, now live on the private side). Revocation (`founder.remove_member` + epoch roll) makes the helper **forward-blind**: the post-roll frame does not decrypt (MLS forward secrecy) → no row, while the pre-revocation row remains. The helper exposes **no authority surface** (join + ingest only; asserted by a source-scan guard mirroring the L2a firewall guard, plus by construction) (3 tests). PASS. Grade: loopback / in-process, no wire pinning — same wall as croft-group L2a; the copied boundary is faithful (does not diverge), so it is noted, not registered as a divergence.

**Registers touched.** `SPEC-DIVERGENCE-REGISTER` Active table +2 declared stand-ins (`run14-A2`, `run14-A4`); `MASTER-INDEX` source-doc inventory +1 RUN-14 row; `appview-validation/README` phase table +2 (phases 8, 9); `proposed-changes` RUN-14 staging section (H-A/H-B/H-C, PROPOSED); `EXPERIMENT-BACKLOG` OAuth/DPoP standing-gap + scope-key-untouched rows. No status tag moved in Part 2 or social-mapping (all moves staged for review).

**RUN-14 follow-up (2026-07-17, owner-approved landing).** The three staged status-moves landed in `social-mapping.md`: (H-A) §H gained a "serve half is now demonstrated" paragraph — offer-gating against a cryptographically-verified viewer identity, content-blindness enforced at a compilation boundary, offering-vs-reading shown — tagged `green-real` (loopback / in-process; was `Synthesis`); (H-B) the §L helper-delegation bullet gained the content-helper demonstration — admit-by-grant → shared index → revocation forward-blindness on real openmls — tagged `green-real` (loopback; was `Design`); (H-C) the caveat that this is server-to-server service auth, not the interactive OAuth/DPoP client-login leg, folded into the §H paragraph. No grade moved to `Verified` (evidence is loopback). The AppView-provisioned scope key stayed `Design; open.` (untouched, stop rule 5b). `proposed-changes` RUN-14 section marked LANDED. No Part 2 tag moved; the live service-auth confirmation (P-A1/P-A2/P-A3 all green end-to-end, owner-supplied creds) is recorded in the EXP-A paragraph above.

## 2026-07-17, RUN-15 (AppView hosting kit — design + build in discovery, staged for production; branch `claude/appview-infra-run-15-bgi0bd`)

Scope: a generic multi-tenant hosting kit for the Croft portfolio's small always-on services (single static binary, SQLite, optional ingest, HTTPS), built and validated entirely in `alpha/experiments/appview-infra/` — manifests + a generator as the single source of truth for systemd units, Caddy vhosts, litestream.yml, and rclone timers, with a Litestream-to-R2 + Porkbun backup plane, an own-data API sidecar, and an access-gated large-group tier. Code-forward, TDD red-first per deliverable; no OVH account, no spend (money gate). Full detail in `alpha/experiments/RUN-15-SUMMARY.md`.

**Spec-facing (D11, this log's concern).** One design-stance reconciliation was **staged, not landed**: the access-gated large-group tier (≥ `group_scale_boundary`, working number 5000, owner-deferred) deliberately takes the **trusted-gatekeeper arm** of social-mapping's open AppView-provisioned-scope-key item — **for the large tier only** — serving private-but-not-E2EE (roster-gated, honestly not cryptographically confidential; the member-leak equivalence argument). Below the boundary the content-blind §H stance (RUN-14 EXP-B `green-real`) is unchanged. Appended to `proposed-changes-2026-07-experiment-reconciliation.md` (RUN-15 section, `needs-call`, PROPOSED not landed) with this row, same commit. The reviewed spec (`part-*`, conventions) is **untouched** (guardrail 2). The design brief with the full Variant A vs B write-path fork analysis is corpus at `alpha/experiments/appview-infra/GROUPS.md`; the write-path fork and the boundary number are owner decisions (GROUPS.md §5), not made here.

**No status tag moved** in Part 2 or social-mapping. This run earns no experiment status move — it is a hosting-kit build plus a staged design stance; the stance awaits the owner's `needs-call`.

## 2026-07-18, RUN-16 (the group tier model — canonical write-up and spec incorporation; branch `claude/group-tier-model-run-16`)

Scope: a docs-and-registers run (no code, no money, no credentials) landing the canonical **group tier model** as `alpha/experiments/appview-infra/GROUPS.md` v2 and staging its spec-facing implications. Docs-TDD: the corpus-tests claims-checks (`appview-infra/corpus-tests/groups_claims.bats`) were extended RED first with the new model's invariants, then the documents landed to turn them green (7 v1 D11 checks preserved + 12 new v2 checks). Full detail in `alpha/experiments/RUN-16-SUMMARY.md`.

**The model (GROUPS.md v2).** Supersedes and extends the RUN-15 D11 two-tier framing: **one lineage, one envelope, one delivery plane, one catalogue**, with the tier expressed as **two independent policy axes** — a membership policy (`open` | `gated` | `sealed`) and a write policy (`open` | `members` | `named-set` | `single`) — not one dial. The familiar shapes (open forum, newsletter, announcement channel, working group, sealed circle) are coordinates on those axes. The RUN-15 Variant A/B write-path fork analysis is **preserved intact** as GROUPS.md Section B; the owner decisions it asked are restated unchanged, plus one new (self-host an iroh relay vs public relays), in A.10. Reverses no landed decision.

**Spec-facing (this log's concern).** The reconciliation note was **extended, not rewritten**: a **RUN-16 update** appended to the RUN-15 section of `proposed-changes-2026-07-experiment-reconciliation.md` (`caveat`/`needs-call`, PROPOSED not landed) records (1) the two-tier framing superseded by the three-tier/two-axis model; (2) the trusted-gatekeeper acceptance now attaching to the **backplane (gated/open) tier**, with **membership as universally verifiable public fact** — the fused sealed-tier check (MLS membership MAC) **splits** in the backplane into signature-vs-DID-document-key (authorship) plus roster-lookup-at-causal-position (membership); (3) the **delivery plane as authority-free roles realized as separate processes** (web-native DS, swarm peer, history-convergence node, helpers — sequence numbers are delivery cursors, never order), with the **transport split** (iroh overlay loaded only by sealed scopes; backplane on the plain web stack; validate-before-relay swarm gossip; envelope-hash dedup) and **membership-interval backfill**. `needs-call` preserved. The reviewed spec (`part-*`, conventions) is **untouched** (guardrail 2); this row lands in the same commit as the staged note.

**No status tag moved** in Part 2 or social-mapping. This run earns no experiment status move — it is a canonical write-up plus a staged, extended design stance; the stance awaits the owner's `needs-call`. Registers touched: `EXPERIMENT-BACKLOG` (+ the churn-simulation experiment, the write-policy axis as landed design, the sealed-helper observation-born taxonomy correction, the relay-hosting owner call, and the history-convergence interval-backfill rule), `MASTER-INDEX` (RUN-16 row). **Cross-reference disposition:** the GROUPS.md v2 pointer is carried by this staged note (which cites `alpha/experiments/appview-infra/GROUPS.md` Section A and its A.x anchors), **not** written into `social-mapping.md` — a clean beta-tier doc takes no back-reference to an alpha-stage path per the tier-cleanliness convention (AGENTS.md), so the "where not [editable per conventions], the staged note carries the pointer" branch applies (checked: `social-mapping.md` holds no alpha/experiment references today). Site gate (`site/build.py`) green.

## 2026-07-18, RUN-18 (reception completeness + the publications positioning; branch `claude/publications-run-18-eh07xx`)

Scope: a delta run on the merged RUN-16/17 surfaces — no re-run, no frozen-record edits. It amends the living corpus (`appview-infra/GROUPS.md` gains the canonical **reception-completeness** paragraph at A.2; the standalone **publications positioning** lands as `appview-infra/PUBLICATIONS.md`) and extends the landed `tier-proof/` experiment with the executable proofs (B1–B6: chaining validation at the relay, gap detection/repair from the chain alone, the honest withheld-tail claim with multimodal closure, chaining×interval composition, the three-way retraction distinction, auditable reach). Docs-TDD and code-TDD red-first throughout; full detail in `alpha/experiments/RUN-18-SUMMARY.md`.

**Spec-facing (this log's concern).** A **RUN-18 addendum** appended to the RUN-16 section of `proposed-changes-2026-07-experiment-reconciliation.md` (`caveat`/`needs-call`, PROPOSED not landed) records (1) the **chaining requirement** for write-restricted scopes and the **detection-not-delivery** framing of reception completeness, with the withheld-tail limit named per the completeness-ahead doctrine and multimodal delivery as mitigation; (2) the **degeneration principle** as a design constraint (open/single scopes ride bare atproto records plus chaining only — native proofs are not rebuilt); (3) the **tamper-evident-history delta** as the publication-facing consequence of chaining, with the never-existed / retracted / withheld three-way distinction. `needs-call` preserved; the reviewed spec (`part-*`, conventions) is **untouched**; this row lands in the same commit as the staged addendum. **No status tag moved.** Registers touched: `EXPERIMENT-BACKLOG` (§6f rows), `MASTER-INDEX` (RUN-18 row), `SPEC-DIVERGENCE-REGISTER` (`run18-retraction-local`).
