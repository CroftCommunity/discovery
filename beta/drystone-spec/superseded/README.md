# drystone-spec / superseded — provenance, not current

date: 2026-07-07

These files were live spec companions in earlier document-passes and are **superseded** by the consolidated
p10/p11 spec (document-pass-6 through -8). They are **kept as provenance, marked superseded, and treated as
raw transcripts would be**: do not read them as current, do not build against them, do not cite them as the
spec. The current spec is `../part-1-reasoning-underpinnings.md` and `../part-2-certifiable-design.md`.

| file | superseded by |
|---|---|
| `persona-definition.md` | Part 2 §5.2 (identity model) + Appendix D (term lattice and invariants of record) |
| `open-items.md` | Part 2 Appendix B (open questions / `ENABLING`) and `../open-threads.md` |
| `bounded-contexts-and-vocabulary.md` | `../conventions-and-decisions.md` (vocabulary discipline) |
| `review-handoff.md` | the consolidated spec itself (it predated the persona migration entirely) |
| `drystone-exposure.svg`, `drystone-catchup-flow.svg` | the p11 rebuild's prose (Part 2 no longer references them by figure number) |

Retained deliberately: the point is provenance (what the spec used to say and how it got here), the same way
`../../../alpha/seeds/` holds raw inputs. If any of these ever contradicts the current spec, the current spec
governs and these are the historical record, nothing more.
